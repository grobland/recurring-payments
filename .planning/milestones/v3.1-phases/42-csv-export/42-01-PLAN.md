---
phase: 42-csv-export
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/utils/csv.ts
  - src/lib/utils/csv.test.ts
autonomous: true
requirements:
  - EXPRT-03
  - EXPRT-04

must_haves:
  truths:
    - "CSV cells starting with =, +, -, @, tab, or CR are prefixed with a tab character to prevent formula execution"
    - "CSV response body starts with UTF-8 BOM (byte sequence EF BB BF) so Excel auto-detects UTF-8 encoding"
    - "Existing CSV quoting behavior (commas, quotes, newlines) is preserved after adding sanitization"
  artifacts:
    - path: "src/lib/utils/csv.ts"
      provides: "Formula sanitization and BOM in createCSVResponse"
      exports: ["objectsToCSV", "createCSVResponse"]
      contains: "sanitizeFormulaInjection"
    - path: "src/lib/utils/csv.test.ts"
      provides: "Unit tests for formula injection and BOM"
      min_lines: 30
  key_links:
    - from: "src/lib/utils/csv.ts"
      to: "src/app/api/subscriptions/export/route.ts"
      via: "import { objectsToCSV, createCSVResponse }"
      pattern: "objectsToCSV|createCSVResponse"
---

<objective>
Add formula injection sanitization (CWE-1236) and UTF-8 BOM to the CSV utility, with unit tests written first.

Purpose: Security and international compatibility — formula injection can execute arbitrary commands in Excel, and missing BOM causes mojibake for non-ASCII characters.
Output: Patched `csv.ts` with full test coverage for sanitization and BOM behavior.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-csv-export/42-RESEARCH.md

<interfaces>
<!-- Current csv.ts exports — executor patches these, does not rewrite -->

From src/lib/utils/csv.ts:
```typescript
// Private (not exported)
function escapeCSVValue(value: string | number | null | undefined): string

// Public exports
export function objectsToCSV<T extends Record<string, unknown>>(
  data: T[],
  columns: { key: keyof T; header: string }[]
): string

export function createCSVResponse(csv: string, filename: string): Response
```
</interfaces>
</context>

<feature>
  <name>CSV Formula Injection Sanitization and UTF-8 BOM</name>
  <files>src/lib/utils/csv.ts, src/lib/utils/csv.test.ts</files>
  <behavior>
    Formula injection sanitization (EXPRT-03):
    - sanitizeFormulaInjection("=SUM(1+1)") → "\t=SUM(1+1)"
    - sanitizeFormulaInjection("+cmd|'/C calc'!A0") → "\t+cmd|'/C calc'!A0"
    - sanitizeFormulaInjection("-100") → "\t-100"
    - sanitizeFormulaInjection("@SUM(A1:A10)") → "\t@SUM(A1:A10)"
    - sanitizeFormulaInjection("\tvalue") → "\t\tvalue"
    - sanitizeFormulaInjection("\rvalue") → "\t\rvalue"
    - sanitizeFormulaInjection("Normal text") → "Normal text" (unchanged)
    - sanitizeFormulaInjection("100") → "100" (numbers unchanged)
    - sanitizeFormulaInjection("") → "" (empty unchanged)

    escapeCSVValue integration:
    - escapeCSVValue("=SUM(1+1)") → tab-prefixed AND quoted (contains special chars after tab prefix)
    - escapeCSVValue(null) → "" (null handling preserved)
    - escapeCSVValue(undefined) → "" (undefined handling preserved)
    - escapeCSVValue("Hello, World") → '"Hello, World"' (comma quoting preserved)
    - escapeCSVValue('Say "hello"') → '"Say ""hello"""' (quote escaping preserved)

    UTF-8 BOM in createCSVResponse (EXPRT-04):
    - createCSVResponse("a,b\n1,2", "test.csv") response body starts with \uFEFF
    - Content-Type header is "text/csv; charset=utf-8"
    - Content-Disposition header contains the filename

    objectsToCSV headers sanitized:
    - Header values also pass through escapeCSVValue (already do — just verify)
  </behavior>
  <implementation>
    1. Create src/lib/utils/csv.test.ts with test cases above — RED phase (all fail)
    2. Add `sanitizeFormulaInjection()` function to csv.ts — called inside `escapeCSVValue()` before quoting
    3. Add `\uFEFF` BOM prefix in `createCSVResponse()` body — GREEN phase (all pass)
    4. Confirm all tests pass — REFACTOR if needed

    Key implementation details:
    - `sanitizeFormulaInjection` is a private function (not exported)
    - It prepends a tab character `\t` to values starting with `=`, `+`, `-`, `@`, `\t`, or `\r`
    - The regex is `/^[=+\-@\t\r]/`
    - Tab prefix is the OWASP-recommended approach — invisible in Excel, does not alter displayed value
    - BOM is added ONLY in `createCSVResponse()`, NOT in `objectsToCSV()` — prevents double-BOM if CSV string is used elsewhere
    - `escapeCSVValue` must call `sanitizeFormulaInjection` on the stringified value BEFORE the quoting logic
  </implementation>
</feature>

<verification>
```bash
npm run test -- src/lib/utils/csv.test.ts
```
All tests green. Formula injection cases, BOM presence, and existing CSV quoting all verified.
</verification>

<success_criteria>
- Unit tests for formula injection cover all 6 trigger characters plus normal-text pass-through
- Unit tests for BOM verify the response body starts with \uFEFF
- Existing escapeCSVValue behavior (comma, quote, newline quoting) preserved
- `npm run test -- src/lib/utils/csv.test.ts` passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/42-csv-export/42-01-SUMMARY.md`
</output>
