---
phase: 37-account-crud-list-page
task: 0
total_tasks: 0
status: between_phases
last_updated: 2026-02-25T23:52:54.307Z
---

<current_state>
Phase 37 (Account CRUD + List Page) has context gathered but no plans created yet. Phase 36 (Navigation Restructure) is fully complete (3/3 plans). The database foundation from Phase 35 is in place — financial_accounts table, accountTypeEnum, and nullable accountId FK on statements all exist in production.
</current_state>

<completed_work>

- Phase 35: Database Foundation — complete (schema + migration 0011)
- Phase 36: Navigation Restructure — complete (3/3 plans: redirects, sidebar, route files)
- Phase 37 CONTEXT.md — gathered via discuss-phase with all 4 gray areas resolved:
  - Account form: modal dialog, segmented type control, conditional fields, type locked on edit
  - Account list: card grid with type tabs, name + institution + source on cards
  - Source linking: dropdown in form, optional, unlinking only affects future imports
  - Delete: confirmation dialog, statements preserved with account_id nulled
</completed_work>

<remaining_work>

- Phase 37: Needs planning (/gsd:plan-phase 37) then execution
- Phase 38: Account Detail Pages — not started
- Phase 39: Payment Type Selector — not started
- Phase 40: Static Pages — not started

Open debug sessions (non-blocking, pre-existing):
- bulk-tagging-single-transaction — bulk tag applies to only 1 transaction
- missing-tag-management-ui — no Settings page for tag creation
- coverage-grid-all-gray — statementDate never populated on insert
</remaining_work>

<decisions_made>

- Modal dialog for create/edit (not full page or inline)
- Segmented control for type picker (Bank/Debit | Credit Card | Loan)
- Single form with conditional type-specific fields (credit limit for CC, interest rate + loan term for Loan)
- Type locked during edit (cannot change account type after creation)
- Card grid layout with type tabs (not sections or accordion)
- Add Account button at page header level (always visible)
- Source linking via dropdown in form, optional field
- Already-linked source handling: Claude's discretion
- Unlinking only affects future imports (existing statements keep account_id)
- Global empty state: illustration + CTA
- Per-type empty state: type-specific message + add button pre-selecting type
- Delete via confirmation dialog (not inline undo)
- On delete: statements keep data, account_id set to NULL (no cascade)
</decisions_made>

<blockers>
- None for Phase 37 planning/execution
- ACCT-01 through ACCT-08 marked "Complete" in REQUIREMENTS.md traceability table but Phase 37 hasn't been executed — likely pre-marked during roadmap creation, will need verification after execution
</blockers>

<context>
The v3.0 milestone (Navigation & Account Vault) is ~10% complete with Phases 35-36 done. Phase 37 is the first feature-facing phase — it builds the account CRUD API and list page on top of the database schema from Phase 35. The CONTEXT.md captures all user decisions needed for planning. The next step is /gsd:plan-phase 37 which will research the codebase, create executable plans, and verify them.

Key codebase constraints to remember:
- financial_accounts (not accounts) table name — NextAuth owns "accounts" at schema.ts line 120
- Nullable accountId FK on statements — existing statements predate accounts
- PATCH /api/accounts/[id] must invalidate 5 query keys per STATE.md blockers section
- Run grep -r sourceType src/ and audit all 37 consumers before writing migration logic
</context>

<next_action>
Start with: `/gsd:plan-phase 37` — create executable plans for Account CRUD + List Page using the gathered CONTEXT.md
</next_action>
