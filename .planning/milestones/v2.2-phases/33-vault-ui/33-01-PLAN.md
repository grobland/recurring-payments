---
phase: 33-vault-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/vault/page.tsx
  - src/components/vault/vault-page.tsx
  - src/components/vault/vault-empty-state.tsx
  - src/components/vault/file-cabinet-view.tsx
  - src/components/vault/folder-card.tsx
  - src/components/vault/folder-statements.tsx
  - src/components/layout/app-sidebar.tsx
autonomous: true
requirements:
  - VAULT-01
  - VAULT-03
  - VAULT-04

must_haves:
  truths:
    - "User can navigate to /vault from the sidebar and see the Vault page"
    - "User sees all statement sources as expandable folder cards in the File Cabinet view"
    - "User can click a folder card to expand it inline and see statement rows with PDF icon, filename, date, transaction count, status breakdown, View PDF button, and View details link"
    - "User can switch between File Cabinet and Timeline tabs (Timeline content is placeholder in this plan)"
    - "User with no statements sees an empty state with illustration, explanatory text, and Upload Statements button linking to /import/batch"
    - "Multiple folder cards can be open simultaneously (not accordion single-open behavior)"
  artifacts:
    - path: "src/app/(dashboard)/vault/page.tsx"
      provides: "Vault page route with DashboardHeader and VaultPage component"
      min_lines: 15
    - path: "src/components/vault/vault-page.tsx"
      provides: "Client component with Tabs for File Cabinet and Timeline switching"
      min_lines: 30
    - path: "src/components/vault/vault-empty-state.tsx"
      provides: "Empty state with icon, text, and Upload Statements CTA"
      min_lines: 20
    - path: "src/components/vault/file-cabinet-view.tsx"
      provides: "Grid of folder cards using useSources hook"
      min_lines: 25
    - path: "src/components/vault/folder-card.tsx"
      provides: "Expandable card tile using Collapsible for inline expand"
      min_lines: 30
    - path: "src/components/vault/folder-statements.tsx"
      provides: "Statement rows inside expanded folder using useSourceStatements"
      min_lines: 40
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Vault entry with Archive icon after Dashboard in mainNavItems"
      contains: "Archive"
  key_links:
    - from: "src/components/vault/file-cabinet-view.tsx"
      to: "/api/sources"
      via: "useSources hook"
      pattern: "useSources"
    - from: "src/components/vault/folder-statements.tsx"
      to: "/api/sources/[sourceType]/statements"
      via: "useSourceStatements hook (lazy on expand)"
      pattern: "useSourceStatements"
    - from: "src/components/layout/app-sidebar.tsx"
      to: "/vault"
      via: "mainNavItems entry"
      pattern: "href.*vault"
---

<objective>
Build the /vault page with sidebar navigation, tab switching between File Cabinet and Timeline views, the File Cabinet view with expandable folder cards, and the empty state for new users.

Purpose: Give users a dedicated home base for browsing all stored statements, starting with the source-grouped file cabinet view that reuses existing APIs.
Output: Working /vault page accessible from sidebar, File Cabinet view with expandable source folders, empty state for new users, tab bar ready for Timeline view (Plan 02).
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-vault-ui/33-RESEARCH.md

Key existing files to reference:
@src/app/(dashboard)/sources/page.tsx — Page layout pattern to follow
@src/components/layout/app-sidebar.tsx — Sidebar nav items array to modify
@src/components/sources/statement-list.tsx — StatementRow pattern to reuse (rich row: PDF icon + filename + date + tx count + status breakdown + View PDF + View details)
@src/lib/hooks/use-sources.ts — useSources hook for file cabinet data
@src/lib/hooks/use-source-statements.ts — useSourceStatements hook for lazy-load on expand
@src/types/source.ts — SourceCoverage and StatementSummary types
@src/components/ui/tabs.tsx — shadcn Tabs component (wraps Radix)
@src/components/ui/collapsible.tsx — shadcn Collapsible component (wraps Radix)
@src/components/statements/pdf-viewer-modal.tsx — PdfViewerModal for View PDF action
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Vault to sidebar and create vault page route</name>
  <files>
    src/components/layout/app-sidebar.tsx
    src/app/(dashboard)/vault/page.tsx
  </files>
  <action>
    **Sidebar change (`app-sidebar.tsx`):**
    1. Add `Archive` to the lucide-react import list
    2. Add a new entry to `mainNavItems` array AFTER Dashboard (index 1, before Subscriptions):
       ```
       { title: "Vault", href: "/vault", icon: Archive }
       ```
    3. No other sidebar changes needed

    **Vault page route (`src/app/(dashboard)/vault/page.tsx`):**
    1. Create as a server component (same pattern as `sources/page.tsx`)
    2. Export metadata: `{ title: "Vault", description: "Browse all your stored bank statements" }`
    3. Render `DashboardHeader` with title "Vault" and breadcrumbs `[{ label: "Dashboard", href: "/dashboard" }, { label: "Vault" }]`
    4. Render `<main className="flex-1 p-4 md:p-6">` with `<div className="mx-auto max-w-5xl space-y-6">` (slightly wider than sources page's max-w-4xl to accommodate card grid)
    5. Import and render `<VaultPage />` client component inside the main container
  </action>
  <verify>
    - `npm run build` compiles without errors
    - The /vault route exists in the app router
    - Archive icon imported and used in sidebar
    - Vault appears after Dashboard in the sidebar nav list
  </verify>
  <done>
    - Vault link appears in sidebar after Dashboard with Archive icon
    - Navigating to /vault shows the vault page with header and breadcrumbs
  </done>
</task>

<task type="auto">
  <name>Task 2: Build vault page shell, empty state, and file cabinet view with expandable folder cards</name>
  <files>
    src/components/vault/vault-page.tsx
    src/components/vault/vault-empty-state.tsx
    src/components/vault/file-cabinet-view.tsx
    src/components/vault/folder-card.tsx
    src/components/vault/folder-statements.tsx
  </files>
  <action>
    **vault-page.tsx** (client component, "use client"):
    1. Import `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` from `@/components/ui/tabs`
    2. Import `FolderOpen`, `CalendarDays` from `lucide-react` for tab icons
    3. Import `useSources` from `@/lib/hooks/use-sources`
    4. Import `FileCabinetView` and `VaultEmptyState`
    5. Fetch sources via `useSources()`. Extract `sources = data?.sources ?? []`
    6. If `isLoading`, render a loading skeleton: 3 placeholder cards in a grid (use Skeleton component from shadcn)
    7. If sources are empty and not loading, render `<VaultEmptyState />`
    8. Otherwise, render the Tabs component:
       - `<Tabs defaultValue="file-cabinet">` — per context decision, always default to File Cabinet, NO persistence
       - `<TabsList>` with two triggers:
         - `<TabsTrigger value="file-cabinet">` with `<FolderOpen className="size-4 mr-1.5" />` + "File Cabinet"
         - `<TabsTrigger value="timeline">` with `<CalendarDays className="size-4 mr-1.5" />` + "Timeline"
       - `<TabsContent value="file-cabinet">` renders `<FileCabinetView sources={sources} />`
       - `<TabsContent value="timeline">` renders placeholder text: "Timeline view coming soon" (Plan 02 replaces this)

    **vault-empty-state.tsx** (client component):
    1. Import `Archive`, `Upload` from `lucide-react` and `Link` from `next/link`
    2. Import `Button` from `@/components/ui/button`
    3. Render centered layout with:
       - Large `Archive` icon (size-16, text-muted-foreground/30)
       - Heading: "Your Statement Vault"
       - Paragraph: "Upload bank statements to build your financial archive. All your PDFs will be stored securely and organized by source."
       - `<Button asChild>` wrapping `<Link href="/import/batch">` with `<Upload className="size-4 mr-2" />` + "Upload Statements"
    4. Use `py-16` vertical padding to center content visually

    **file-cabinet-view.tsx** (client component):
    1. Accept `sources: SourceCoverage[]` prop
    2. Render folder cards in a responsive grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`
    3. Map over sources, render `<FolderCard key={source.sourceType} source={source} />` for each
    4. Always show folder cards even with a single source (per locked decision)

    **folder-card.tsx** (client component):
    1. Import `Collapsible`, `CollapsibleContent`, `CollapsibleTrigger` from `@/components/ui/collapsible`
    2. Import `FolderOpen`, `FolderClosed`, `ChevronDown` from `lucide-react`
    3. Accept `source: SourceCoverage` prop
    4. Use `useState(false)` for open state
    5. The card trigger is a `<CollapsibleTrigger asChild>` wrapping a `<button>`:
       - Full width, rounded-xl border, p-4, hover:bg-muted/50 transition
       - Shows folder icon (FolderOpen when expanded, FolderClosed when collapsed)
       - Source name (sourceType) as primary text
       - "{statementCount} statements" + "{transactionCount} transactions" as secondary text
       - ChevronDown icon that rotates on open (use `transition-transform duration-200` + conditional `rotate-180`)
    6. `<CollapsibleContent>` renders `<FolderStatements sourceType={source.sourceType} />` inside a div with `border-t mt-2 pt-2`
    7. Use Collapsible (NOT Accordion) so multiple cards can be open simultaneously (per locked decision)

    **folder-statements.tsx** (client component):
    1. This is the statement rows inside an expanded folder — follows the rich row format from the locked decision
    2. Import `useSourceStatements` from `@/lib/hooks/use-source-statements`
    3. Import `PdfViewerModal` from `@/components/statements/pdf-viewer-modal`
    4. Import `FileText`, `ChevronRight` from `lucide-react`
    5. Accept `sourceType: string` prop
    6. Fetch statements lazily via `useSourceStatements(sourceType)`
    7. Loading state: 3 skeleton rows
    8. Error state: red error message
    9. Empty state: "No statements found for this source."
    10. For each statement, render a **rich row** (reuse pattern from `statement-list.tsx` StatementRow):
        - PDF icon (green if hasPdf, gray if not) — clickable button to open PdfViewerModal (disabled if no PDF)
        - Filename (truncated)
        - Statement date formatted as "MMM yyyy" (or "No date")
        - Transaction count
        - Status breakdown: "{converted} converted · {skipped} skipped · {pending} pending"
        - "View PDF" action (only when hasPdf) — opens PdfViewerModal
        - "View details" link → `/statements/{id}`
    11. Statements imported before v2.2 (no stored PDF) shown with gray icon and "No file stored" indicator (per locked decision)
    12. Sort statements by date descending (newest first), dateless last
    13. Render PdfViewerModal for each statement that hasPdf (same Fragment wrapper pattern as statement-list.tsx)
  </action>
  <verify>
    - `npm run build` compiles without errors
    - Navigate to /vault — sources display as card tiles
    - Click a card — it expands inline showing statement rows
    - Multiple cards can be open simultaneously
    - Statement rows show PDF icon, filename, date, tx count, status, View details link
    - Empty state shown when no sources exist (create test scenario if possible)
    - Tab bar visible with File Cabinet and Timeline tabs
  </verify>
  <done>
    - File Cabinet view renders sources as expandable card tiles in a responsive grid
    - Clicking a folder card expands it inline to show statement rows with rich row format
    - Multiple folders can be open at once
    - Statements lazy-load only when folder is expanded
    - Empty state displayed for users with no statements (illustration + Upload Statements CTA linking to /import/batch)
    - Tab bar switches between File Cabinet and Timeline (Timeline is placeholder)
  </done>
</task>

</tasks>

<verification>
1. Navigate to /vault from the sidebar — page loads with DashboardHeader
2. File Cabinet tab is active by default
3. Source folder cards render in responsive grid
4. Click a folder card — it expands inline showing statement rows
5. Click another folder card — both are open simultaneously
6. Statement rows show: PDF icon + filename + date + tx count + status breakdown + View details link
7. Clicking PDF icon opens PdfViewerModal for statements with stored PDFs
8. Non-PDF statements show gray icon (no crash, no blank state)
9. Timeline tab shows placeholder text (ready for Plan 02)
10. With zero statements, empty state is shown with Upload Statements button
</verification>

<success_criteria>
- /vault is accessible from the sidebar (Archive icon, positioned after Dashboard)
- File Cabinet view displays all sources as expandable folder cards
- Statement rows inside expanded folders match the rich row specification
- Empty state displays when user has no statements
- Tab switching works between File Cabinet and Timeline views
- No new npm dependencies required
</success_criteria>

<output>
After completion, create `.planning/phases/33-vault-ui/33-01-SUMMARY.md`
</output>
