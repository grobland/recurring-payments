---
phase: 34-coverage-historical-upload
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/batch/upload/route.ts
  - src/app/api/batch/process/route.ts
  - src/components/vault/historical-upload-modal.tsx
autonomous: true
gap_closure: true
requirements:
  - VENH-01
  - VENH-02

must_haves:
  truths:
    - "Coverage grid cells show green for months with a stored PDF"
    - "Coverage grid cells show yellow for months with data but no PDF"
    - "After uploading via the coverage wizard, the cell turns green and the gap count updates"
  artifacts:
    - path: "src/app/api/batch/upload/route.ts"
      provides: "Accepts optional statementDate from FormData and sets it on INSERT"
      contains: "statementDate"
    - path: "src/app/api/batch/process/route.ts"
      provides: "Derives statementDate from earliest transaction date after AI extraction"
      contains: "statementDate"
    - path: "src/components/vault/historical-upload-modal.tsx"
      provides: "Sends targetMonth as statementDate in FormData for both missing and attach modes"
      contains: "statementDate"
  key_links:
    - from: "src/components/vault/historical-upload-modal.tsx"
      to: "/api/batch/upload"
      via: "FormData.append('statementDate', ...)"
      pattern: "formData\\.append.*statementDate"
    - from: "src/app/api/batch/process/route.ts"
      to: "statements table"
      via: "db.update sets statementDate from transaction dates"
      pattern: "statementDate.*transactionDate|Math\\.min"
    - from: "src/app/api/vault/coverage/route.ts"
      to: "statements.statementDate"
      via: "gte filter on statementDate (existing, unchanged)"
      pattern: "gte.*statementDate"
---

<objective>
Fix the root cause of all coverage grid cells showing gray: the `statementDate` column is never populated.

Purpose: Without statementDate, the coverage API skips every statement (line 77: `if (!stmt.statementDate) continue`), so the cell map is always empty and every cell renders as "missing" (gray). This single root cause causes both UAT failures (test 3: no green/yellow cells, test 11: cell doesn't turn green after upload).

Output: Three files patched so that (1) the historical upload wizard sends targetMonth as statementDate, (2) batch/upload accepts and persists it, and (3) batch/process derives statementDate from the earliest transaction date for statements uploaded through the normal import flow.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-coverage-historical-upload/34-01-SUMMARY.md
@.planning/phases/34-coverage-historical-upload/34-02-SUMMARY.md
@.planning/debug/coverage-grid-all-gray.md

Key source files (read before modifying):
@src/app/api/batch/upload/route.ts
@src/app/api/batch/process/route.ts
@src/app/api/vault/coverage/route.ts
@src/components/vault/historical-upload-modal.tsx
@src/lib/db/schema.ts (statementDate column definition)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire statementDate through upload pipeline and historical upload modal</name>
  <files>
    src/app/api/batch/upload/route.ts
    src/components/vault/historical-upload-modal.tsx
  </files>
  <action>
    **In `src/app/api/batch/upload/route.ts`:**

    1. After parsing the existing FormData fields (file, hash, sourceType), parse an optional `statementDate` field:
       ```
       const statementDateStr = formData.get("statementDate") as string | null;
       ```
    2. If `statementDateStr` is present and non-empty, parse it into a Date object. The value will be in "yyyy-MM" format (e.g., "2026-02"), so parse as the first day of that month:
       ```
       let statementDate: Date | undefined;
       if (statementDateStr && statementDateStr.trim().length > 0) {
         statementDate = new Date(statementDateStr.trim() + "-01T00:00:00Z");
       }
       ```
    3. In the `db.insert(statements).values({...})` call (around line 88), add `statementDate` to the values object. Since it may be undefined, Drizzle will skip it when undefined (same as not providing it):
       ```
       ...(statementDate ? { statementDate } : {}),
       ```

    **In `src/components/vault/historical-upload-modal.tsx`:**

    1. In the `handleUpload` function, in the `else` branch (mode === "missing", around line 96-108), after appending sourceType and hash to formData, also append the statementDate:
       ```
       formData.append("statementDate", targetMonth);
       ```
       This sends the "yyyy-MM" string (e.g., "2026-02") which the API will parse.

    2. In the `if (mode === "attach")` branch (around line 86-95), also append statementDate so attach-pdf can set it too. However, the attach-pdf endpoint already has the statement — it just needs to ensure statementDate is set. For simplicity, do NOT modify the attach-pdf endpoint in this plan (the yellow cell issue is moot until statements actually have dates). The primary fix is for new uploads.

    Do NOT modify any other files in this task. The coverage API route does NOT need changes — it already correctly reads statementDate.
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero TypeScript errors. Grep both files for "statementDate" to confirm the field appears in the right places.
  </verify>
  <done>
    batch/upload accepts optional statementDate from FormData and persists it on INSERT. historical-upload-modal sends targetMonth as statementDate when uploading for a missing cell.
  </done>
</task>

<task type="auto">
  <name>Task 2: Derive statementDate from transaction dates in batch/process</name>
  <files>
    src/app/api/batch/process/route.ts
  </files>
  <action>
    **In `src/app/api/batch/process/route.ts`:**

    This fixes the normal import flow (not historical upload). When a user uploads a statement through the standard batch import, statementDate is not provided. After AI extraction produces transaction records, derive it.

    1. After the `transactionRecords` array is built (around line 154), and before the "Update statement as complete" block (around line 167), compute the earliest transaction date:
       ```
       // Derive statementDate from earliest transaction date (if not already set)
       let derivedStatementDate: Date | undefined;
       if (transactionRecords.length > 0) {
         const dates = transactionRecords.map(t => t.transactionDate.getTime());
         derivedStatementDate = new Date(Math.min(...dates));
       }
       ```

    2. But only use it if the statement doesn't already have a statementDate (it might have been provided via the historical upload wizard). First, fetch the current statementDate before updating:
       - The `statement` variable fetched on line 86-95 only selects `id` and `processingStatus`. Add `statementDate` to the `columns` selection:
         ```
         columns: {
           id: true,
           processingStatus: true,
           statementDate: true,
         },
         ```

    3. In the "Update statement as complete" db.update().set() call (line 169-173), conditionally include statementDate:
       ```
       await db
         .update(statements)
         .set({
           processingStatus: "complete",
           transactionCount: transactionRecords.length,
           processedAt: new Date(),
           ...(!statement.statementDate && derivedStatementDate
             ? { statementDate: derivedStatementDate }
             : {}),
         })
         .where(eq(statements.id, statementId));
       ```

    This ensures:
    - Historical uploads (which already have statementDate from Task 1) are NOT overwritten
    - Normal imports get statementDate derived from their earliest transaction
    - Empty statements (0 transactions) keep statementDate as null (no data to derive from)
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero TypeScript errors. Verify the logic by reading the final file and confirming: (1) statementDate is in the columns selection, (2) derivedStatementDate is computed from Math.min of transaction dates, (3) the update conditionally includes statementDate only when not already set.
  </verify>
  <done>
    After AI extraction in batch/process, statementDate is derived from the earliest transaction date and set on the statement record (unless already provided by the upload step). Combined with Task 1, both upload paths now populate statementDate, so the coverage API will correctly map statements to grid cells — green for PDF-stored, yellow for data-only, gray only for truly missing months.
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. `npx tsc --noEmit` passes with zero errors
2. `src/app/api/batch/upload/route.ts` contains `statementDate` in both FormData parsing and the INSERT values
3. `src/app/api/batch/process/route.ts` contains `derivedStatementDate` computation and conditional SET
4. `src/components/vault/historical-upload-modal.tsx` contains `formData.append("statementDate", targetMonth)`
5. `src/app/api/vault/coverage/route.ts` is UNCHANGED — its existing logic (skip null dates, derive cell state) is correct once statementDate is populated

Note: Existing statements in the database still have NULL statementDate. They will remain gray until the user re-imports or a future backfill migration is run. New uploads and re-processed statements will correctly populate the grid. This is acceptable — the coverage grid's purpose is to show gaps and guide users to fill them.
</verification>

<success_criteria>
- Coverage grid shows green cells for months with PDF-stored statements (after new upload)
- Coverage grid shows yellow cells for months with data-only statements (after re-processing)
- Uploading via the historical upload wizard sets statementDate, so the cell turns green after upload and the gap count decreases
- Normal batch imports derive statementDate from transaction dates, so they appear in the coverage grid
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/34-coverage-historical-upload/34-03-SUMMARY.md`
</output>
