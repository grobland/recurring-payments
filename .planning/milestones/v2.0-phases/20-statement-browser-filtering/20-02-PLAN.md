---
phase: 20-statement-browser-filtering
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - package.json
  - src/components/transactions/transaction-browser.tsx
  - src/components/transactions/transaction-table.tsx
  - src/components/transactions/transaction-card-list.tsx
  - src/components/transactions/transaction-row.tsx
  - src/components/transactions/transaction-card.tsx
  - src/components/transactions/transaction-filters.tsx
  - src/components/transactions/tag-status-badge.tsx
  - src/components/transactions/index.ts
  - src/app/(dashboard)/transactions/page.tsx
  - src/components/layout/sidebar.tsx
autonomous: true

must_haves:
  truths:
    - "User can view list of transactions with virtualized scrolling"
    - "User can filter by source, date range, search, and tag status"
    - "Mobile shows card layout, desktop shows table"
    - "10k+ items render at 60fps"
  artifacts:
    - path: "src/components/transactions/transaction-browser.tsx"
      provides: "Main browser component with filters and list"
      min_lines: 50
    - path: "src/components/transactions/transaction-table.tsx"
      provides: "Desktop virtualized table"
      min_lines: 80
    - path: "src/components/transactions/transaction-card-list.tsx"
      provides: "Mobile virtualized card list"
      min_lines: 50
    - path: "src/components/transactions/transaction-filters.tsx"
      provides: "Filter bar component"
      min_lines: 40
    - path: "src/app/(dashboard)/transactions/page.tsx"
      provides: "Transactions page route"
      min_lines: 10
  key_links:
    - from: "src/components/transactions/transaction-browser.tsx"
      to: "useTransactions"
      via: "hook import and call"
      pattern: "useTransactions"
    - from: "src/components/transactions/transaction-table.tsx"
      to: "@tanstack/react-virtual"
      via: "useVirtualizer hook"
      pattern: "useVirtualizer"
    - from: "src/app/(dashboard)/transactions/page.tsx"
      to: "transaction-browser.tsx"
      via: "component import"
      pattern: "TransactionBrowser"
---

<objective>
Create the UI layer for transaction browsing: virtualized table/cards, filter bar, and page route.

Purpose: Enable users to browse 10k+ transactions at 60fps with responsive desktop/mobile layouts.
Output: Working /transactions page with filters, virtualized list, and navigation link.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-statement-browser-filtering/20-RESEARCH.md
@.planning/phases/20-statement-browser-filtering/20-CONTEXT.md
@.planning/phases/20-statement-browser-filtering/20-01-SUMMARY.md
@src/lib/hooks/use-transactions.ts
@src/lib/hooks/use-debounced-value.ts
@src/types/transaction.ts
@src/hooks/use-mobile.ts (useIsMobile hook)
@src/components/ui/skeleton.tsx
@src/components/ui/badge.tsx
@src/components/ui/input.tsx
@src/components/ui/select.tsx
@src/components/ui/button.tsx
@src/components/ui/card.tsx
@src/components/layout/sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install TanStack Virtual and create base components</name>
  <files>
    package.json
    src/components/transactions/tag-status-badge.tsx
    src/components/transactions/transaction-row.tsx
    src/components/transactions/transaction-card.tsx
    src/components/transactions/index.ts
  </files>
  <action>
**Install TanStack Virtual:**
```bash
npm install @tanstack/react-virtual
```

**Create TagStatusBadge component (src/components/transactions/tag-status-badge.tsx):**
- Import Badge from @/components/ui/badge
- Map tagStatus to colors (from CONTEXT.md):
  - unreviewed: return null (no badge)
  - potential_subscription: blue badge "Potential"
  - converted: green badge "Converted"
  - not_subscription: gray badge "Dismissed"
- Use cn() for class merging
- Text size: text-xs

**Create TransactionRow component (src/components/transactions/transaction-row.tsx):**
Table row for desktop view. Props: transaction (TransactionWithSource), style (for virtualization positioning).
Columns: Date | Merchant | Amount | Source | Category | Tag Status
- Date: format with date-fns (MMM d, yyyy)
- Merchant: transaction.merchantName
- Amount: format with currency (e.g., -$12.99)
- Source: transaction.sourceType
- Category: transaction.categoryGuess or "-"
- Tag Status: TagStatusBadge component
- Apply absolute positioning style from virtualizer

**Create TransactionCard component (src/components/transactions/transaction-card.tsx):**
Card for mobile view. Props: transaction (TransactionWithSource), style (for virtualization).
Layout:
- Top row: Date (left), Amount (right, bold)
- Middle: Merchant name (truncate if long)
- Bottom row: Source badge, Category, TagStatusBadge
- Use Card component from shadcn/ui
- Touch-friendly padding (p-4)

**Create index.ts barrel export** for all transaction components.
  </action>
  <verify>
Package installed: `npm ls @tanstack/react-virtual`
TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
TanStack Virtual installed, TagStatusBadge/TransactionRow/TransactionCard components created with proper styling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create filter bar and virtualized list components</name>
  <files>
    src/components/transactions/transaction-filters.tsx
    src/components/transactions/transaction-table.tsx
    src/components/transactions/transaction-card-list.tsx
  </files>
  <action>
**Create TransactionFilters component (src/components/transactions/transaction-filters.tsx):**
Props: filters (TransactionFilters), onFiltersChange (callback), sourceTypes (string[] for dropdown).

Layout: Horizontal flex row, sticky top, gap-4, items-center, flex-wrap on mobile.

Controls:
1. Search Input: placeholder "Search merchant...", onChange updates filters.search
2. Source Dropdown: Select with "All sources" default, options from sourceTypes prop
3. Tag Status Dropdown: Select with options: All, Potential, Converted, Dismissed (map to enum values)
4. Date Range: Two date inputs (From, To) or use existing date picker pattern
5. Clear Filters Button: Resets all filters to defaults, always visible

Use existing shadcn/ui components: Input, Select, SelectContent, SelectItem, SelectTrigger, SelectValue, Button.

**Create TransactionTable component (src/components/transactions/transaction-table.tsx):**
Props: transactions (TransactionWithSource[]), onLoadMore (callback), hasMore (boolean), isLoading (boolean).

Implementation (from RESEARCH.md pattern):
1. Create parentRef for scroll container
2. Use useVirtualizer with:
   - count: transactions.length
   - getScrollElement: () => parentRef.current
   - estimateSize: () => 48 (row height)
   - overscan: 20
3. Render table with thead (sticky headers) and tbody
4. tbody style: height = getTotalSize(), position: relative
5. Map virtualItems to TransactionRow with absolute positioning
6. Sortable column headers (click to toggle sort - store in local state or pass to parent)
7. Infinite scroll: use IntersectionObserver on last row to trigger onLoadMore when hasMore
8. Loading state: show Skeleton rows at bottom when isLoading
9. Container height: h-[calc(100vh-200px)] or similar (leave room for header/filters)

**Create TransactionCardList component (src/components/transactions/transaction-card-list.tsx):**
Props: Same as TransactionTable.

Same virtualization pattern but:
- estimateSize: () => 120 (card height estimate)
- Render TransactionCard instead of TransactionRow
- Stack layout instead of table
- Same infinite scroll logic with IntersectionObserver
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
TransactionFilters renders filter bar, TransactionTable/TransactionCardList virtualize 10k+ items with infinite scroll.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create browser component, page, and navigation</name>
  <files>
    src/components/transactions/transaction-browser.tsx
    src/app/(dashboard)/transactions/page.tsx
    src/components/layout/sidebar.tsx
  </files>
  <action>
**Create TransactionBrowser component (src/components/transactions/transaction-browser.tsx):**
Main component that orchestrates everything.

Implementation:
1. State for filters (TransactionFilters)
2. Use useDebouncedValue for search (300ms delay)
3. Create debouncedFilters object with debounced search
4. Call useTransactions(debouncedFilters)
5. Extract unique sourceTypes from loaded transactions for filter dropdown
6. Use useIsMobile() to switch between table and cards
7. Handle fetchNextPage when user scrolls to bottom

Layout:
```tsx
<div className="flex flex-col h-full">
  <TransactionFilters
    filters={filters}
    onFiltersChange={setFilters}
    sourceTypes={uniqueSourceTypes}
  />
  {isMobile ? (
    <TransactionCardList
      transactions={allTransactions}
      onLoadMore={() => fetchNextPage()}
      hasMore={hasNextPage}
      isLoading={isFetchingNextPage}
    />
  ) : (
    <TransactionTable
      transactions={allTransactions}
      onLoadMore={() => fetchNextPage()}
      hasMore={hasNextPage}
      isLoading={isFetchingNextPage}
    />
  )}
</div>
```

Handle states:
- isLoading (initial): Show skeleton grid
- error: Show error message with retry button
- empty (no transactions): "No transactions found" message
- empty with filters: "No transactions match your filters" with clear button

**Create page (src/app/(dashboard)/transactions/page.tsx):**
```tsx
import { TransactionBrowser } from "@/components/transactions"

export default function TransactionsPage() {
  return (
    <div className="container mx-auto py-6">
      <h1 className="text-2xl font-bold mb-6">Transactions</h1>
      <TransactionBrowser />
    </div>
  )
}
```

**Update sidebar (src/components/layout/sidebar.tsx):**
Add navigation link to /transactions:
- Icon: Receipt or FileText from lucide-react
- Label: "Transactions"
- Place after "Batch Import" in navigation order
  </action>
  <verify>
1. Dev server running: `npm run dev`
2. Navigate to http://localhost:3000/transactions
3. Page loads with filter bar and transaction list
4. Sidebar shows Transactions link
  </verify>
  <done>
/transactions page renders with virtualized list, filters work, sidebar has navigation link.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. /transactions page loads and shows transaction list
3. Filters update results in real-time (search debounced)
4. Scrolling is smooth at 60fps with 1000+ items
5. Mobile view shows cards, desktop shows table
6. Infinite scroll loads more pages automatically
7. Empty state shows appropriate message
8. Sidebar navigation works
</verification>

<success_criteria>
- BRWS-01: User can view list of all transactions - verified by page loading with data
- BRWS-02: Filter by source works - dropdown filters transactions
- BRWS-03: Filter by date range works - date inputs filter transactions
- BRWS-04: Search by merchant works - search input filters with debounce
- BRWS-05: Virtualized scrolling at 60fps - smooth scroll with 10k+ items
- BRWS-06: Filter by tag status works - dropdown filters by tag
- Mobile shows card layout, desktop shows table
- Sidebar has Transactions link
</success_criteria>

<output>
After completion, create `.planning/phases/20-statement-browser-filtering/20-02-SUMMARY.md`
</output>
