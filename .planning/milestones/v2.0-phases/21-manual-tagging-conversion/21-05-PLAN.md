---
phase: 21-manual-tagging-conversion
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tags/tag-manager.tsx
  - src/components/tags/tag-form.tsx
  - src/components/tags/tag-delete-dialog.tsx
  - src/components/tags/index.ts
  - src/app/(dashboard)/settings/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can view all their tags on Settings page"
    - "User can create new tag with name and color"
    - "User can edit existing tag name or color"
    - "User can delete tag with confirmation"
  artifacts:
    - path: "src/components/tags/tag-manager.tsx"
      provides: "List/create/edit/delete tags UI"
      min_lines: 150
    - path: "src/components/tags/tag-form.tsx"
      provides: "Name and color input form"
      min_lines: 50
    - path: "src/components/tags/tag-delete-dialog.tsx"
      provides: "Delete confirmation dialog"
      min_lines: 30
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "Settings page with TagManager added"
      contains: "TagManager"
  key_links:
    - from: "src/components/tags/tag-manager.tsx"
      to: "src/lib/hooks/use-tags.ts"
      via: "useTags, useCreateTag, useUpdateTag, useDeleteTag hooks"
      pattern: "use(Create|Update|Delete)?Tag"
    - from: "src/app/(dashboard)/settings/page.tsx"
      to: "src/components/tags/tag-manager.tsx"
      via: "import and render TagManager component"
      pattern: "import.*TagManager"
---

<objective>
Create Tag Management UI for Settings page

Purpose: Users cannot create tags without API access. UAT Test 5 failed because there is no UI for creating tags - only the backend exists. This plan adds TagManager component to Settings page following the established CategoryManager pattern.

Output: TagManager component with CRUD operations rendered on Settings page below CategoryManager.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-manual-tagging-conversion/21-UAT.md

# Reference pattern
@src/components/categories/category-manager.tsx
@src/components/categories/category-form.tsx
@src/components/categories/category-delete-dialog.tsx

# Existing hooks to use
@src/lib/hooks/use-tags.ts
@src/lib/validations/tag.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TagForm component</name>
  <files>src/components/tags/tag-form.tsx</files>
  <action>
Create TagForm component following CategoryForm pattern but simpler (no icon picker).

Form fields:
- name: text input, placeholder "e.g., Recurring", max 50 chars
- color: text input for hex color with live preview swatch (like CategoryForm)

Use React Hook Form + Zod resolver with createTagSchema from @/lib/validations/tag.
Buttons: Submit (configurable label) and Cancel.

Props interface:
- defaultValues?: Partial<CreateTagInput>
- onSubmit: (data: CreateTagInput) => Promise<void>
- onCancel: () => void
- isSubmitting?: boolean
- submitLabel?: string (default "Save")

Add preset color swatches below the color input for quick selection:
["#3B82F6", "#10B981", "#F59E0B", "#EF4444", "#8B5CF6", "#EC4899", "#6366F1", "#14B8A6"]

Do NOT use IconPicker - tags don't have icons.
  </action>
  <verify>File exists at src/components/tags/tag-form.tsx with form using createTagSchema</verify>
  <done>TagForm component with name/color inputs and preset swatches</done>
</task>

<task type="auto">
  <name>Task 2: Create TagDeleteDialog and TagManager components</name>
  <files>
    src/components/tags/tag-delete-dialog.tsx
    src/components/tags/tag-manager.tsx
    src/components/tags/index.ts
  </files>
  <action>
1. Create TagDeleteDialog following CategoryDeleteDialog pattern:
   - Props: tag (Tag | null), isOpen, onClose, onConfirm, isDeleting
   - Use AlertDialog from shadcn
   - Show tag name in confirmation message
   - Note: No "affected count" needed - tags just cascade delete from transactionTags

2. Create TagManager following CategoryManager pattern:
   - Use useTags() to fetch tags
   - Use useCreateTag(), useUpdateTag(), useDeleteTag() mutations
   - State for dialogs: createDialogOpen, editDialogOpen, deleteDialogOpen
   - State for selected tag: selectedTag (Tag | null)

   UI structure (Card component):
   - Header: "Tags" title, "Create custom tags to organize transactions" description, "New Tag" button
   - Content: List of tags, each row shows:
     - Color swatch (10x10 rounded-full)
     - Tag name
     - Edit button (Pencil icon)
     - Delete button (Trash2 icon)
   - If no tags: Empty state message "No tags yet. Create one to get started."

   Handlers:
   - handleCreate: call createMutation.mutateAsync, toast success, close dialog
   - handleEdit: call updateMutation.mutateAsync, toast success, close dialog, clear selection
   - handleDelete: call deleteMutation.mutateAsync, toast success, close dialog, clear selection

   Dialogs (at bottom of component):
   - Create dialog with TagForm
   - Edit dialog with TagForm (defaultValues from selectedTag)
   - TagDeleteDialog

3. Create index.ts barrel export:
   export { TagManager } from "./tag-manager";
   export { TagForm } from "./tag-form";
   export { TagDeleteDialog } from "./tag-delete-dialog";
  </action>
  <verify>All three files exist in src/components/tags/</verify>
  <done>TagManager with full CRUD UI, TagDeleteDialog for confirmations, barrel export</done>
</task>

<task type="auto">
  <name>Task 3: Add TagManager to Settings page</name>
  <files>src/app/(dashboard)/settings/page.tsx</files>
  <action>
Add TagManager to the Settings page below CategoryManager.

1. Add import:
   import { TagManager } from "@/components/tags";

2. In the return statement, add TagManager after CategoryManager:
   <div className="space-y-6">
     <Card>...</Card>  {/* Profile card */}
     <CategoryManager />
     <TagManager />
   </div>

This places tags management below categories on the Settings page.
  </action>
  <verify>npm run build succeeds and Settings page imports TagManager</verify>
  <done>TagManager visible on Settings page below CategoryManager</done>
</task>

</tasks>

<verification>
1. Run npm run build - should complete without errors
2. Start dev server, navigate to /settings
3. Verify TagManager card appears below CategoryManager
4. Click "New Tag", fill form, verify tag appears in list
5. Click edit button, change name/color, verify updates
6. Click delete button, confirm, verify tag removed
</verification>

<success_criteria>
- TagManager component renders on Settings page
- User can create tag with name and hex color
- User can edit existing tag
- User can delete tag with confirmation dialog
- Toast notifications appear for all operations
- UAT Test 5 passes: "user can create a tag" via Settings UI
</success_criteria>

<output>
After completion, create `.planning/phases/21-manual-tagging-conversion/21-05-SUMMARY.md`
</output>
