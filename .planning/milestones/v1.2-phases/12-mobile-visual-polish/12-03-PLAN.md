---
phase: 12-mobile-visual-polish
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - src/app/(dashboard)/subscriptions/new/page.tsx
  - src/app/(dashboard)/subscriptions/[id]/edit/page.tsx
  - src/components/shared/empty-state.tsx
  - src/components/shared/service-unavailable.tsx
autonomous: false

must_haves:
  truths:
    - "Remaining pages (new, edit) and shared components have responsive typography and spacing"
    - "Page headings scale from text-2xl on mobile to text-3xl on desktop"
    - "Empty states and service unavailable components are mobile-friendly"
    - "Human has verified mobile responsiveness on actual mobile viewport"
    - "Sidebar collapses to hamburger menu on mobile (SidebarTrigger visible)"
  artifacts:
    - path: "src/app/(dashboard)/subscriptions/new/page.tsx"
      provides: "Mobile-optimized new subscription page"
      contains: "p-4 md:p-6"
    - path: "src/components/shared/empty-state.tsx"
      provides: "Mobile-friendly empty state"
      contains: "h-11"
  key_links:
    - from: "typography patterns"
      to: "remaining pages and shared components"
      via: "consistent responsive classes"
      pattern: "text-2xl.*md:text-3xl"
---

<objective>
Complete mobile polish with remaining pages, typography consistency check, and human verification.

Purpose: Ensure all remaining pages follow the mobile patterns established in plans 01 and 02, and verify the complete mobile experience with human eyes on actual mobile viewport.

Output: Remaining pages polished, shared components mobile-optimized, and human verification that the app works well on mobile.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-mobile-visual-polish/12-RESEARCH.md

# Prior plan summaries for context
@.planning/phases/12-mobile-visual-polish/12-01-SUMMARY.md
@.planning/phases/12-mobile-visual-polish/12-02-SUMMARY.md

@src/app/(dashboard)/subscriptions/new/page.tsx
@src/app/(dashboard)/subscriptions/[id]/edit/page.tsx
@src/components/shared/empty-state.tsx
@src/components/shared/service-unavailable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish remaining subscription pages</name>
  <files>
src/app/(dashboard)/subscriptions/new/page.tsx
src/app/(dashboard)/subscriptions/[id]/edit/page.tsx
  </files>
  <action>
**New Subscription Page (subscriptions/new/page.tsx):**

1. **Main container (line ~41):** Change `p-6` to `p-4 md:p-6`

2. **Card title (line ~44):** Consider adding responsive typography:
   ```tsx
   <CardTitle className="text-xl md:text-2xl">Add New Subscription</CardTitle>
   ```
   (Optional - current default CardTitle styling may be sufficient)

The form itself is the SubscriptionForm component, already updated in plan 02.

**Edit Subscription Page (subscriptions/[id]/edit/page.tsx):**

Read the file first to understand its structure, then apply:

1. **Main container:** Change padding from `p-6` to `p-4 md:p-6`

2. **Card:** Ensure responsive padding if needed

3. The form is also SubscriptionForm component, already updated.
  </action>
  <verify>
1. Run `npm run build` to ensure no TypeScript/build errors
2. Verify new page has pattern: `grep -q "p-4 md:p-6" src/app/\(dashboard\)/subscriptions/new/page.tsx && echo "PASS" || echo "FAIL"`
3. Verify edit page has pattern: `grep -q "p-4 md:p-6" src/app/\(dashboard\)/subscriptions/\[id\]/edit/page.tsx && echo "PASS" || echo "FAIL"`
  </verify>
  <done>New and edit subscription pages have consistent mobile-optimized spacing.</done>
</task>

<task type="auto">
  <name>Task 2: Polish shared components</name>
  <files>
src/components/shared/empty-state.tsx
src/components/shared/service-unavailable.tsx
  </files>
  <action>
**EmptyState Component:**

Read the current implementation, then ensure:

1. **Icon sizing:** Should scale responsively. If using fixed `h-12 w-12`, that's fine for mobile.

2. **Typography:**
   - Title: Use `text-lg md:text-xl` pattern if not already
   - Description: Use `text-sm md:text-base` pattern if not already

3. **Action buttons:** If present, ensure they have `h-11` for touch targets and `w-full sm:w-auto` for mobile stacking

4. **Container padding:** If the component has padding, make it responsive

**ServiceUnavailable Component:**

1. **Typography:** Ensure responsive scaling
   - Title: `text-lg md:text-xl`
   - Description: `text-sm md:text-base`

2. **Retry button:** Ensure `h-11` for touch target

3. **Container:** Responsive padding if applicable
  </action>
  <verify>
1. Run `npm run build` to ensure no TypeScript/build errors
2. Verify empty-state has h-11: `grep -q "h-11" src/components/shared/empty-state.tsx && echo "PASS" || echo "FAIL"`
3. Verify service-unavailable has h-11: `grep -q "h-11" src/components/shared/service-unavailable.tsx && echo "PASS" || echo "FAIL"`
  </verify>
  <done>Shared UI components (EmptyState, ServiceUnavailable) are mobile-optimized with responsive typography and touch targets.</done>
</task>

<task type="auto">
  <name>Task 3: Automated verification of all mobile patterns</name>
  <files></files>
  <action>
Run automated verification to confirm all mobile patterns are in place before human verification:

1. **Build verification:**
   ```bash
   npm run build
   ```
   Must complete without errors.

2. **Sidebar collapse verification (UX-01):**
   Verify SidebarTrigger is present in dashboard layout:
   ```bash
   grep -q "SidebarTrigger" src/components/layout/dashboard-sidebar.tsx && echo "PASS: SidebarTrigger exists" || echo "FAIL: SidebarTrigger missing"
   ```

3. **Responsive spacing pattern verification:**
   ```bash
   echo "Checking p-4 md:p-6 pattern in modified files..."
   grep -l "p-4 md:p-6" src/app/\(dashboard\)/dashboard/page.tsx src/app/\(dashboard\)/subscriptions/page.tsx src/app/\(dashboard\)/subscriptions/\[id\]/page.tsx src/app/\(dashboard\)/subscriptions/new/page.tsx src/app/\(dashboard\)/import/page.tsx src/app/\(dashboard\)/settings/layout.tsx 2>/dev/null | wc -l
   ```
   Should return 6 (all files have the pattern).

4. **Touch target verification:**
   ```bash
   echo "Checking h-11 pattern in form files..."
   grep -c "h-11" src/components/subscriptions/subscription-form.tsx src/app/\(dashboard\)/import/page.tsx src/app/\(dashboard\)/settings/page.tsx src/components/shared/empty-state.tsx src/components/shared/service-unavailable.tsx 2>/dev/null
   ```
   Each file should have multiple h-11 occurrences.

5. **Button stacking verification:**
   ```bash
   echo "Checking flex-col-reverse pattern..."
   grep -l "flex-col-reverse" src/components/subscriptions/subscription-form.tsx src/app/\(dashboard\)/import/page.tsx src/app/\(dashboard\)/subscriptions/\[id\]/page.tsx 2>/dev/null | wc -l
   ```
   Should return 3 (all files with button groups have stacking).

Output a summary of all checks.
  </action>
  <verify>
All automated checks pass:
1. `npm run build` exits 0
2. SidebarTrigger grep returns PASS
3. Spacing pattern found in >= 5 files
4. h-11 pattern found in all form-related files
5. flex-col-reverse found in >= 2 files
  </verify>
  <done>All mobile patterns verified programmatically before human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete mobile responsiveness across all dashboard pages including:
- Dashboard with mobile-first spacing
- Subscriptions list with touch-friendly table and filters
- Subscription detail with stacked action buttons
- All forms with 44px touch targets and stacked buttons
- Settings with touch-friendly navigation
- Import page with mobile-optimized review flow
- Shared components (EmptyState, ServiceUnavailable)
  </what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev`
2. Open browser to http://localhost:3000
3. Open DevTools and toggle to mobile viewport (iPhone 12/13 - 390px width)
4. Navigate through each page and verify:

**Sidebar Collapse (UX-01 - CRITICAL):**
- [ ] Hamburger menu icon (SidebarTrigger) is visible at mobile viewport
- [ ] Tapping hamburger opens sidebar as overlay/drawer
- [ ] Sidebar can be closed by tapping outside or close button

**Dashboard (/dashboard):**
- [ ] Page has comfortable spacing (not cramped, not too spacious)
- [ ] Stats cards stack to single column
- [ ] Quick action buttons are easy to tap (44px height)
- [ ] Upcoming renewals list items are tappable

**Subscriptions List (/subscriptions):**
- [ ] Filter dropdowns are full-width on mobile
- [ ] Table scrolls horizontally if needed
- [ ] Add Subscription button is easy to tap
- [ ] List items are touch-friendly

**Subscription Detail (/subscriptions/{id}):**
- [ ] Edit/Delete buttons stack vertically
- [ ] Card content is readable
- [ ] Back navigation works

**New Subscription (/subscriptions/new):**
- [ ] Form inputs are easy to tap (44px height)
- [ ] Save/Cancel buttons stack with Save below Cancel
- [ ] Date pickers work on mobile

**Import (/import):**
- [ ] Upload area is touch-friendly
- [ ] Process Files button is easy to tap
- [ ] (If you have a PDF) Review step shows items well on mobile

**Settings (/settings):**
- [ ] Navigation tabs are tappable (icons on mobile)
- [ ] Form inputs are touch-friendly
- [ ] Save button is easy to tap

5. Test on actual mobile device if available (not just emulator)
6. Verify dark mode still works correctly on mobile
  </how-to-verify>
  <resume-signal>Type "approved" if all mobile checks pass, or describe specific issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
After human verification:
1. All pages display correctly at 390px viewport width
2. Sidebar collapses to hamburger menu and works correctly
3. Touch targets are comfortable for finger tapping
4. Text is readable without horizontal scrolling
5. Forms are usable on mobile
6. Navigation works smoothly
</verification>

<success_criteria>
Requirements satisfied:
- [x] UX-01: All pages are mobile responsive (sidebar collapses - verified, forms stack - done, dashboard adapts - done)
- [x] UX-06: Typography is consistent (responsive scaling applied)
- [x] UX-07: Spacing is consistent (p-4 md:p-6 pattern across all pages)
- [x] UX-08: Color usage is consistent (using semantic tokens throughout - verified during implementation)

Phase 12 Success Criteria:
1. Sidebar collapses to hamburger menu on mobile screens - VERIFIED (SidebarTrigger present + human verification)
2. Forms stack vertically and remain usable on mobile - DONE (button stacking, touch targets)
3. Dashboard adapts from multi-column to single-column on mobile - DONE (grid patterns + responsive spacing)
4. Typography follows consistent scale - DONE (responsive text sizing)
5. Spacing uses consistent values - DONE (p-4 md:p-6 pattern)
6. Colors applied consistently - VERIFIED (semantic tokens used throughout)
</success_criteria>

<output>
After completion, create `.planning/phases/12-mobile-visual-polish/12-03-SUMMARY.md`
</output>
