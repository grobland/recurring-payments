---
phase: 09-reliability-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/instrumentation.ts
  - src/instrumentation-client.ts
  - src/sentry.server.config.ts
  - src/sentry.edge.config.ts
  - next.config.ts
  - src/app/error.tsx
  - src/app/global-error.tsx
  - src/app/(dashboard)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Errors thrown in production are captured in Sentry"
    - "Errors include user context (id, email) when user is authenticated"
    - "Page load times and API latency are automatically measured"
    - "Web Vitals (LCP, INP, CLS) are tracked"
  artifacts:
    - path: "src/instrumentation.ts"
      provides: "Next.js instrumentation hook for server/edge Sentry init"
      exports: ["register", "onRequestError"]
    - path: "src/instrumentation-client.ts"
      provides: "Client-side Sentry initialization"
    - path: "src/sentry.server.config.ts"
      provides: "Server runtime Sentry configuration"
    - path: "src/sentry.edge.config.ts"
      provides: "Edge runtime Sentry configuration"
    - path: "next.config.ts"
      provides: "Sentry wrapper and external packages config"
      contains: "withSentryConfig"
  key_links:
    - from: "src/instrumentation.ts"
      to: "src/sentry.server.config.ts"
      via: "dynamic import in register()"
      pattern: "import.*sentry\\.server\\.config"
    - from: "src/app/error.tsx"
      to: "@sentry/nextjs"
      via: "captureException in useEffect"
      pattern: "Sentry\\.captureException"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "@sentry/nextjs"
      via: "setUser with session data"
      pattern: "Sentry\\.setUser"
---

<objective>
Set up Sentry error tracking and performance monitoring for the application.

Purpose: Capture errors in production with user context and automatically track Web Vitals, page loads, and API latency.
Output: Working Sentry integration with instrumentation files, error boundary integration, and user context propagation.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-reliability-foundation/09-RESEARCH.md
@next.config.ts
@src/app/error.tsx
@src/app/global-error.tsx
@src/app/(dashboard)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Sentry and create configuration files</name>
  <files>
    - package.json
    - src/instrumentation.ts
    - src/instrumentation-client.ts
    - src/sentry.server.config.ts
    - src/sentry.edge.config.ts
    - next.config.ts
  </files>
  <action>
1. Install @sentry/nextjs:
   ```bash
   npm install @sentry/nextjs
   ```

2. Create `src/instrumentation.ts` with:
   - register() function that imports sentry.server.config for nodejs runtime, sentry.edge.config for edge runtime
   - Export onRequestError = Sentry.captureRequestError
   - Check process.env.NEXT_RUNTIME to determine which config to import

3. Create `src/sentry.server.config.ts`:
   - Initialize Sentry with DSN from NEXT_PUBLIC_SENTRY_DSN
   - Set tracesSampleRate: 0.1 in production, 1.0 in development
   - Set environment from NODE_ENV

4. Create `src/sentry.edge.config.ts`:
   - Same configuration as server config (Sentry handles runtime differences)

5. Create `src/instrumentation-client.ts`:
   - Initialize Sentry for client-side
   - Include replayIntegration() for session replays
   - Set replaysSessionSampleRate: 0.1, replaysOnErrorSampleRate: 1.0
   - Set same tracesSampleRate and environment as server

6. Update `next.config.ts`:
   - Import withSentryConfig from @sentry/nextjs
   - Wrap nextConfig with withSentryConfig
   - Configure: org from SENTRY_ORG, project from SENTRY_PROJECT
   - Set silent: !process.env.CI, widenClientFileUpload: true
   - Add tunnelRoute: "/monitoring" to bypass ad-blockers

IMPORTANT: Use NEXT_PUBLIC_SENTRY_DSN (public prefix required for client-side access).
  </action>
  <verify>
- All 5 files created in correct locations
- `npm run build` completes without errors
- next.config.ts exports withSentryConfig wrapper
  </verify>
  <done>Sentry SDK installed and all configuration files created with proper runtime separation</done>
</task>

<task type="auto">
  <name>Task 2: Update error boundaries and add user context</name>
  <files>
    - src/app/error.tsx
    - src/app/global-error.tsx
    - src/app/(dashboard)/layout.tsx
  </files>
  <action>
1. Update `src/app/error.tsx`:
   - Import * as Sentry from "@sentry/nextjs"
   - In the existing useEffect, replace console.error with Sentry.captureException(error)
   - Keep the console.error for development debugging

2. Update `src/app/global-error.tsx`:
   - Import * as Sentry from "@sentry/nextjs"
   - In the existing useEffect, replace console.error with Sentry.captureException(error)
   - Keep the console.error for development debugging

3. Update `src/app/(dashboard)/layout.tsx`:
   - Import * as Sentry from "@sentry/nextjs"
   - After the auth() call, when session.user exists, call:
     ```typescript
     Sentry.setUser({
       id: session.user.id,
       email: session.user.email || undefined,
     });
     ```
   - This sets user context for all errors within authenticated routes

Note: User context is set server-side in the layout. Client-side user context will be set automatically through Sentry's session propagation. If client errors lack user context, we can add a client component wrapper later.
  </action>
  <verify>
- `npm run build` completes without errors
- error.tsx contains Sentry.captureException call
- global-error.tsx contains Sentry.captureException call
- layout.tsx calls Sentry.setUser with session data
  </verify>
  <done>Error boundaries capture exceptions to Sentry, and authenticated users have their context attached to errors</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` succeeds
2. All Sentry config files exist: instrumentation.ts, instrumentation-client.ts, sentry.server.config.ts, sentry.edge.config.ts
3. next.config.ts uses withSentryConfig wrapper
4. error.tsx and global-error.tsx call Sentry.captureException
5. Dashboard layout sets Sentry user context

Manual verification (when SENTRY_DSN is configured):
- Trigger an error in the app
- Verify error appears in Sentry dashboard with user context
- Check Sentry Performance tab for Web Vitals data
</verification>

<success_criteria>
- MON-01: Sentry integration captures errors with context (DONE when error boundaries call captureException and layout sets user)
- MON-05: Performance metrics tracked (DONE when Sentry auto-instrumentation is active - tracesSampleRate > 0 enables Web Vitals and API latency tracking)
</success_criteria>

<output>
After completion, create `.planning/phases/09-reliability-foundation/09-01-SUMMARY.md`
</output>
