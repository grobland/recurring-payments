---
phase: 24-webhook-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/db/migrations/XXXX_webhook_events.sql
  - src/app/api/webhooks/stripe/route.ts
autonomous: true

must_haves:
  truths:
    - "Webhook events are processed exactly once, even during Stripe retries"
    - "Processed event IDs are tracked in a database table"
    - "Duplicate events return 200 to Stripe without processing"
    - "Failed event processing is logged with error details"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "webhookEvents table definition"
      contains: "webhookEvents"
    - path: "src/app/api/webhooks/stripe/route.ts"
      provides: "Idempotent webhook handler with metrics tracking"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/webhooks/stripe/route.ts"
      to: "webhookEvents table"
      via: "insert before processing, update after"
      pattern: "db\\.insert\\(webhookEvents\\)"
---

<objective>
Implement webhook event idempotency tracking with database-backed deduplication.

Purpose: Prevent duplicate processing when Stripe retries webhooks, ensuring exactly-once semantics for payment events.
Output: webhook_events table schema, migration, and updated webhook handler with idempotency checks.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v2.1-ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-webhook-hardening/24-RESEARCH.md

# Existing files to modify
@src/lib/db/schema.ts
@src/app/api/webhooks/stripe/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add webhook_events table to schema</name>
  <files>src/lib/db/schema.ts</files>
  <action>
Add the webhookEvents table definition to the schema file with the following fields:
- id: uuid primary key with defaultRandom()
- eventId: varchar(255) NOT NULL with unique index (Stripe event.id)
- eventType: varchar(100) NOT NULL (e.g., "invoice.payment_failed")
- processedAt: timestamp with timezone, defaultNow(), NOT NULL
- status: varchar(20) NOT NULL - one of 'processed', 'failed', 'skipped'
- errorMessage: text (nullable, for failed events)
- processingTimeMs: integer (nullable, for metrics)
- expiresAt: timestamp with timezone NOT NULL (processedAt + 30 days for TTL cleanup)

Add indexes:
- uniqueIndex on eventId for fast idempotency lookups
- index on expiresAt for efficient TTL cleanup queries
- index on status for filtering in admin UI

Add relations to usersRelations (not strictly needed but for consistency).

Add type exports: WebhookEvent, NewWebhookEvent.

Use existing enum patterns in the file. Do NOT create a new enum for status - use varchar as specified.
  </action>
  <verify>Run `npm run db:generate` to generate migration (should complete without errors)</verify>
  <done>webhookEvents table defined in schema.ts with all fields, indexes, relations, and type exports</done>
</task>

<task type="auto">
  <name>Task 2: Run migration and verify table creation</name>
  <files>src/lib/db/migrations/</files>
  <action>
After generating migration:
1. Review the generated migration file in src/lib/db/migrations/
2. Run `npm run db:push` to apply schema to database
3. Verify table was created with correct structure

If migration fails, check for syntax errors in schema definition and fix.
  </action>
  <verify>Run `npm run db:studio` and verify webhook_events table exists with correct columns</verify>
  <done>webhook_events table exists in database with unique constraint on event_id</done>
</task>

<task type="auto">
  <name>Task 3: Add idempotency to webhook handler</name>
  <files>src/app/api/webhooks/stripe/route.ts</files>
  <action>
Refactor the webhook POST handler to implement idempotency:

1. After signature verification and before processing:
   - Check if event.id exists in webhookEvents table
   - If exists: log "Duplicate event: {eventId} ({eventType})", return { received: true } with 200 status

2. If first time processing:
   - Record start time for metrics
   - Insert into webhookEvents with status='processing' (prevents race conditions)
   - Use try-catch around the insert with unique constraint check (PostgreSQL error code 23505)
   - If insert fails with duplicate key, return 200 (concurrent request won)

3. Wrap event processing in try-catch:
   - On success: update webhookEvents set status='processed', processingTimeMs
   - On failure: update webhookEvents set status='failed', errorMessage

4. Always return 200 to Stripe after recording the event (prevent retries for non-retriable errors)
   - Return 500 only for truly retriable errors (db connection issues)

5. Calculate expiresAt as processedAt + 30 days using date-fns addDays()

Import webhookEvents from schema, addDays from date-fns, eq from drizzle-orm.

Pattern to follow from research:
```typescript
const startTime = Date.now();
const expiresAt = addDays(new Date(), 30);

// Check idempotency with insert-on-conflict pattern
try {
  await db.insert(webhookEvents).values({
    eventId: event.id,
    eventType: event.type,
    status: 'processing',
    expiresAt,
  });
} catch (error: any) {
  if (error?.code === '23505') { // Unique violation
    console.log(`Duplicate event: ${event.id} (${event.type})`);
    return NextResponse.json({ received: true });
  }
  throw error;
}

// Process event...
// Update status on completion
```
  </action>
  <verify>
Test with curl or Stripe CLI:
1. Send a test webhook event
2. Send the same event again - should log "Duplicate event" and return 200
3. Check webhook_events table has one row for the event
  </verify>
  <done>Webhook handler processes events exactly once with idempotency tracking, processing time metrics, and proper error handling</done>
</task>

</tasks>

<verification>
1. Schema compiles: `npm run build` passes
2. Migration applied: webhook_events table exists in Supabase
3. Idempotency works: Same event ID processed only once
4. Metrics tracked: processingTimeMs populated for processed events
5. Errors logged: Failed events have errorMessage populated
</verification>

<success_criteria>
- webhook_events table exists with eventId unique constraint
- Webhook handler checks idempotency BEFORE processing
- Duplicate events return 200 without re-processing
- Processing time tracked in milliseconds
- Failed events logged with error message
- All existing webhook event types continue to work
</success_criteria>

<output>
After completion, create `.planning/phases/24-webhook-hardening/24-01-SUMMARY.md`
</output>
