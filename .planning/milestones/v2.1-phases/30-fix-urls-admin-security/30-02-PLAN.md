---
phase: 30-fix-urls-admin-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/webhooks/stripe/route.ts
  - src/app/(marketing)/layout.tsx
  - src/components/pricing/feature-comparison-table.tsx
  - src/lib/stripe/products.ts
  - eslint.config.mjs
autonomous: true
requirements:
  - HOOK-02

must_haves:
  truths:
    - "Payment failed email return URL points to /settings/billing (not /dashboard/billing)"
    - "Marketing site header has a Pricing nav link"
    - "Marketing site footer has a Pricing nav link"
    - "No unused imports remain in feature-comparison-table.tsx"
    - "No unreferenced exports remain in products.ts (getTierDisplayName, getTierFeatures removed)"
    - "ESLint warns on unused variables/imports for future prevention"
    - "Development artifacts (.bak files, _ul files) are deleted"
  artifacts:
    - path: "src/app/api/webhooks/stripe/route.ts"
      provides: "Corrected return_url for payment failed email"
      contains: "/settings/billing"
    - path: "src/app/(marketing)/layout.tsx"
      provides: "Pricing nav links in header and footer"
      contains: "/pricing"
    - path: "eslint.config.mjs"
      provides: "Stricter unused-vars ESLint rule"
      contains: "no-unused-vars"
  key_links:
    - from: "src/app/api/webhooks/stripe/route.ts"
      to: "src/app/(dashboard)/settings/billing/page.tsx"
      via: "return_url in payment failed email"
      pattern: "/settings/billing"
    - from: "src/app/(marketing)/layout.tsx"
      to: "src/app/(marketing)/pricing/page.tsx"
      via: "nav link href"
      pattern: "href=\"/pricing\""
---

<objective>
Fix the broken payment failed email return URL, add pricing navigation links, remove dead code, and enable stricter ESLint rules.

Purpose: Close the HOOK-02 payment failed flow gap (URL points to non-existent route), make the pricing page discoverable from marketing site navigation, clean up unused code identified in the v2.1 audit, and prevent future dead code accumulation via ESLint.

Output: Corrected webhook URL, pricing nav links in marketing header/footer, cleaned up dead imports/exports, stricter ESLint config, deleted artifact files.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v2.1-ROADMAP.md
@.planning/phases/30-fix-urls-admin-security/30-RESEARCH.md
@src/app/api/webhooks/stripe/route.ts
@src/app/(marketing)/layout.tsx
@src/components/pricing/feature-comparison-table.tsx
@src/lib/stripe/products.ts
@eslint.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix payment failed return URL and add pricing navigation links</name>
  <files>
    src/app/api/webhooks/stripe/route.ts
    src/app/(marketing)/layout.tsx
  </files>
  <action>
    **Webhook URL fix (src/app/api/webhooks/stripe/route.ts):**
    1. Find the line containing `return_url: ` with `/dashboard/billing` (approximately line 308 per research).
    2. Change `/dashboard/billing` to `/settings/billing`.
    3. The full line should become: `return_url: \`${process.env.NEXT_PUBLIC_APP_URL}/settings/billing\`,`
    4. Verify no other occurrences of `/dashboard/billing` exist in this file.

    **Marketing header pricing link (src/app/(marketing)/layout.tsx):**
    5. In the header, between the logo `<Link>` and the auth buttons `<div>`, add a nav element with a pricing link:
       ```tsx
       <nav className="flex items-center gap-6">
         <Link href="/pricing" className="text-sm font-medium text-muted-foreground hover:text-foreground transition-colors">
           Pricing
         </Link>
       </nav>
       ```
       This goes AFTER the logo link and BEFORE the `<div className="flex items-center gap-4">` that contains Log in / Get Started buttons.
    6. The pricing link is visible to ALL users regardless of auth state (always visible) per locked decision.

    **Marketing footer pricing link (src/app/(marketing)/layout.tsx):**
    7. In the footer, add a Pricing link to the existing links div that contains Privacy and Terms:
       ```tsx
       <Link href="/pricing" className="hover:underline">
         Pricing
       </Link>
       ```
       Add it BEFORE the Privacy link so the order is: Pricing, Privacy, Terms.
  </action>
  <verify>
    - Grep for `/dashboard/billing` in `src/app/api/webhooks/stripe/route.ts` returns zero matches
    - Grep for `/settings/billing` in webhook route confirms the corrected URL exists
    - Grep for `href="/pricing"` in marketing layout confirms both header and footer links exist
    - `npm run build` succeeds
  </verify>
  <done>
    - Payment failed email return URL points to /settings/billing (not /dashboard/billing)
    - Marketing header contains /pricing nav link between logo and auth buttons
    - Marketing footer contains /pricing link alongside Privacy and Terms
    - All links are visible regardless of authentication state
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dead code, delete artifact files, and configure stricter ESLint rules</name>
  <files>
    src/components/pricing/feature-comparison-table.tsx
    src/lib/stripe/products.ts
    eslint.config.mjs
  </files>
  <action>
    **Remove unused imports (src/components/pricing/feature-comparison-table.tsx):**
    1. Remove the `import { TIER_LEVELS } from "@/lib/features/config";` line (unused in this file).
    2. Remove the `import { cn } from "@/lib/utils";` line (cn() is never called in this file).
    3. Do NOT remove TIER_LEVELS from `src/lib/features/config.ts` itself — it's used internally by `canTierAccessFeature()`.

    **Remove unreferenced exports (src/lib/stripe/products.ts):**
    4. Delete the `getTierDisplayName` function entirely (exported but never imported anywhere).
    5. Delete the `getTierFeatures` function entirely (exported but never imported anywhere).
    6. Keep all other exports in the file intact — they are used elsewhere.

    **Delete artifact files:**
    7. Delete `src/lib/db/schema.ts.bak` (development backup file).
    8. Delete `_ul`, `_ul-CORSAIRONEPRO`, and `_ul-CORSAIRONEPRO-2` from the project root (development artifacts from git status).
    9. These files should be removed from the filesystem, not just gitignored.

    **ESLint config (eslint.config.mjs):**
    10. Add a rules override block to the ESLint flat config array, after the globalIgnores entry:
        ```javascript
        {
          rules: {
            "@typescript-eslint/no-unused-vars": [
              "warn",
              {
                argsIgnorePattern: "^_",
                varsIgnorePattern: "^_",
                ignoreRestSiblings: true,
              },
            ],
          },
        },
        ```
    11. Use `"warn"` severity (NOT `"error"`) — the project likely has existing warnings and `"error"` would break the build.
    12. The `argsIgnorePattern: "^_"` allows intentionally unused args prefixed with underscore.
    13. The `varsIgnorePattern: "^_"` allows intentionally unused vars prefixed with underscore.
    14. The `ignoreRestSiblings: true` allows destructuring like `const { unused, ...rest } = obj`.

    **Full dead code sweep:**
    15. After the targeted removals above, run `npx eslint src/ --no-error-on-unmatched-pattern 2>&1 | head -50` to check for any additional unused imports/variables flagged by the new rule.
    16. Fix any newly-flagged unused imports that are trivially removable (simple import removals, not structural changes).
    17. For any complex warnings that require significant refactoring, leave as warnings (they are "warn" not "error" so they won't break the build).
  </action>
  <verify>
    - `grep -r "TIER_LEVELS" src/components/pricing/feature-comparison-table.tsx` returns no matches
    - `grep -r "import { cn }" src/components/pricing/feature-comparison-table.tsx` returns no matches
    - `grep -r "getTierDisplayName" src/` returns no matches (function fully removed)
    - `grep -r "getTierFeatures" src/` returns no matches (function fully removed)
    - `ls src/lib/db/schema.ts.bak` returns "No such file"
    - `ls _ul _ul-CORSAIRONEPRO _ul-CORSAIRONEPRO-2` returns "No such file" for all three
    - `grep "no-unused-vars" eslint.config.mjs` confirms the rule exists
    - `npm run build` succeeds (ESLint warnings don't break the build)
  </verify>
  <done>
    - TIER_LEVELS and cn imports removed from feature-comparison-table.tsx
    - getTierDisplayName and getTierFeatures functions deleted from products.ts
    - schema.ts.bak deleted
    - _ul, _ul-CORSAIRONEPRO, _ul-CORSAIRONEPRO-2 deleted from project root
    - ESLint configured with @typescript-eslint/no-unused-vars at "warn" level
    - Build passes cleanly
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Payment failed return URL is `/settings/billing` (not `/dashboard/billing`)
3. Marketing header has Pricing nav link
4. Marketing footer has Pricing nav link
5. No unused TIER_LEVELS or cn imports in feature-comparison-table.tsx
6. getTierDisplayName and getTierFeatures removed from products.ts
7. No .bak files or _ul artifact files remain
8. ESLint config has @typescript-eslint/no-unused-vars warn rule
9. `npm run lint` runs without errors (warnings acceptable)
</verification>

<success_criteria>
- HOOK-02 payment failed flow works end-to-end (correct return URL)
- Pricing page is discoverable from marketing site navigation
- Dead code eliminated and ESLint configured to prevent future accumulation
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/30-fix-urls-admin-security/30-02-SUMMARY.md`
</output>
