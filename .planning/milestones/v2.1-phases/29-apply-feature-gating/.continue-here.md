---
phase: 29-apply-feature-gating
plan: 01
task: 0
total_tasks: 3
status: planned_not_started
last_updated: 2026-02-17T22:56:00Z
---

<current_state>
Phase 29 planning is COMPLETE. Research done, plan created, plan verified. Ready to execute.

This is a gap closure phase from the v2.1 milestone audit. The feature gating infrastructure (FeatureGate, LockedNavItem, requireFeature) was built in Phase 26 but never wired into the actual application.
</current_state>

<completed_work>

- Research: 29-RESEARCH.md written with full analysis of existing Phase 26 infrastructure
- Planning: 29-01-PLAN.md created with 3 tasks in 1 wave
- Verification: Plan checker passed all 6 dimensions
</completed_work>

<remaining_work>

- Plan 29-01: Execute 3 tasks
  - Task 1: Add requireFeature(FEATURES.PDF_IMPORTS) to /api/import/route.ts
  - Task 2: Wrap analytics page <main> with <FeatureGate feature={FEATURES.BASIC_ANALYTICS}>
  - Task 3: Add LockedNavItem for "Spending Monitor" placeholder in app-sidebar.tsx
</remaining_work>

<decisions_made>

- Chose PDF imports API for requireFeature() — clear tier boundary, existing route structure
- Chose analytics page for FeatureGate — already "use client", visible feature to demonstrate gating
- Chose "Spending Monitor" as locked nav item — enhanced-tier placeholder that doesn't exist yet, demonstrates locked state
- Primary-tier features (pdf_imports, basic_analytics) — trial users automatically have access via `userTier ?? "primary"` in canTierAccessFeature
</decisions_made>

<blockers>
None
</blockers>

<context>
This is v2.1 milestone gap closure. The audit found that Phase 26 built the feature gating infrastructure but never applied it. All three components (FeatureGate, LockedNavItem, requireFeature) are exported but never imported anywhere.

The plan is simple: wire the existing infrastructure into three places:
1. API route protection (403 for wrong tier)
2. Page content gating (upgrade modal for locked features)
3. Navigation item locking (visual locked state)

All infrastructure is verified to exist and work. This is purely a wiring task with no new components needed.
</context>

<next_action>
Run `/gsd:execute-phase 29` to execute the plan.

The plan has 1 wave with 3 tasks:
1. Modify src/app/api/import/route.ts - add requireFeature call and 403 handler
2. Modify src/app/(dashboard)/analytics/page.tsx - wrap <main> with FeatureGate
3. Modify src/components/layout/app-sidebar.tsx - add LockedNavItem for Spending Monitor

After Phase 29, Phase 30 (Fix URLs & Admin Security) remains for v2.1 completion.
</next_action>
