---
phase: 51-ui-screens-verification
plan: 04
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - src/components/vault/batch-uploader.tsx
  - src/components/statements/statement-line-items.tsx
  - src/components/transactions/transaction-filters.tsx
  - src/components/transactions/transaction-browser.tsx
autonomous: true
requirements: [UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "Batch uploader shows post-processing stats after upload"
    - "Statement detail shows inferred merchant and recurring status per line item"
    - "Transaction browser has recurring-only and unmatched-only filter toggles"
  artifacts:
    - path: "src/components/vault/batch-uploader.tsx"
      provides: "Extended batch uploader with processing stats"
      contains: "recurring"
    - path: "src/components/statements/statement-line-items.tsx"
      provides: "Statement line items with merchant + recurring status"
      contains: "recurring"
    - path: "src/components/transactions/transaction-filters.tsx"
      provides: "Extended transaction filters with recurring/unmatched toggles"
      contains: "recurringOnly"
  key_links:
    - from: "src/components/statements/statement-line-items.tsx"
      to: "src/lib/hooks/use-recurring.ts"
      via: "useLabelTransaction mutation"
      pattern: "useLabelTransaction"
    - from: "src/components/transactions/transaction-filters.tsx"
      to: "src/components/transactions/transaction-browser.tsx"
      via: "filter props"
      pattern: "recurringOnly"
---

<objective>
Extend existing statement uploads (UI-02), statement detail (UI-03), and transaction explorer (UI-04) with recurring payment information and actions.

Purpose: These are enhancements to existing pages — adding recurring payment context to the existing upload flow, statement detail, and transaction browser.
Output: Modified existing components with recurring-aware features.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-ui-screens-verification/51-CONTEXT.md
@.planning/phases/51-ui-screens-verification/51-01-SUMMARY.md

<interfaces>
<!-- Hooks from Plan 01 -->
From src/lib/hooks/use-recurring.ts:
```typescript
export function useLabelTransaction(): UseMutationResult;
```

Existing components to extend:
- src/components/vault/batch-uploader.tsx — Upload flow
- src/components/statements/statement-line-items.tsx — Line item display
- src/components/transactions/transaction-filters.tsx — Filter controls
- src/components/transactions/transaction-browser.tsx — Browser container
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add processing stats to batch uploader and recurring status to statement detail (UI-02, UI-03)</name>
  <files>src/components/vault/batch-uploader.tsx, src/components/statements/statement-line-items.tsx</files>
  <read_first>src/components/vault/batch-uploader.tsx, src/components/statements/statement-line-items.tsx, src/lib/hooks/use-recurring.ts, src/app/api/statements/route.ts</read_first>
  <action>
**Extend `src/components/vault/batch-uploader.tsx` (UI-02):**
- After the existing upload success state, add a "Processing Results" section showing stats
- The batch upload response (from the existing API) should already include counts from the pipeline. Check the response shape.
- If the response includes transaction counts, show: "Lines extracted: X", "Transactions created: Y", "Merchants resolved: Z", "Recurring patterns found: W"
- If the API response doesn't include these counts, add a simplified stats display showing file count and status
- Use a small Card or div with a grid of stat items below the success message
- Style as informational (blue/muted background), not as an error or warning
- Do NOT restructure the upload flow — just add stats display after successful processing

**Extend `src/components/statements/statement-line-items.tsx` (UI-03):**
- Read the existing component first to understand its structure and props
- For each line item row, add:
  1. **Inferred merchant column/badge:** If the transaction has a merchantEntityId or merchantName from normalization, show it as a small Badge or text label. Check what data is available on the transaction object returned by the statement detail API.
  2. **Recurring status indicator:** If the transaction is linked to a recurring_series, show a small green Badge "Recurring" with the master name if available. If unmatched, show nothing or a subtle "Unmatched" badge.
  3. **User action buttons:** Add a small DropdownMenu with 3 actions on each row:
     - "Mark as Recurring" — calls useLabelTransaction with label: "recurring"
     - "Not Recurring" — calls useLabelTransaction with label: "not_recurring"
     - "Ignore" — calls useLabelTransaction with label: "ignore"
  - Import `useLabelTransaction` from `@/lib/hooks/use-recurring`
  - After mutation success, invalidate relevant queries
- Keep existing functionality intact. These are ADDITIVE changes only.
- If the component is a Server Component, add "use client" if needed, or create a small client wrapper for the action buttons.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "recurring\|Recurring\|processing" src/components/vault/batch-uploader.tsx
    - grep -q "useLabelTransaction\|label\|recurring" src/components/statements/statement-line-items.tsx
  </acceptance_criteria>
  <done>Batch uploader shows processing stats after upload. Statement line items show inferred merchant, recurring status badges, and label action buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Add recurring/unmatched filter toggles to transaction browser (UI-04)</name>
  <files>src/components/transactions/transaction-filters.tsx, src/components/transactions/transaction-browser.tsx</files>
  <read_first>src/components/transactions/transaction-filters.tsx, src/components/transactions/transaction-browser.tsx, src/lib/hooks/use-transactions.ts, src/app/api/transactions/route.ts</read_first>
  <action>
**Extend `src/components/transactions/transaction-filters.tsx` (UI-04):**
- Read the existing filters component to understand its structure and how it communicates filter state
- Add two new toggle filters:
  1. "Recurring only" — a toggle/switch or checkbox that when enabled, filters to only show transactions linked to a recurring_series
  2. "Unmatched only" — a toggle/switch or checkbox that shows only transactions NOT linked to any series
- These should be mutually exclusive (can't be both recurring and unmatched)
- Add the toggle controls in the same visual row/group as existing filters
- Use the existing filter communication pattern (likely URL params via nuqs or props callback)

**Extend `src/components/transactions/transaction-browser.tsx`:**
- Pass new filter values (recurringOnly, unmatchedOnly) to the API query
- The transaction list API (GET /api/transactions) may need query params: `recurringOnly=true` or `unmatchedOnly=true`
- Check if the API supports these filters. If not, add client-side filtering as a fallback:
  - Filter transactions array: recurringOnly shows items where recurringSeriesId is not null
  - unmatchedOnly shows items where recurringSeriesId is null
- If the transaction objects don't have recurringSeriesId, check what fields are available and use normalizedDescription or merchantEntityId as proxy

**Important:** Read the existing components CAREFULLY before modifying. Preserve all existing filter functionality. These are ADDITIVE changes only.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "recurringOnly\|recurring" src/components/transactions/transaction-filters.tsx
    - grep -q "unmatchedOnly\|unmatched" src/components/transactions/transaction-filters.tsx
    - grep -q "recurringOnly\|recurring" src/components/transactions/transaction-browser.tsx
  </acceptance_criteria>
  <done>Transaction browser has recurring-only and unmatched-only toggles in the filter bar, filtering the transaction list accordingly</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (or only pre-existing errors)
- Batch uploader shows processing stats after successful upload
- Statement line items show merchant and recurring status with label actions
- Transaction browser has recurring/unmatched filter toggles
</verification>

<success_criteria>
- UI-02: Statement upload shows processing status with line item and recurring pattern counts
- UI-03: Statement detail shows inferred merchant, recurring status badges, and user action buttons per row
- UI-04: Transaction explorer has recurring-only and unmatched-only filter toggles alongside existing filters
</success_criteria>

<output>
After completion, create `.planning/phases/51-ui-screens-verification/51-04-SUMMARY.md`
</output>
