---
phase: 51-ui-screens-verification
plan: 03
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - src/app/(dashboard)/recurring/[id]/page.tsx
  - src/components/recurring/recurring-master-detail.tsx
  - src/app/(dashboard)/recurring/settings/page.tsx
  - src/components/recurring/merchant-alias-manager.tsx
autonomous: true
requirements: [UI-07, UI-08]

must_haves:
  truths:
    - "Recurring master detail page shows metadata, series chain, and event history"
    - "Status can be changed via pause/cancel/reactivate buttons"
    - "Merge action opens modal to select target master"
    - "Settings page shows merchant aliases with add/edit/delete"
  artifacts:
    - path: "src/app/(dashboard)/recurring/[id]/page.tsx"
      provides: "Recurring master detail page"
      contains: "RecurringMasterDetail"
    - path: "src/components/recurring/recurring-master-detail.tsx"
      provides: "Master detail view with metadata, series, events"
      exports: ["RecurringMasterDetail"]
    - path: "src/app/(dashboard)/recurring/settings/page.tsx"
      provides: "Settings page with merchant alias management"
      contains: "MerchantAliasManager"
    - path: "src/components/recurring/merchant-alias-manager.tsx"
      provides: "Merchant alias CRUD interface"
      exports: ["MerchantAliasManager"]
  key_links:
    - from: "src/components/recurring/recurring-master-detail.tsx"
      to: "src/lib/hooks/use-recurring.ts"
      via: "useRecurringMasterDetail, useChangeMasterStatus, useMergeMasters, useUpdateMaster"
      pattern: "useRecurringMasterDetail"
    - from: "src/components/recurring/merchant-alias-manager.tsx"
      to: "/api/recurring/merchants"
      via: "fetch calls for merchant CRUD"
      pattern: "fetch.*merchant"
---

<objective>
Build the recurring master detail page (UI-07) and settings/rules page with merchant alias management (UI-08, MERCH-05).

Purpose: The detail page provides the full view of a recurring payment item with history and management controls. The settings page fulfills MERCH-05 by allowing users to manage merchant aliases.
Output: Two new pages with their component trees.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-ui-screens-verification/51-CONTEXT.md
@.planning/phases/51-ui-screens-verification/51-01-SUMMARY.md

<interfaces>
<!-- Hooks from Plan 01 -->
From src/lib/hooks/use-recurring.ts:
```typescript
export function useRecurringMasterDetail(id: string): UseQueryResult;
export function useUpdateMaster(): UseMutationResult;
export function useMergeMasters(): UseMutationResult;
export function useChangeMasterStatus(): UseMutationResult;
export function useRecurringMasters(filters): UseQueryResult;
```

API response shapes (from Phase 50):
```typescript
// GET /api/recurring/masters/[id]
{
  data: {
    master: { id, name, recurringKind, status, expectedAmount, currency, billingFrequency, billingDayOfMonth, importanceRating, url, notes, confidence, nextExpectedDate, merchantEntityId, merchantName, createdAt, updatedAt },
    seriesLinks: Array<{ id, seriesId, isPrimary, linkedAt, series: { merchantName, cadence, avgAmount, amountType, transactionCount, firstSeen, lastSeen } }>,
    events: Array<{ id, eventType, metadata, createdAt }>
  }
}
```

From src/lib/validations/recurring.ts:
```typescript
export const updateMasterSchema = createMasterSchema.partial();
export const mergeSchema = z.object({ mergeIntoId: z.string().uuid() });
export const statusChangeSchema = z.object({ status: z.enum(["active", "paused", "cancelled", "dormant", "needs_review"]) });
```

From src/lib/db/schema.ts:
```typescript
// merchantEntities and merchantAliases table definitions
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build recurring master detail page (UI-07)</name>
  <files>src/app/(dashboard)/recurring/[id]/page.tsx, src/components/recurring/recurring-master-detail.tsx</files>
  <read_first>src/lib/hooks/use-recurring.ts, src/app/api/recurring/masters/[id]/route.ts, src/lib/validations/recurring.ts, src/components/subscriptions/subscription-form.tsx</read_first>
  <action>
**Create `src/components/recurring/recurring-master-detail.tsx`:**
- "use client" component receiving `id` prop (string)
- Uses `useRecurringMasterDetail(id)`, `useUpdateMaster()`, `useChangeMasterStatus()`, `useMergeMasters()`, `useRecurringMasters({})` (for merge target list)

**Header section:**
- Name as h1, recurringKind Badge, status Badge (same color scheme as list page)
- "Edit" Button opening a Dialog with form fields from updateMasterSchema (name, recurringKind, description, expectedAmount, billingFrequency, billingDayOfMonth, importanceRating, url, notes)
- Use React Hook Form + zodResolver(updateMasterSchema)

**Metadata card (Card component):**
- Grid layout (2 cols on md) showing: expectedAmount + currency, billingFrequency, billingDayOfMonth, importanceRating (1-5 stars or number), confidence (percentage), nextExpectedDate (formatted), url (clickable link), notes
- Use label + value pairs

**Status action buttons (below metadata):**
- Conditional based on current status:
  - If active: "Pause" and "Cancel" buttons
  - If paused: "Reactivate" and "Cancel" buttons
  - If cancelled/dormant: "Reactivate" button
  - If needs_review: "Activate" and "Cancel" buttons
- Each calls `changeMasterStatus.mutate({ id, status: newStatus })`
- "Merge" button always visible — opens Dialog showing list of other masters (from useRecurringMasters) to select merge target. Calls `mergeMasters.mutate({ id, mergeIntoId: targetId })`
- After merge success, navigate to target master detail page using router.push

**Series chain section (Card):**
- Title "Linked Series"
- List of series links showing: merchantName, cadence, avgAmount, amountType badge, transactionCount, date range (firstSeen - lastSeen), isPrimary Badge
- If no series: "No linked series yet" text

**Event log section (Card):**
- Title "Activity Log"
- Scrollable list (max-h-80 overflow-y-auto) of events in reverse chronological order
- Each event: eventType as Badge, metadata (JSON displayed as key-value if available), createdAt formatted

**Loading:** Full-page Skeleton with useDelayedLoading
**Error:** ServiceUnavailable with retry
**Not found:** If data is null/undefined after load, show "Recurring item not found" message

**Create `src/app/(dashboard)/recurring/[id]/page.tsx`:**
- Server component page
- Extract `id` from params
- Returns DashboardHeader with title "Recurring Detail" + breadcrumb-style link back to /recurring
- Renders `RecurringMasterDetail id={params.id}`
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "RecurringMasterDetail" src/components/recurring/recurring-master-detail.tsx
    - grep -q "useRecurringMasterDetail" src/components/recurring/recurring-master-detail.tsx
    - grep -q "useChangeMasterStatus" src/components/recurring/recurring-master-detail.tsx
    - grep -q "useMergeMasters" src/components/recurring/recurring-master-detail.tsx
    - grep -q "useUpdateMaster" src/components/recurring/recurring-master-detail.tsx
    - grep -q "seriesLinks" src/components/recurring/recurring-master-detail.tsx
    - grep -q "events" src/components/recurring/recurring-master-detail.tsx
    - grep -q "RecurringMasterDetail" src/app/(dashboard)/recurring/[id]/page.tsx
  </acceptance_criteria>
  <done>Detail page at /recurring/[id] shows master metadata, edit form, status change buttons, merge action, linked series chain, and event log timeline</done>
</task>

<task type="auto">
  <name>Task 2: Build settings page with merchant alias management (UI-08, MERCH-05)</name>
  <files>src/app/(dashboard)/recurring/settings/page.tsx, src/components/recurring/merchant-alias-manager.tsx</files>
  <read_first>src/lib/db/schema.ts, src/app/api/recurring/masters/route.ts, src/lib/hooks/use-recurring.ts</read_first>
  <action>
**Create `src/components/recurring/merchant-alias-manager.tsx`:**
- "use client" component
- Since Phase 50 did not create dedicated merchant entity/alias CRUD endpoints, this component will use direct fetch calls to a simple inline API pattern OR create the endpoints inline:
  - Fetch merchant entities: GET /api/recurring/merchants (to be created — simple select from merchantEntities where userId)
  - Create/update: POST /api/recurring/merchants (creates entity + alias), PATCH /api/recurring/merchants/[id] (updates)
  - Delete alias: DELETE /api/recurring/merchants/[id] (deletes entity and cascaded aliases)

**If API endpoints don't exist yet, create them:**
- `src/app/api/recurring/merchants/route.ts` — GET (list merchant entities with aliases join) + POST (create entity + alias)
- `src/app/api/recurring/merchants/[id]/route.ts` — PATCH (update entity name) + DELETE (delete entity)

**MerchantAliasManager component:**
- Uses useQuery with key ["recurring", "merchants"] to fetch merchant list
- Table layout: columns — Merchant Name | Aliases (comma-separated list) | Actions
- Actions column: Edit button (opens inline edit or Dialog), Delete button (with confirmation Dialog)
- Add form at top: two inputs — "Merchant Name" + "Alias Text" + "Add" button
- On add: POST to create merchant entity with the alias
- On edit: Opens Dialog to edit merchant name
- On delete: Confirmation dialog, then DELETE
- Each mutation invalidates ["recurring", "merchants"] query key
- Loading: Skeleton table
- Empty: EmptyState with text "No merchant aliases yet. Add aliases to improve matching accuracy."
- Note in UI: "Aliases help the system match statement descriptions to known merchants"

**Create `src/app/(dashboard)/recurring/settings/page.tsx`:**
- Server component page with metadata: `title: "Recurring Settings"`
- Returns DashboardHeader with title "Recurring Settings" + breadcrumb to /recurring
- Renders `<MerchantAliasManager />`
- Add a note below: "Detection thresholds are system-defined and cannot be customized" (per CONTEXT.md: no tolerance settings in v4.0)
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "MerchantAliasManager" src/components/recurring/merchant-alias-manager.tsx
    - grep -q "merchantEntities\|merchant_entities\|merchants" src/components/recurring/merchant-alias-manager.tsx
    - grep -q "MerchantAliasManager" src/app/(dashboard)/recurring/settings/page.tsx
    - grep -q "Recurring Settings" src/app/(dashboard)/recurring/settings/page.tsx
  </acceptance_criteria>
  <done>Settings page at /recurring/settings shows merchant alias table with add/edit/delete. MERCH-05 delivered. Tolerance settings noted as system-defined.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (or only pre-existing errors)
- /recurring/[id] page renders detail view with metadata, status buttons, series chain, events
- /recurring/settings page renders merchant alias management table
</verification>

<success_criteria>
- UI-07: Master detail shows full metadata, linked series history, event log, edit controls, status change buttons, merge action
- UI-08: Settings page has merchant alias management (MERCH-05) with CRUD operations
</success_criteria>

<output>
After completion, create `.planning/phases/51-ui-screens-verification/51-03-SUMMARY.md`
</output>
