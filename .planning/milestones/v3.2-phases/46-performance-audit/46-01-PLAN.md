---
phase: 46-performance-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - package.json
  - .planning/performance/bundle-treemap-before.html
autonomous: true
requirements:
  - PERF-01
  - PERF-03

must_haves:
  truths:
    - "Bundle analyzer is configured and gated behind ANALYZE=true env var"
    - "npm run analyze script exists and works"
    - "optimizePackageImports includes lucide-react in next.config.ts"
    - "Before-optimization treemap HTML is committed to .planning/performance/"
  artifacts:
    - path: "next.config.ts"
      provides: "Bundle analyzer config + optimizePackageImports"
      contains: "optimizePackageImports"
    - path: "package.json"
      provides: "@next/bundle-analyzer dev dependency + analyze script"
      contains: "bundle-analyzer"
    - path: ".planning/performance/bundle-treemap-before.html"
      provides: "Pre-optimization bundle treemap"
      min_lines: 10
  key_links:
    - from: "next.config.ts"
      to: "@next/bundle-analyzer"
      via: "import and withBundleAnalyzer wrapper"
      pattern: "withBundleAnalyzer"
    - from: "next.config.ts"
      to: "withSentryConfig"
      via: "outer wrapper (Sentry must be outermost)"
      pattern: "withSentryConfig\\(withBundleAnalyzer"
---

<objective>
Install @next/bundle-analyzer, configure next.config.ts with bundle analyzer chaining and optimizePackageImports for lucide-react, add the npm analyze script, and generate the before-optimization treemap.

Purpose: Establish the measurement baseline before any dynamic import optimizations are applied (Plan 02). The before treemap is essential for showing optimization impact.
Output: Configured next.config.ts, package.json with analyze script, before-treemap HTML committed.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-performance-audit/46-CONTEXT.md
@.planning/phases/46-performance-audit/46-RESEARCH.md
@next.config.ts
@package.json

<interfaces>
<!-- Current next.config.ts structure that must be preserved -->
From next.config.ts:
```typescript
import type { NextConfig } from "next";
import { withSentryConfig } from "@sentry/nextjs";

const nextConfig: NextConfig = {
  serverExternalPackages: ["pino", "pino-pretty"],
  async redirects() { /* ... */ },
};

export default withSentryConfig(nextConfig, { /* sentry opts */ });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install bundle analyzer, configure next.config.ts, add analyze script</name>
  <files>next.config.ts, package.json</files>
  <action>
1. Install @next/bundle-analyzer as a dev dependency:
   `npm install --save-dev @next/bundle-analyzer`

2. Update next.config.ts to chain bundle analyzer with Sentry:
   - Add `import bundleAnalyzer from "@next/bundle-analyzer"` at the top
   - Create `const withBundleAnalyzer = bundleAnalyzer({ enabled: process.env.ANALYZE === "true" })`
   - Add `experimental: { optimizePackageImports: ["lucide-react"] }` to the nextConfig object (PERF-03)
   - Change the export to `withSentryConfig(withBundleAnalyzer(nextConfig), { ... })` — Sentry MUST be the outermost wrapper
   - Preserve all existing config (serverExternalPackages, redirects, sentry options) unchanged

   Note on PERF-03: lucide-react is already auto-optimized by default in Next.js 16. This explicit entry is a belt-and-suspenders declaration per the locked user decision in CONTEXT.md. Add a comment noting this.

3. Add analyze script to package.json scripts:
   `"analyze": "ANALYZE=true next build"`

4. If TypeScript complains about the @next/bundle-analyzer import (no type declarations), the import pattern `import bundleAnalyzer from "@next/bundle-analyzer"` should work. If it fails, fall back to adding `// @ts-expect-error -- no type declarations` above the import.

5. Run `npm run build` (without ANALYZE) to verify the config changes do not break the standard build.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/OneDrive/19. Vibe codeing/recurring-payments" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>next.config.ts has withBundleAnalyzer chained inside withSentryConfig, optimizePackageImports includes lucide-react, package.json has @next/bundle-analyzer in devDependencies and "analyze" script, standard build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Generate before-optimization bundle treemap</name>
  <files>.planning/performance/bundle-treemap-before.html</files>
  <action>
1. Create the `.planning/performance/` directory if it does not exist.

2. Run `ANALYZE=true npm run build` (or equivalently `npm run analyze`). This will generate HTML treemap files in `.next/analyze/`.

   On Windows bash, use: `ANALYZE=true npm run build`

3. The bundle analyzer generates files at `.next/analyze/client.html` (and possibly server.html, edge.html). Copy the client.html to `.planning/performance/bundle-treemap-before.html`.

   `cp .next/analyze/client.html .planning/performance/bundle-treemap-before.html`

   If the analyzer opens browser tabs automatically, that is expected behavior — the key artifact is the saved HTML file.

4. Verify the HTML file exists and is non-trivial (should be several hundred KB with embedded JavaScript for the interactive treemap).

5. Inspect the treemap output briefly to note: total client bundle size, largest chunks, whether react-pdf/pdfjs-dist appears in initial bundles (relevant for PERF-04 verification in Plan 02). Document any notable findings as a comment in the SUMMARY.
  </action>
  <verify>
    <automated>ls -la "C:/Users/andre/OneDrive/19. Vibe codeing/recurring-payments/.planning/performance/bundle-treemap-before.html" 2>&1</automated>
  </verify>
  <done>bundle-treemap-before.html exists in .planning/performance/ with non-trivial file size (100KB+), representing the pre-optimization bundle composition.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds without errors (config is valid)
- `grep "optimizePackageImports" next.config.ts` returns a match
- `grep "bundle-analyzer" package.json` returns a match
- `grep "analyze" package.json` shows the analyze script
- `.planning/performance/bundle-treemap-before.html` exists and is >100KB
</verification>

<success_criteria>
- next.config.ts has @next/bundle-analyzer chained correctly (inner) with withSentryConfig (outer)
- optimizePackageImports explicitly lists lucide-react with a comment noting it is already auto-optimized in Next.js 16
- package.json has @next/bundle-analyzer in devDependencies and "analyze" in scripts
- Before-optimization treemap HTML committed to .planning/performance/
- Standard build (without ANALYZE) still passes
</success_criteria>

<output>
After completion, create `.planning/phases/46-performance-audit/46-01-SUMMARY.md`
</output>
