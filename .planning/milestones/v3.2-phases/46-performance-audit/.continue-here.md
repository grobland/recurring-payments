---
phase: 46-performance-audit
task: post-milestone
total_tasks: 0
status: milestone_complete_pending_e2e
last_updated: 2026-03-10T12:01:49.286Z
---

<current_state>
v3.2 milestone (phases 43-46) is fully complete — all 7 plans executed with summaries.
Before formally closing the milestone, user wants to do a full E2E test on a fresh empty dataset to validate the new-user experience end-to-end.
</current_state>

<completed_work>

- Phase 43: Overlap Detection — 2/2 plans done
- Phase 44: Onboarding Hints — 2/2 plans done
- Phase 45: Sidebar Redesign — 1/1 plans done
- Phase 46: Performance Audit — 2/2 plans done
- Created `scripts/reset-database.sql` — full clean-slate reset script (drops all tables, enums, materialized view)
</completed_work>

<remaining_work>

- Run reset SQL in Supabase SQL Editor (paste scripts/reset-database.sql)
- Recreate schema: `npm run db:push`
- Seed data: `npm run db:seed` + `npx tsx src/scripts/seed-stripe-prices.ts`
- Manual E2E walkthrough on empty dataset:
  1. Register new account (14-day trial auto-starts)
  2. Verify onboarding hints appear on all empty screens (Phase 44)
  3. Add subscriptions manually, verify overlap detection (Phase 43)
  4. Import PDFs, check vault, transactions, analytics
  5. Verify sidebar redesign in light and dark mode (Phase 45)
  6. Confirm page loads are snappy (Phase 46 dynamic imports)
- Fix any bugs found during E2E testing
- Close milestone: `/gsd:complete-milestone`
</remaining_work>

<decisions_made>

- Chose Option A (separate/reset Supabase project) over keeping existing data for clean test
- Created SQL reset script instead of manual table drops for repeatability
</decisions_made>

<blockers>
- None — ready to execute the E2E test
</blockers>

<context>
The 46-02 checkpoint from last session noted "charts render verification and Lighthouse scores pending" — this was a human-verify step that can be checked during the E2E walkthrough. The milestone is functionally complete but user wants confidence before closing v3.2.
</context>

<next_action>
Start with: Run scripts/reset-database.sql in Supabase SQL Editor, then `npm run db:push && npm run db:seed` to get a fresh environment, then `npm run dev` and walk through as a new user.
</next_action>
