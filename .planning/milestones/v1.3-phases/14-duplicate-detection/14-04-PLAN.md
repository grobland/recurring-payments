---
phase: 14-duplicate-detection
plan: 04
type: execute
wave: 3
depends_on: ["14-02", "14-03"]
files_modified:
  - src/lib/hooks/index.ts
  - src/app/api/import/confirm/route.ts
autonomous: false

must_haves:
  truths:
    - "User sees warning banner when importing subscription similar to existing one"
    - "User can view similarity score and matching fields during import"
    - "User can choose to keep both, skip duplicate, or merge during import"
    - "User can trigger background scan from subscriptions page"
    - "User can review list of potential duplicates with evidence"
    - "User can merge two subscriptions into one"
  artifacts: []
  key_links: []
---

<objective>
Wire up remaining integrations and verify the complete duplicate detection feature.

Purpose: Ensure all parts work together: import-time detection with merge action, hooks exported from central index, and end-to-end verification with user.

Output: Complete, verified duplicate detection feature ready for production.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-duplicate-detection/14-01-SUMMARY.md
@.planning/phases/14-duplicate-detection/14-02-SUMMARY.md
@.planning/phases/14-duplicate-detection/14-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export hooks and handle import merge action</name>
  <files>
    src/lib/hooks/index.ts
    src/app/api/import/confirm/route.ts
  </files>
  <action>
    Update src/lib/hooks/index.ts:
    - Export useDuplicateScan from './use-duplicate-scan'
    - Export useMergeSubscription from './use-merge-subscription'

    Update src/app/api/import/confirm/route.ts to handle merge action:
    1. When processing subscriptions, check action field:
       - 'create': create new subscription (existing behavior)
       - 'skip': don't create, increment skippedCount (existing behavior)
       - 'merge': merge into existing subscription
    2. For merge action:
       - Validate mergeWithId is provided
       - Fetch existing subscription by mergeWithId
       - Verify it belongs to user
       - Update existing subscription with imported values (name, amount, frequency, etc.)
       - Don't create new subscription
       - Increment mergedCount in audit
    3. Return summary with merged count

    This allows "Merge" button during import to work end-to-end.
  </action>
  <verify>
    - TypeScript compiles: `npx tsc --noEmit`
    - Import flow with merge action works
  </verify>
  <done>
    Hooks exported from central index, import confirm handles merge action.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete duplicate detection feature:
    1. Similarity algorithm with multi-field weighted scoring
    2. Import-time duplicate detection with warnings
    3. Keep/Skip/Merge actions during import
    4. Background duplicate scan on subscriptions page
    5. Merge field picker with undo capability
  </what-built>
  <how-to-verify>
    **Test Import-Time Detection (DUP-01, DUP-02, DUP-03):**
    1. Start dev server: `npm run dev`
    2. Ensure you have at least one subscription (e.g., "Netflix $15.99/month")
    3. Navigate to /import
    4. Select account and upload a PDF statement that includes Netflix or similar
    5. In review step:
       - [ ] Warning badge appears on matching item with similarity % (DUP-01)
       - [ ] Click badge or item to see side-by-side comparison (DUP-02)
       - [ ] Comparison shows matching fields highlighted
       - [ ] Keep/Skip/Merge buttons work (DUP-03)
       - [ ] 85%+ match has red warning, 70-84% has yellow

    **Test Background Scan (DUP-04, DUP-05):**
    1. Create two similar subscriptions manually:
       - "Netflix Premium" $15.99/month
       - "Netflix" $14.99/month
    2. Navigate to /subscriptions
    3. Click "Find Duplicates" button
       - [ ] Spinner shows while scanning
       - [ ] Results appear inline (DUP-04)
       - [ ] Pair shows with similarity score and matching fields (DUP-05)

    **Test Merge (DUP-06):**
    1. From scan results, click "Merge" on a duplicate pair
    2. In field picker dialog:
       - [ ] All fields shown with target/source options
       - [ ] "Newer wins" pre-selected by default
       - [ ] Can click to change selection for each field
    3. Click "Merge" to confirm
       - [ ] Source subscription disappears from list
       - [ ] Toast appears with "Undo" button
    4. Click "Undo" in toast
       - [ ] Both subscriptions restored
       - [ ] Toast confirms undo

    **Test Edge Cases:**
    - [ ] Empty state: "No duplicates found" message appears briefly
    - [ ] Already merged subscription doesn't appear in scan
    - [ ] Undo after 24hr shows error (test by changing mergedAt in DB if needed)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All requirements verified through checkpoint.
</verification>

<success_criteria>
- Human verified all 6 DUP requirements work
- Import-time detection shows warnings and allows actions
- Background scan finds duplicates
- Merge combines subscriptions with field selection
- Undo restores merged subscription
</success_criteria>

<output>
After completion, create `.planning/phases/14-duplicate-detection/14-04-SUMMARY.md`
</output>
