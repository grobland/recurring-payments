---
phase: 03-core-crud-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/auth.setup.ts
  - playwright.config.ts
  - .gitignore
autonomous: true

user_setup:
  - service: test-user
    why: "E2E tests need a test user account to authenticate"
    env_vars:
      - name: TEST_USER_EMAIL
        source: "Create a test user in the app, then add email to .env.local"
      - name: TEST_USER_PASSWORD
        source: "Password for the test user account"

must_haves:
  truths:
    - "Playwright can authenticate as a test user"
    - "Auth state is saved and reused across tests"
    - "E2E tests run in authenticated state without re-logging in"
  artifacts:
    - path: "tests/auth.setup.ts"
      provides: "Playwright authentication setup"
      min_lines: 20
    - path: "playwright.config.ts"
      provides: "Project-based setup with storageState"
      contains: "storageState"
    - path: ".gitignore"
      provides: "Ignores auth state files"
      contains: "playwright/.auth"
  key_links:
    - from: "tests/auth.setup.ts"
      to: "/login"
      via: "page.goto and form submission"
      pattern: "page\\.goto.*login"
    - from: "playwright.config.ts"
      to: "tests/auth.setup.ts"
      via: "setup project dependency"
      pattern: "dependencies.*setup"
---

<objective>
Set up Playwright authentication for E2E tests.

Purpose: Enable authenticated E2E testing by creating auth.setup.ts that logs in once and saves browser state for reuse. This fixes the Phase 2 pending todo (auth.setup.ts) and enables all authenticated E2E tests.
Output: Working Playwright auth setup that authenticates once per test run.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-crud-verification/03-RESEARCH.md

@playwright.config.ts
@src/app/(auth)/login/login-form.tsx
@tests/e2e/pdf-import.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth.setup.ts for Playwright authentication</name>
  <files>tests/auth.setup.ts</files>
  <action>
Create `tests/auth.setup.ts` that:
1. Navigates to `/login`
2. Fills email field (id="email") with `process.env.TEST_USER_EMAIL`
3. Fills password field (id="password") with `process.env.TEST_USER_PASSWORD`
4. Clicks the "Sign in" button
5. Waits for redirect to `/dashboard`
6. Verifies login succeeded by checking for dashboard content
7. Saves auth state to `playwright/.auth/user.json`

Use `test as setup` from `@playwright/test` (not regular `test`).
Use `path.join(__dirname, '../playwright/.auth/user.json')` for the auth file path.

Handle the case where env vars are missing with meaningful error messages.
  </action>
  <verify>File exists at `tests/auth.setup.ts` with correct structure</verify>
  <done>auth.setup.ts created with login flow and storageState save</done>
</task>

<task type="auto">
  <name>Task 2: Update playwright.config.ts for project-based auth</name>
  <files>playwright.config.ts, .gitignore</files>
  <action>
Update `playwright.config.ts` to:
1. Change testDir to `"./tests"` (to include both auth.setup.ts and e2e folder)
2. Add a `setup` project that matches `**/auth.setup.ts`
3. Update the chromium project to:
   - Use `storageState: 'playwright/.auth/user.json'`
   - Add `dependencies: ['setup']`
4. Keep firefox, webkit, and Mobile Chrome projects but add the same dependencies and storageState
5. Keep existing webServer config

Add to `.gitignore`:
```
# Playwright auth state
playwright/.auth/
```

Ensure the directory `playwright/.auth/` can be created (parent dirs exist or will be created by Playwright).
  </action>
  <verify>
Run `npx playwright test --list` - should show setup project and other projects with dependencies.
Check `.gitignore` contains `playwright/.auth/`.
  </verify>
  <done>playwright.config.ts uses project-based setup with storageState, .gitignore updated</done>
</task>

<task type="auto">
  <name>Task 3: Verify auth setup works with test run</name>
  <files>None (verification only)</files>
  <action>
1. Ensure TEST_USER_EMAIL and TEST_USER_PASSWORD are set in .env.local (prompt user if missing)
2. Run `npx playwright test --project=setup` to execute only the auth setup
3. Verify `playwright/.auth/user.json` is created
4. Check the file contains cookies and localStorage data

If auth fails:
- Check if test user exists in database
- Verify credentials are correct
- Check for any redirect issues

Note: The user may need to create a test account first via the registration page.
  </action>
  <verify>
`npx playwright test --project=setup` passes.
`playwright/.auth/user.json` file exists and contains session data.
  </verify>
  <done>Auth setup runs successfully and creates valid auth state file</done>
</task>

</tasks>

<verification>
1. `tests/auth.setup.ts` exists and exports a setup test
2. `playwright.config.ts` has setup project with testMatch for auth.setup.ts
3. All browser projects have `dependencies: ['setup']` and `storageState`
4. `.gitignore` includes `playwright/.auth/`
5. `npx playwright test --project=setup` passes (when test user exists)
</verification>

<success_criteria>
- Auth setup test can log in with test user credentials
- Auth state is saved to `playwright/.auth/user.json`
- Playwright config correctly chains setup -> browser projects
- Auth state files are gitignored
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-crud-verification/03-01-SUMMARY.md`
</output>
