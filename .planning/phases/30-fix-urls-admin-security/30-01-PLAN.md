---
phase: 30-fix-urls-admin-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/auth/config.ts
  - src/types/next-auth.d.ts
  - src/scripts/seed-admin.ts
  - .env.example
  - src/app/(dashboard)/admin/layout.tsx
  - src/app/api/admin/trial-extensions/route.ts
  - src/components/layout/app-sidebar.tsx
autonomous: true
requirements:
  - HOOK-02

must_haves:
  truths:
    - "Admin routes check session.user.role before allowing access"
    - "Non-admin users are silently redirected to /dashboard when accessing /admin/* pages"
    - "Admin API routes return 403 for non-admin authenticated users"
    - "Admin navigation links are completely hidden from non-admin users"
    - "Seed script exists to bootstrap first admin from ADMIN_EMAIL env var"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "userRoleEnum and role column on users table"
      contains: "userRoleEnum"
    - path: "src/lib/auth/config.ts"
      provides: "role field in JWT and session callbacks"
      contains: "token.role"
    - path: "src/types/next-auth.d.ts"
      provides: "TypeScript type declarations for role field"
      contains: "role:"
    - path: "src/scripts/seed-admin.ts"
      provides: "Admin bootstrap seed script"
      contains: "ADMIN_EMAIL"
    - path: "src/app/(dashboard)/admin/layout.tsx"
      provides: "Admin role guard at layout level"
      contains: "session.user.role"
    - path: "src/app/api/admin/trial-extensions/route.ts"
      provides: "Inline admin role check in API route"
      contains: "session.user.role"
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Conditional admin nav rendering"
      contains: "isAdmin"
  key_links:
    - from: "src/lib/db/schema.ts"
      to: "src/lib/auth/config.ts"
      via: "dbUser.role read in JWT callback"
      pattern: "token\\.role = dbUser\\.role"
    - from: "src/lib/auth/config.ts"
      to: "src/app/(dashboard)/admin/layout.tsx"
      via: "session.user.role available from session callback"
      pattern: "session\\.user\\.role"
    - from: "src/lib/auth/config.ts"
      to: "src/components/layout/app-sidebar.tsx"
      via: "useSession provides role to client component"
      pattern: "session\\?\\.user\\?\\.role"
---

<objective>
Add admin role-based access control to protect admin routes and conditionally render admin navigation.

Purpose: Secure admin pages and API routes so only admin users can access them. Currently any authenticated user can access /admin/* pages and POST to /api/admin/* endpoints.

Output: Schema migration with role column, NextAuth session extension, admin layout guard, API route guard, conditional sidebar rendering, and admin bootstrap seed script.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v2.1-ROADMAP.md
@.planning/phases/30-fix-urls-admin-security/30-RESEARCH.md
@src/lib/db/schema.ts
@src/lib/auth/config.ts
@src/types/next-auth.d.ts
@src/app/(dashboard)/admin/layout.tsx
@src/app/api/admin/trial-extensions/route.ts
@src/components/layout/app-sidebar.tsx
@src/scripts/seed-stripe-prices.ts
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add role enum to schema, extend NextAuth session, update TypeScript types, and generate migration</name>
  <files>
    src/lib/db/schema.ts
    src/lib/auth/config.ts
    src/types/next-auth.d.ts
    src/scripts/seed-admin.ts
    .env.example
  </files>
  <action>
    **Schema (src/lib/db/schema.ts):**
    1. Add a new pgEnum BEFORE the users table definition:
       `export const userRoleEnum = pgEnum("user_role", ["user", "admin"]);`
    2. Add role column to users table (after `onboardingCompleted` field):
       `role: userRoleEnum("role").default("user").notNull(),`

    **NextAuth config (src/lib/auth/config.ts):**
    3. In the JWT callback, in the `if (user)` branch where `dbUser` fields are assigned to `token`, add:
       `token.role = dbUser.role;`
    4. In the JWT callback, in the `if (trigger === "update")` branch, add the same:
       `token.role = dbUser.role;`
    5. In the session callback, after existing `user.displayCurrency = token.displayCurrency;`, add:
       `user.role = token.role;`

    **TypeScript types (src/types/next-auth.d.ts):**
    6. In the `Session` interface's `user` object, add: `role: "user" | "admin";`
    7. In the `User` interface, add: `role?: "user" | "admin";`
    8. In the `JWT` interface, add: `role: "user" | "admin";`

    **Seed script (src/scripts/seed-admin.ts):**
    9. Create seed script following the pattern of `src/scripts/seed-stripe-prices.ts`:
       - Import dotenv, configure with `.env.local`
       - Import `db` from `@/lib/db`, `users` from `@/lib/db/schema`, `eq` from `drizzle-orm`
       - Read `ADMIN_EMAIL` from `process.env` — exit with error if not set
       - Run `db.update(users).set({ role: "admin" }).where(eq(users.email, adminEmail))`
       - Log the result and exit

    **Environment (.env.example):**
    10. Add `ADMIN_EMAIL=""` with a comment: `# Admin bootstrap - email of first admin user`

    **Migration:**
    11. Run `npm run db:generate` to generate the Drizzle migration for the new enum and column.
    12. Run `npm run db:migrate` to apply the migration.
  </action>
  <verify>
    - `npm run db:generate` succeeds without errors
    - `npm run db:migrate` succeeds without errors
    - The generated migration SQL contains `CREATE TYPE "public"."user_role"` and `ALTER TABLE "users" ADD COLUMN "role"`
    - TypeScript compilation: `npx tsc --noEmit` shows no new errors related to role types
    - File `src/scripts/seed-admin.ts` exists with ADMIN_EMAIL reading logic
    - `.env.example` contains ADMIN_EMAIL
  </verify>
  <done>
    - userRoleEnum exists in schema.ts with values "user" and "admin"
    - users table has role column with default "user" and notNull
    - JWT callback populates token.role from dbUser.role in both user-login and trigger=update branches
    - Session callback exposes role on session.user
    - TypeScript declarations updated for Session, User, and JWT interfaces
    - Migration generated and applied successfully
    - Seed script created at src/scripts/seed-admin.ts
    - ADMIN_EMAIL added to .env.example
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire admin role checks into layout, API route, and sidebar navigation</name>
  <files>
    src/app/(dashboard)/admin/layout.tsx
    src/app/api/admin/trial-extensions/route.ts
    src/components/layout/app-sidebar.tsx
  </files>
  <action>
    **Admin layout (src/app/(dashboard)/admin/layout.tsx):**
    1. Uncomment and activate the role check that is currently commented out (lines 15-18):
       Replace the comment block with:
       ```
       if (session.user.role !== "admin") {
         redirect("/dashboard");
       }
       ```
       Remove the `// Future: Add admin role check here` comment.

    **Admin API route (src/app/api/admin/trial-extensions/route.ts):**
    2. After the existing auth check (`if (!session?.user?.id)` returning 401), add an inline role check:
       ```
       if (session.user.role !== "admin") {
         return NextResponse.json({ error: "Forbidden" }, { status: 403 });
       }
       ```
    3. Remove the MVP tradeoff comments: `// Note: No admin role check - session auth only (MVP tradeoff)` and `// Admin role gating can be added later if needed`

    **Sidebar (src/components/layout/app-sidebar.tsx):**
    4. The sidebar is a `"use client"` component using `useSession()`. Add a derived variable after getting session data:
       `const isAdmin = session?.user?.role === "admin";`
    5. Find the existing admin nav section. Per research, the admin links (Trial Extensions, Webhooks) are currently visible to all users. Wrap the entire admin `SidebarGroup` in `{isAdmin && (...)}` so it only renders for admin users.
    6. If admin nav items currently exist as individual items mixed with other nav, extract them into a dedicated admin section wrapped with the isAdmin conditional. Import `Shield` and `Webhook` icons from lucide-react if not already imported.
    7. The admin nav items should include:
       - Trial Extensions → `/admin/trial-extensions` with Shield icon
       - Webhooks → `/admin/webhooks` with Webhook icon
    8. Ensure these links are completely hidden (not disabled/grayed) for non-admin users per locked decision.
  </action>
  <verify>
    - `npm run build` succeeds (no TypeScript errors, no build failures)
    - Admin layout file contains `session.user.role !== "admin"` check
    - Admin API route file contains role check returning 403
    - Sidebar file contains `isAdmin` conditional rendering
    - No commented-out role check code remains in admin layout
    - No MVP tradeoff comments remain in admin API route
  </verify>
  <done>
    - Admin layout redirects non-admin users to /dashboard silently
    - Admin API route returns 403 Forbidden for non-admin authenticated users
    - Admin nav section in sidebar only renders when session.user.role === "admin"
    - Admin navigation links are completely hidden from non-admin users
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. TypeScript: `npx tsc --noEmit` passes
3. Admin layout contains active role check (not commented out)
4. Admin API route contains inline role check returning 403
5. Sidebar conditionally renders admin section based on session.user.role
6. Schema has userRoleEnum and role column on users table
7. NextAuth JWT callback includes token.role in both branches
8. NextAuth session callback exposes role
9. TypeScript declarations include role in Session, User, JWT
10. Seed script exists and reads ADMIN_EMAIL
</verification>

<success_criteria>
- Build passes with no errors
- Admin routes are protected by role-based access control
- Admin navigation hidden from non-admin users
- Role field flows: DB schema -> JWT callback -> session callback -> components
- Seed script ready for admin bootstrap
</success_criteria>

<output>
After completion, create `.planning/phases/30-fix-urls-admin-security/30-01-SUMMARY.md`
</output>
