---
phase: 31-storage-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/source.ts
  - src/app/api/sources/[sourceType]/statements/route.ts
  - src/app/api/statements/[id]/route.ts
  - src/components/sources/statement-list.tsx
  - src/components/sources/statement-detail.tsx
autonomous: true
requirements:
  - STOR-02

must_haves:
  truths:
    - "Statement list API returns hasPdf boolean for each statement"
    - "Statement detail API returns hasPdf boolean"
    - "Statements with a stored PDF show a green file icon in the statement row"
    - "Statements without a stored PDF (pre-v2.2 or storage failure) show a muted gray file icon"
    - "The icons are informational only — clicking does nothing in Phase 31"
  artifacts:
    - path: "src/types/source.ts"
      provides: "StatementSummary type with hasPdf field"
      contains: "hasPdf"
    - path: "src/components/sources/statement-list.tsx"
      provides: "PdfStatusIcon component rendering green/gray file icon"
      contains: "PdfStatusIcon"
    - path: "src/components/sources/statement-detail.tsx"
      provides: "PDF status indicator in statement header"
      contains: "PdfStatusIcon"
  key_links:
    - from: "src/app/api/sources/[sourceType]/statements/route.ts"
      to: "src/lib/db/schema.ts"
      via: "pdfStoragePath IS NOT NULL as hasPdf in select"
      pattern: "pdfStoragePath"
    - from: "src/app/api/statements/[id]/route.ts"
      to: "src/lib/db/schema.ts"
      via: "pdfStoragePath IS NOT NULL as hasPdf in select"
      pattern: "pdfStoragePath"
    - from: "src/components/sources/statement-list.tsx"
      to: "src/types/source.ts"
      via: "StatementSummary.hasPdf drives icon state"
      pattern: "hasPdf"
---

<objective>
Surface the PDF storage status across statement APIs and UI so users can see which statements have stored PDFs and which do not.

Purpose: Pre-v2.2 statements and any where storage failed must show a clear "no file stored" indicator rather than a broken link or error. This gives users visibility into their data completeness.

Output: Updated APIs returning `hasPdf` boolean, `PdfStatusIcon` component, green/gray icon toggle on statement rows and detail view.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-storage-foundation/31-RESEARCH.md
@.planning/phases/31-storage-foundation/31-CONTEXT.md
@src/types/source.ts
@src/app/api/sources/[sourceType]/statements/route.ts
@src/app/api/statements/[id]/route.ts
@src/components/sources/statement-list.tsx
@src/components/sources/statement-detail.tsx
@src/lib/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hasPdf to statement APIs and type definitions</name>
  <files>
    src/types/source.ts
    src/app/api/sources/[sourceType]/statements/route.ts
    src/app/api/statements/[id]/route.ts
  </files>
  <action>
1. Update `src/types/source.ts` — add `hasPdf` to `StatementSummary`:
   - Add `hasPdf: boolean;` field to the `StatementSummary` interface, with JSDoc: `/** Whether the original PDF is stored in Supabase Storage */`

2. Modify `src/app/api/sources/[sourceType]/statements/route.ts`:
   - Add `pdfStoragePath: statements.pdfStoragePath` to the `db.select()` call (alongside id, originalFilename, etc.)
   - In the response mapping (the `statementList.map()` call), add `hasPdf: stmt.pdfStoragePath !== null` to each result object
   - Do NOT select pdfStoragePath into the response itself — only derive the boolean

3. Modify `src/app/api/statements/[id]/route.ts`:
   - Add `pdfStoragePath: statements.pdfStoragePath` to the `db.select()` call
   - In the response JSON, add `hasPdf: statement.pdfStoragePath !== null` to the statement object
   - Do NOT include the raw pdfStoragePath in the response — only the boolean
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no new type errors
    - `StatementSummary` type has `hasPdf: boolean`
    - Both API routes include `hasPdf` in their JSON responses
  </verify>
  <done>
    Statement list and detail APIs return `hasPdf: boolean` derived from `pdfStoragePath IS NOT NULL`. The `StatementSummary` type includes the new field. Pre-v2.2 statements with null `pdfStoragePath` correctly return `hasPdf: false`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add PDF status icon to statement list and detail views</name>
  <files>
    src/components/sources/statement-list.tsx
    src/components/sources/statement-detail.tsx
  </files>
  <action>
1. Create a `PdfStatusIcon` component inside `src/components/sources/statement-list.tsx` (local to the file, not exported — reused via copy in detail):
   ```tsx
   import { FileText } from 'lucide-react';
   import { cn } from '@/lib/utils';

   function PdfStatusIcon({ hasPdf }: { hasPdf: boolean }) {
     return (
       <FileText
         className={cn(
           'h-4 w-4 shrink-0',
           hasPdf ? 'text-green-500' : 'text-muted-foreground/40'
         )}
         aria-label={hasPdf ? 'PDF stored' : 'No file stored'}
       />
     );
   }
   ```
   - `FileText` is already imported in statement-list.tsx. Reuse the import.
   - Green (`text-green-500`) for stored, muted gray (`text-muted-foreground/40`) for not stored — per locked decision.
   - The icon is the same shape in both states (just different color) — per locked decision.

2. Modify `StatementRow` in `statement-list.tsx`:
   - The existing `FileText` icon on line 89 should be replaced with `<PdfStatusIcon hasPdf={statement.hasPdf} />`
   - Adjust positioning: keep `mt-0.5 shrink-0` on the icon wrapper (PdfStatusIcon already has shrink-0, add mt-0.5 via a wrapper div or modify the component)
   - The `hasPdf` prop comes from the API response via `StatementSummary` type (which was updated in Task 1)

3. Modify `StatementDetail` in `src/components/sources/statement-detail.tsx`:
   - Add the same `PdfStatusIcon` component locally in this file (or extract to a shared location — Claude's discretion: keep it local for simplicity, two copies is fine for a 6-line component)
   - In the statement header section, next to the statement filename (around line 166), add the PDF status icon:
     - After the `<h2>` with the filename, add `<PdfStatusIcon hasPdf={statement.hasPdf ?? false} />` inline
     - Use `?? false` as a safety fallback since `hasPdf` might not exist on older cached responses
   - Import `cn` from `@/lib/utils` if not already imported
   - Note: `FileText` is already imported in this file. Just add the local `PdfStatusIcon` function.

4. Per locked decision: Clicking the icon does nothing in Phase 31. No `onClick` handler. No cursor change. Phase 32 adds the viewer on click.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no new type errors
    - `statement-list.tsx` renders `PdfStatusIcon` per statement row
    - `statement-detail.tsx` renders `PdfStatusIcon` in the header
    - Icons have `aria-label` for accessibility
    - No onClick handler on the icons (Phase 31 = informational only)
  </verify>
  <done>
    Statement rows show a green `FileText` icon when the PDF is stored and a muted gray `FileText` icon when no file is stored. The statement detail header includes the same indicator. Both states are clearly visually distinct. Icons are informational only — no interactivity.
  </done>
</task>

</tasks>

<verification>
1. Type check passes: `npx tsc --noEmit` has no new errors
2. `StatementSummary` type includes `hasPdf: boolean`
3. GET `/api/sources/[sourceType]/statements` response includes `hasPdf` per statement
4. GET `/api/statements/[id]` response includes `hasPdf` on the statement object
5. Statement rows display green icon for stored PDFs, gray icon for missing
6. Statement detail displays the same PDF status icon in the header
7. Pre-v2.2 statements (null `pdfStoragePath`) show gray "No file stored" icon
</verification>

<success_criteria>
- `hasPdf` boolean derived from `pdfStoragePath IS NOT NULL` in both statement APIs
- `StatementSummary` type updated with `hasPdf: boolean` field
- Green `FileText` icon (text-green-500) for statements with stored PDFs
- Muted gray `FileText` icon (text-muted-foreground/40) for statements without stored PDFs
- Icons have `aria-label` for screen readers
- No click handlers on icons (Phase 31 is informational only)
- Pre-v2.2 statements correctly show "No file stored" state
</success_criteria>

<output>
After completion, create `.planning/phases/31-storage-foundation/31-02-SUMMARY.md`
</output>
