---
phase: 40-static-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/app-sidebar.tsx
  - src/app/(dashboard)/support/schema/page.tsx
  - src/app/(dashboard)/support/help/page.tsx
autonomous: true
requirements:
  - SCHEMA-01
  - HELP-01

must_haves:
  truths:
    - "Data Schema page at /support/schema renders all 21 database tables as static cards with column name + type per row"
    - "FK columns show arrow notation (e.g. uuid -> users) inline — no separate relationships section"
    - "Help page at /support/help renders FAQ questions grouped under category headings with Accordion type=multiple"
    - "All accordion sections collapsed by default on page load — no defaultValue prop"
    - "Contact us mailto link appears at the bottom of the Help page"
    - "Both pages appear in sidebar Support section and highlight correctly when active"
    - "Schema data is hardcoded static — no live DB queries"
  artifacts:
    - path: "src/app/(dashboard)/support/schema/page.tsx"
      provides: "Data Schema page with hardcoded table cards in 2-column grid"
      exports: ["DataSchemaPage", "metadata"]
    - path: "src/app/(dashboard)/support/help/page.tsx"
      provides: "Help/FAQ page with multi-open accordion sections and contact link"
      exports: ["HelpPage", "metadata"]
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Updated supportItems array with Help and Data Schema nav entries"
      exports: ["AppSidebar"]
  key_links:
    - from: "src/components/layout/app-sidebar.tsx"
      to: "/support/help, /support/schema"
      via: "supportItems array href values route to both pages"
      pattern: "supportItems.*href.*support"
---

<objective>
Create two static content pages and add sidebar navigation entries: a Data Schema page showing all 21 database tables as card-based column listings, and a Help page with accordion-organized FAQ sections grouped by category.

Purpose: Completes the final two v3.0 requirements (SCHEMA-01, HELP-01). Both pages are purely static server components with hardcoded content — no API routes, no DB queries, no client-side state management.

Output: Two new page files under src/app/(dashboard)/support/, updated supportItems in app-sidebar.tsx with Help and Data Schema nav entries.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40-static-pages/40-CONTEXT.md
@.planning/phases/40-static-pages/40-RESEARCH.md

<interfaces>
From src/components/layout/app-sidebar.tsx:
```typescript
// supportItems array at lines 73-76:
const supportItems = [
  { title: "Settings", href: "/settings", icon: Settings },
  // Help and Schema added by Phase 40
];
// Lucide imports at lines 6-26 — Database already imported, HelpCircle NOT yet imported
// isNavItemActive at line 78 uses exact match by default — correct for /support/help and /support/schema
```

From src/components/ui/accordion.tsx:
```typescript
// Already has "use client" directive — page can remain server component
export { Accordion, AccordionItem, AccordionTrigger, AccordionContent }
// type="multiple" enables multi-open; omitting defaultValue = all collapsed
```

From src/components/ui/card.tsx:
```typescript
export { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter }
```

From src/components/layout/index.ts:
```typescript
export { DashboardHeader } from "./dashboard-header";
```
</interfaces>

<locked_decisions>
## Schema Presentation
- Table cards layout — each database table rendered as a card with columns listed vertically
- Essential detail only — column name and type per row, no nullable/default/constraint details
- FK columns show -> tableName inline as text labels (no diagram or separate relationships section)
- Hardcoded static data — manually written, not auto-generated from Drizzle schema
- All table cards expanded on page load (no collapsible behavior on schema page)

## Help Content & Structure
- Casual & friendly tone — conversational, uses "you", approachable
- Both feature walkthroughs AND troubleshooting sections (comprehensive coverage)
- Brief answers — 2-3 sentences per FAQ answer, quick to scan
- Questions grouped under category headers (e.g., Getting Started, Importing, Subscriptions, Billing, Troubleshooting)
- Simple mailto "Contact us" email link at the bottom of the page

## Accordion Behavior
- Multi-open — multiple sections can be expanded simultaneously
- All sections collapsed by default on page load
- Questions grouped under category headings with accordions nested within each group

## Page Styling & Layout
- Monochrome type display — no color coding for column types
- Title + 1-line description header on both pages
- Minimal documentation style — clean, text-focused, prioritize readability over decoration
</locked_decisions>

<claudes_discretion>
- Schema card layout: 2-column responsive grid (md:grid-cols-2) for 21 tables
- Exact spacing, typography, and card styling within existing design system
- Which FAQ questions to include and how to group them into categories
- Contact email address: use support@subscriptions.app as placeholder
</claudes_discretion>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Help and Data Schema nav entries to sidebar supportItems</name>
  <files>
    src/components/layout/app-sidebar.tsx
  </files>
  <action>
Update the sidebar to add two new navigation entries for the static pages.

**1. Add `HelpCircle` to the Lucide import** (lines 6-26):

Find the existing import block from `"lucide-react"` and add `HelpCircle` to the destructured imports (alphabetically, after `FolderUp`). `Database` is already imported at line 25 — do not duplicate it.

```typescript
import {
  // ... existing imports ...
  HelpCircle,
  // ... rest of imports ...
} from "lucide-react";
```

**2. Update `supportItems` array** (lines 73-76):

Replace the current array (which has the Phase 40 placeholder comment):
```typescript
const supportItems = [
  { title: "Settings", href: "/settings", icon: Settings },
  // Help and Schema added by Phase 40
];
```

With:
```typescript
const supportItems = [
  { title: "Settings", href: "/settings", icon: Settings },
  { title: "Help", href: "/support/help", icon: HelpCircle },
  { title: "Data Schema", href: "/support/schema", icon: Database },
];
```

**3. No changes needed to `isNavItemActive`:** The function at line 78 uses exact match by default. `/support/help` and `/support/schema` have no children, so exact match is correct.
  </action>
  <verify>
Run `npx tsc --noEmit` — zero errors. Verify `HelpCircle` is imported from `lucide-react`. Verify `supportItems` array has exactly 3 entries: Settings, Help, Data Schema. Verify the hrefs are `/settings`, `/support/help`, `/support/schema`.
  </verify>
  <done>
Sidebar supportItems updated with Help (HelpCircle icon, /support/help) and Data Schema (Database icon, /support/schema) nav entries. HelpCircle imported from lucide-react. isNavItemActive unchanged — exact match works for both new routes. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Data Schema page with hardcoded table cards</name>
  <files>
    src/app/(dashboard)/support/schema/page.tsx
  </files>
  <action>
Create a new server component page that renders all 21 database tables as static cards.

**1. Create the file** at `src/app/(dashboard)/support/schema/page.tsx` (create the `support/schema/` directories).

**2. Page imports and metadata** — server component, no `"use client"`:

```typescript
import { Metadata } from "next";
import { DashboardHeader } from "@/components/layout";
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export const metadata: Metadata = {
  title: "Data Schema",
  description: "System data model reference",
};
```

**3. Define `SCHEMA_TABLES` as a const array** covering all 21 tables from `src/lib/db/schema.ts`:

| # | Table Name | Description |
|---|-----------|-------------|
| 1 | users | User accounts with preferences and billing status |
| 2 | accounts | NextAuth OAuth provider accounts |
| 3 | sessions | NextAuth active sessions |
| 4 | verification_tokens | NextAuth email verification tokens |
| 5 | authenticators | NextAuth WebAuthn credentials |
| 6 | password_reset_tokens | Password reset flow tokens |
| 7 | categories | Subscription categories (predefined and custom) |
| 8 | subscriptions | User subscription records with renewal tracking |
| 9 | reminder_logs | Email reminder delivery tracking |
| 10 | import_audits | PDF import history and extraction data |
| 11 | fx_rates_cache | Exchange rate caching |
| 12 | recurring_patterns | AI-detected recurring charge patterns |
| 13 | alerts | Price increase and missed renewal alerts |
| 14 | financial_accounts | User financial accounts (bank, credit card, loan) |
| 15 | statements | Uploaded PDF statement metadata |
| 16 | transactions | Extracted transactions from statements |
| 17 | tags | User-defined transaction tags |
| 18 | transaction_tags | Junction table linking transactions to tags |
| 19 | webhook_events | Stripe webhook idempotency log |
| 20 | stripe_prices | Stripe price catalog |
| 21 | trial_extensions | Admin-granted trial extensions |

**4. Column listing rules** — reference `src/lib/db/schema.ts` for accurate column names/types:
- Column name: camelCase property name from Drizzle (e.g., `userId`, `createdAt`)
- Type: simple SQL-like strings (e.g., `uuid`, `varchar(255)`, `timestamp`, `boolean`, `decimal(10,2)`, `integer`, `text`, `jsonb`)
- FK columns: append ` -> tableName` after the type (e.g., `uuid -> users`)
- Enum columns: show as `enum_name enum` (e.g., `billing_status enum`, `frequency enum`)
- Do NOT include nullable/default/constraint info — essential detail only per locked decision

**5. Page layout:**

```tsx
export default function DataSchemaPage() {
  return (
    <>
      <DashboardHeader title="Data Schema" />
      <main className="flex-1 p-4 md:p-6">
        <div className="mx-auto max-w-6xl space-y-6">
          <div>
            <h2 className="text-2xl font-bold tracking-tight">Data Schema</h2>
            <p className="text-muted-foreground">
              A read-only reference for the system&apos;s data model
            </p>
          </div>
          <div className="grid gap-4 md:grid-cols-2">
            {SCHEMA_TABLES.map((table) => (
              <Card key={table.name}>
                <CardHeader>
                  <CardTitle className="font-mono text-base">{table.name}</CardTitle>
                  <CardDescription>{table.description}</CardDescription>
                </CardHeader>
                <CardContent>
                  <div className="space-y-1.5 text-sm">
                    {table.columns.map((col) => (
                      <div key={col.name} className="flex gap-3">
                        <span className="text-muted-foreground font-mono w-48 shrink-0 truncate">
                          {col.name}
                        </span>
                        <span className="font-mono text-muted-foreground/70">
                          {col.type}
                        </span>
                      </div>
                    ))}
                  </div>
                </CardContent>
              </Card>
            ))}
          </div>
        </div>
      </main>
    </>
  );
}
```

**6. Use `max-w-6xl`** (not `max-w-5xl`) for the 2-column grid to give cards breathing room.

**Implementation notes:**
- Schema data is a hardcoded snapshot — does NOT auto-update from Drizzle (locked decision)
- Do not include indexes, unique constraints, or composite primary keys — only column-level data
- Group auth-related tables (accounts, sessions, verification_tokens, authenticators) near each other for logical visual grouping
- Use monospace for table names and column names; muted foreground for column types
  </action>
  <verify>
Run `npx tsc --noEmit` — zero errors. Verify file exists at `src/app/(dashboard)/support/schema/page.tsx`. Count table cards — must be exactly 21. Verify FK columns show `-> tableName` notation. Verify no database import or query in the file (grep for "from.*db" — should find none). Run `npm run build` — succeeds.
  </verify>
  <done>
Data Schema page created at src/app/(dashboard)/support/schema/page.tsx as a server component. All 21 tables rendered as cards in a 2-column responsive grid with column name + type rows. FK columns display -> tableName notation. Enum columns display as enum_name enum. All data is hardcoded static — zero DB queries. Monochrome monospace typography throughout. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Help page with accordion FAQ sections</name>
  <files>
    src/app/(dashboard)/support/help/page.tsx
  </files>
  <action>
Create a new server component page that renders FAQ sections with multi-open accordion behavior.

**1. Create the file** at `src/app/(dashboard)/support/help/page.tsx` (create the `support/help/` directories).

**2. Page imports and metadata** — server component, no `"use client"` at page level:

```typescript
import { Metadata } from "next";
import { DashboardHeader } from "@/components/layout";
import {
  Accordion,
  AccordionItem,
  AccordionTrigger,
  AccordionContent,
} from "@/components/ui/accordion";

export const metadata: Metadata = {
  title: "Help",
  description: "Frequently asked questions and documentation",
};
```

**3. Define FAQ data** as a const array. Use these category groups (Claude's discretion on exact questions and wording):

**Getting Started** (~3-4 questions):
- What is Subscription Manager?
- How do I add my first subscription?
- What's included in the free trial?
- How do I connect my bank statements?

**Subscriptions** (~3-4 questions):
- How do I add a subscription manually?
- What do the subscription statuses mean?
- Can I track subscriptions in different currencies?
- How do I delete a subscription?

**Importing Statements** (~3-4 questions):
- How does PDF import work?
- Which banks are supported?
- What happens after I upload a statement?
- Why did my import fail?

**Reminders** (~2-3 questions):
- How do renewal reminders work?
- Can I customize reminder timing?
- How do I turn off reminders for a specific subscription?

**Billing & Plans** (~2-3 questions):
- What features are included in each plan?
- How do I upgrade or change my plan?
- How do I cancel my subscription?

**Troubleshooting** (~3-4 questions):
- My subscriptions aren't showing up
- I'm not receiving reminder emails
- The PDF import didn't detect all my transactions
- How do I reset my password?

Each answer: 2-3 sentences, casual and friendly ("you" language, approachable, like a helpful friend — not a technical manual).

**4. Data structure:**

```typescript
const FAQ_CATEGORIES = [
  {
    id: "getting-started",
    title: "Getting Started",
    questions: [
      {
        id: "getting-started-1",
        question: "What is Subscription Manager?",
        answer: "Subscription Manager helps you keep track of all your recurring payments..."
      },
      // ... more questions
    ],
  },
  // ... more categories
];
```

**5. Page layout:**

```tsx
export default function HelpPage() {
  return (
    <>
      <DashboardHeader title="Help" />
      <main className="flex-1 p-4 md:p-6">
        <div className="mx-auto max-w-3xl space-y-6">
          <div>
            <h2 className="text-2xl font-bold tracking-tight">Help</h2>
            <p className="text-muted-foreground">
              Frequently asked questions and how things work
            </p>
          </div>
          <div className="space-y-8">
            {FAQ_CATEGORIES.map((category) => (
              <div key={category.id}>
                <h3 className="text-lg font-semibold mb-3">{category.title}</h3>
                <Accordion type="multiple" className="border rounded-lg px-4">
                  {category.questions.map((item) => (
                    <AccordionItem key={item.id} value={item.id}>
                      <AccordionTrigger>{item.question}</AccordionTrigger>
                      <AccordionContent>
                        <p className="text-muted-foreground">{item.answer}</p>
                      </AccordionContent>
                    </AccordionItem>
                  ))}
                </Accordion>
              </div>
            ))}
          </div>
          <div className="mt-12 pt-8 border-t text-center">
            <p className="text-muted-foreground text-sm">
              Didn&apos;t find what you&apos;re looking for?{" "}
              <a
                href="mailto:support@subscriptions.app"
                className="text-primary hover:underline font-medium"
              >
                Contact us
              </a>
            </p>
          </div>
        </div>
      </main>
    </>
  );
}
```

**6. Use `max-w-3xl`** for the help page — narrow column reads best for FAQ text content.

**7. CRITICAL — Accordion props:**
- `type="multiple"` on every `<Accordion>` — enables independent expand/collapse per item (locked decision)
- Do NOT add `defaultValue` prop — omitting it means all items start collapsed (locked decision)

**8. Each category gets its own `<Accordion>` wrapper** — creates visual grouping (heading + accordion block) per locked decision "Questions grouped under category headings with accordions nested within each group".

**9. Use unique IDs** for AccordionItem values (e.g., `"getting-started-1"`, `"importing-2"`) — must be unique within each Accordion instance.
  </action>
  <verify>
Run `npx tsc --noEmit` — zero errors. Verify file exists at `src/app/(dashboard)/support/help/page.tsx`. Check that every `<Accordion>` has `type="multiple"`. Check that no `defaultValue` prop is present on any Accordion. Verify the mailto link exists with `href="mailto:support@subscriptions.app"`. Verify at least 6 category sections with 2+ questions each. Run `npm run build` — succeeds.
  </verify>
  <done>
Help page created at src/app/(dashboard)/support/help/page.tsx as a server component. FAQ content organized into 6 category sections (Getting Started, Subscriptions, Importing, Reminders, Billing, Troubleshooting) with multi-open Accordion (type="multiple", no defaultValue). Answers written in casual friendly tone, 2-3 sentences each. Contact us mailto link at bottom. max-w-3xl narrow column for readability. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no TypeScript errors
2. `/support/schema` renders all 21 database table cards with column name + type rows in a 2-column grid
3. `/support/help` renders FAQ sections with multi-open accordion behavior (multiple items open simultaneously)
4. Both pages appear in the sidebar Support section and highlight correctly when active
5. HelpCircle icon renders for the Help nav entry, Database icon for Data Schema
6. Schema page has no live DB queries — all data is hardcoded static content
7. Help page accordion items all start collapsed, multiple can be open simultaneously
8. Contact us mailto link at bottom of Help page is clickable
</verification>
