---
phase: 10-error-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils/errors.ts
  - src/lib/validations/subscription.ts
  - src/lib/validations/auth.ts
  - src/lib/validations/user.ts
  - src/lib/validations/import.ts
  - src/components/subscriptions/subscription-form.tsx
autonomous: true

must_haves:
  truths:
    - "Form fields show 'This field is required' for empty required fields"
    - "Validation errors appear below fields on blur"
    - "Errors clear immediately when user corrects input"
    - "Error utility transforms technical errors to user-friendly messages"
  artifacts:
    - path: "src/lib/utils/errors.ts"
      provides: "Error message transformation and retry detection"
      exports: ["getErrorMessage", "isRetryableError"]
    - path: "src/lib/validations/subscription.ts"
      provides: "Subscription schema with custom error messages"
      contains: "required_error"
    - path: "src/components/subscriptions/subscription-form.tsx"
      provides: "Form with onBlur validation mode"
      contains: "mode: \"onBlur\""
  key_links:
    - from: "src/lib/utils/errors.ts"
      to: "mutation hooks"
      via: "import and function call"
      pattern: "getErrorMessage\\(error\\)"
    - from: "src/lib/validations/subscription.ts"
      to: "src/components/subscriptions/subscription-form.tsx"
      via: "zodResolver"
      pattern: "zodResolver\\(createSubscriptionFormSchema\\)"
---

<objective>
Create error handling foundation with user-friendly error utilities and improved form validation.

Purpose: Establish the core infrastructure for transforming technical errors into actionable user messages and ensuring forms validate with clear feedback on blur.

Output: Error utilities module, enhanced Zod schemas with custom messages, and form configured with proper validation modes.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-error-handling/10-CONTEXT.md
@.planning/phases/10-error-handling/10-RESEARCH.md

# Existing files to modify
@src/lib/validations/subscription.ts
@src/lib/validations/auth.ts
@src/components/subscriptions/subscription-form.tsx
</context>

<scope_note>
Form validation scope: The subscription-form.tsx is the primary form in the application (add/edit subscriptions). Login and register forms use NextAuth's built-in handling. Other forms (user settings) are minimal. This phase focuses on subscription-form as the primary target for validation mode configuration. Other forms can be enhanced in future phases if needed.
</scope_note>

<tasks>

<task type="auto">
  <name>Task 1: Create error utilities module</name>
  <files>src/lib/utils/errors.ts</files>
  <action>
Create a new error utilities module with two exported functions:

1. `getErrorMessage(error: unknown): string`
   - Transform technical errors to user-friendly messages
   - Handle specific patterns:
     - "Unauthorized" or 401 -> "Your session has expired. Please sign in again."
     - "Invalid category" -> "The selected category is not valid. Please choose another."
     - "trial has expired" -> "Your trial has expired. Please upgrade to continue."
     - "Failed to fetch" or "Network" or TypeError -> "Unable to connect. Please check your internet connection and try again."
     - "too large" -> "File too large. Please use a smaller file."
     - "Invalid PDF" -> "Invalid PDF format. Please upload a valid PDF file."
     - "No transactions" -> "No transactions found. Try a different file or add subscriptions manually."
     - "OpenAI" or "AI service" -> "AI service temporarily unavailable. Please try again in a moment."
     - 503 status -> "Service temporarily unavailable. Please try again in a moment."
   - Generic fallback: "Something went wrong. Please try again."

2. `isRetryableError(error: unknown): boolean`
   - Return true for transient failures that should trigger automatic retry:
     - Network errors (message includes "Failed to fetch" or "Network" or TypeError)
     - 503 Service Unavailable
     - 408 Request Timeout
   - Return false for all other errors (validation, auth, etc.)

Include proper TypeScript types. The error parameter can be Error, Response, or unknown.
  </action>
  <verify>
Run `npx tsc --noEmit` to verify no TypeScript errors.
Create a quick test by importing the functions and logging sample outputs.
  </verify>
  <done>
getErrorMessage transforms various error types to friendly messages.
isRetryableError correctly identifies network errors and 503 responses.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance Zod schemas with custom error messages</name>
  <files>
    src/lib/validations/subscription.ts
    src/lib/validations/auth.ts
    src/lib/validations/user.ts
    src/lib/validations/import.ts
  </files>
  <action>
Update Zod schemas to provide user-friendly validation messages. Focus on required fields showing "This field is required" pattern.

For subscription.ts - update createSubscriptionFormSchema:
- name: Add `required_error: "This field is required"` and keep min(1) message
- amount: Add `required_error: "This field is required"` and `invalid_type_error: "Please enter a valid amount"`
- nextRenewalDate: Update to `required_error: "Please select a renewal date"`
- currency: Add `required_error: "Please select a currency"`
- frequency: Add `required_error: "Please select a frequency"`

For auth.ts - review existing messages and ensure consistency:
- password in loginSchema: Change to "This field is required"
- Ensure register schema messages are clear

For user.ts and import.ts - add required_error messages to any required fields.

Pattern: Use `z.string({ required_error: "..." })` for strings, `z.number({ required_error: "...", invalid_type_error: "..." })` for numbers.

Do NOT change the schema structure, only add/update error messages.
  </action>
  <verify>
Run `npx tsc --noEmit` to verify no TypeScript errors.
Spot-check that schemas still export correctly.
  </verify>
  <done>
All required fields have "This field is required" or appropriate specific messages.
Invalid type errors have user-friendly messages.
  </done>
</task>

<task type="auto">
  <name>Task 3: Configure form validation modes</name>
  <files>src/components/subscriptions/subscription-form.tsx</files>
  <action>
Update the SubscriptionForm to use proper validation modes per CONTEXT.md decisions:

1. Add validation mode configuration to useForm:
   ```typescript
   const form = useForm<CreateSubscriptionInput>({
     resolver: zodResolver(createSubscriptionFormSchema),
     mode: "onBlur",           // Validate when field loses focus
     reValidateMode: "onChange", // Clear errors immediately when typing
     defaultValues: { ... }
   });
   ```

2. The form already uses FormMessage components which will display the Zod error messages.

3. No other changes needed - the existing FormField/FormMessage structure handles inline display.

This ensures:
- Errors appear when user leaves a field (onBlur)
- Errors clear as soon as user fixes the issue (onChange revalidation)
  </action>
  <verify>
Run `npx tsc --noEmit` and `npm run build` to verify no errors.
Verify subscription-form.tsx contains `mode: "onBlur"` and `reValidateMode: "onChange"` in useForm config.
  </verify>
  <done>
Form validates on blur and clears errors on change.
useForm config includes mode: "onBlur" and reValidateMode: "onChange".
  </done>
</task>

</tasks>

<verification>
1. Error utilities compile without TypeScript errors
2. Zod schemas maintain their structure with enhanced messages
3. SubscriptionForm validates on blur with immediate error clearing
4. `npm run build` succeeds
5. Grep confirms: `grep -E "mode.*onBlur|reValidateMode.*onChange" src/components/subscriptions/subscription-form.tsx`
</verification>

<success_criteria>
- src/lib/utils/errors.ts exists with getErrorMessage and isRetryableError exports
- All Zod schemas have user-friendly required_error messages
- SubscriptionForm uses mode="onBlur" and reValidateMode="onChange"
- Required fields show "This field is required" message below field
- ERR-02 (inline validation) and ERR-03 (required field prevention) are addressed
</success_criteria>

<output>
After completion, create `.planning/phases/10-error-handling/10-01-SUMMARY.md`
</output>
