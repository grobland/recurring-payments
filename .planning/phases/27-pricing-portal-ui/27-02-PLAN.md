---
phase: 27-pricing-portal-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/settings/billing/page.tsx
autonomous: true

must_haves:
  truths:
    - "Current tier is visually highlighted when user is subscribed"
    - "Current Plan badge appears on the user's active tier card"
    - "Subscribe button is disabled for current tier"
    - "Tier highlighting uses primary color styling (border, background)"
  artifacts:
    - path: "src/app/(dashboard)/settings/billing/page.tsx"
      provides: "Billing page with current tier highlighting"
      contains: "Current Plan"
  key_links:
    - from: "src/app/(dashboard)/settings/billing/page.tsx"
      to: "getUserTier"
      via: "server action call in useEffect"
      pattern: "getUserTier"
---

<objective>
Add current tier highlighting to billing settings page

Purpose: Paid users need clear visual indication of their current tier when viewing plan options
Output: Billing page with highlighted current tier card, "Current Plan" badge, and disabled subscribe button
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v2.1-ROADMAP.md
@.planning/phases/27-pricing-portal-ui/27-RESEARCH.md

# Existing file to modify
@src/app/(dashboard)/settings/billing/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add current tier highlighting to billing page</name>
  <files>src/app/(dashboard)/settings/billing/page.tsx</files>
  <action>
Modify the billing page to highlight the user's current tier in the plan selection cards.

Note: The billing page already fetches userTier via getUserTier() in the useEffect around line 68-84. This task only modifies the tier card rendering.

1. Locate the tier cards rendering section (around line 314-386, inside the map over tiers).

2. Add isCurrentTier check after the existing isSelected and isRecommended variables:
   ```typescript
   const isCurrentTier = isPaid && userTier === tier;
   ```

3. Update the card div's className to include current tier highlighting (update around line 324):
   ```typescript
   className={cn(
     "relative rounded-lg border p-6 transition-all cursor-pointer",
     isSelected && "border-primary shadow-lg",
     isCurrentTier && "border-2 border-primary bg-primary/5",
     isRecommended && !isCurrentTier && "border-2 border-primary",
     !isSelected && !isCurrentTier && !isRecommended && "border-gray-200 hover:border-gray-300"
   )}
   ```
   Note: Import cn from "@/lib/utils" if not already imported (it is not currently imported).

4. Update the badge rendering to show "Current Plan" for current tier (replace lines 333-337):
   ```tsx
   {isCurrentTier ? (
     <Badge className="absolute -top-3 left-1/2 -translate-x-1/2 bg-primary">
       Current Plan
     </Badge>
   ) : isRecommended ? (
     <Badge className="absolute -top-3 left-1/2 -translate-x-1/2">
       Recommended
     </Badge>
   ) : null}
   ```

5. Update the Subscribe button to show "Current Plan" text and be disabled when current tier (update around line 365-382):
   ```tsx
   <Button
     className="w-full"
     variant={isSelected && !isCurrentTier ? "default" : "outline"}
     onClick={(e) => {
       e.stopPropagation();
       handleCheckout(tier);
     }}
     disabled={isCheckoutLoading !== null || isCurrentTier}
   >
     {isCurrentTier ? (
       "Current Plan"
     ) : isCheckoutLoading === tier ? (
       <>
         <Loader2 className="mr-2 h-4 w-4 animate-spin" />
         Loading...
       </>
     ) : (
       "Subscribe"
     )}
   </Button>
   ```

6. Add import for cn utility at the top of the file:
   ```typescript
   import { cn } from "@/lib/utils";
   ```
  </action>
  <verify>
Run dev server: npm run dev
Log in as a paid user (or modify test user to have billingStatus: "active" and a userTier)
Navigate to /settings/billing
Verify: Current tier card has primary border and light primary background
Verify: "Current Plan" badge appears above current tier card
Verify: Subscribe button shows "Current Plan" text and is disabled for current tier
Verify: Other tier cards still show "Subscribe" button and can be clicked
  </verify>
  <done>
Billing page visually highlights user's current tier with badge, border styling, and disabled button
  </done>
</task>

</tasks>

<verification>
1. npm run lint passes
2. For paid user: /settings/billing shows "Current Plan" badge on their tier
3. For paid user: Current tier card has border-2 border-primary bg-primary/5 styling
4. For paid user: Subscribe button on current tier shows "Current Plan" and is disabled
5. For trial/free user: No tier is highlighted (userTier is null), all cards show "Subscribe"
6. Click on non-current tier still works (handleCheckout is called)
</verification>

<success_criteria>
- Paid users see their current tier highlighted with visual distinction
- "Current Plan" badge replaces "Recommended" badge for current tier
- Subscribe button is disabled and shows "Current Plan" text for current tier
- Non-current tiers remain fully interactive
- Trial/free users see no tier highlighted (correct behavior)
</success_criteria>

<output>
After completion, create `.planning/phases/27-pricing-portal-ui/27-02-SUMMARY.md`
</output>
