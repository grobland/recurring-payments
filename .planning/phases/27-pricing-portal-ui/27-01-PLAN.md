---
phase: 27-pricing-portal-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/pricing/feature-comparison-table.tsx
  - src/app/(marketing)/pricing/page.tsx
autonomous: true

must_haves:
  truths:
    - "Pricing page shows feature comparison table with all three tiers"
    - "Features are organized by tier level (Primary, Enhanced, Advanced)"
    - "Check/X icons indicate feature availability per tier"
    - "Table is responsive with horizontal scroll on mobile"
  artifacts:
    - path: "src/components/pricing/feature-comparison-table.tsx"
      provides: "Feature comparison table component"
      exports: ["FeatureComparisonTable"]
    - path: "src/app/(marketing)/pricing/page.tsx"
      provides: "Updated pricing page with feature comparison"
      contains: "FeatureComparisonTable"
  key_links:
    - from: "src/app/(marketing)/pricing/page.tsx"
      to: "src/components/pricing/feature-comparison-table.tsx"
      via: "import and render"
      pattern: "import.*FeatureComparisonTable"
    - from: "src/components/pricing/feature-comparison-table.tsx"
      to: "src/lib/stripe/products.ts"
      via: "TIER_CONFIG import"
      pattern: "import.*TIER_CONFIG"
---

<objective>
Create feature comparison table component and integrate it into the pricing page

Purpose: Users need to see detailed feature comparison across tiers to make informed subscription decisions
Output: FeatureComparisonTable component rendered below pricing cards on /pricing page
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v2.1-ROADMAP.md
@.planning/phases/27-pricing-portal-ui/27-RESEARCH.md

# Existing files to reference
@src/lib/stripe/products.ts
@src/lib/features/config.ts
@src/components/ui/table.tsx
@src/app/(marketing)/pricing/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FeatureComparisonTable component</name>
  <files>src/components/pricing/feature-comparison-table.tsx</files>
  <action>
Create a new client component that renders a feature comparison table:

1. Import dependencies:
   - Table components from @/components/ui/table
   - Check, X icons from lucide-react
   - TIER_CONFIG from @/lib/stripe/products
   - TIER_LEVELS from @/lib/features/config
   - cn from @/lib/utils

2. Define FEATURE_MATRIX as a static array of features with their tier availability:
   ```typescript
   interface FeatureRow {
     name: string;
     primary: boolean;
     enhanced: boolean;
     advanced: boolean;
   }

   const FEATURE_MATRIX: FeatureRow[] = [
     // Primary tier features
     { name: "Unlimited subscription tracking", primary: true, enhanced: true, advanced: true },
     { name: "PDF statement imports", primary: true, enhanced: true, advanced: true },
     { name: "Spending analytics dashboard", primary: true, enhanced: true, advanced: true },
     { name: "Email renewal reminders", primary: true, enhanced: true, advanced: true },
     { name: "Category organization", primary: true, enhanced: true, advanced: true },
     // Enhanced tier features
     { name: "Spending monitoring", primary: false, enhanced: true, advanced: true },
     { name: "Budget management", primary: false, enhanced: true, advanced: true },
     { name: "Debt tracking", primary: false, enhanced: true, advanced: true },
     { name: "Transaction categorization", primary: false, enhanced: true, advanced: true },
     // Advanced tier features
     { name: "Investment tracking", primary: false, enhanced: false, advanced: true },
     { name: "Net worth dashboard", primary: false, enhanced: false, advanced: true },
     { name: "Multi-account aggregation", primary: false, enhanced: false, advanced: true },
     { name: "Financial goal planning", primary: false, enhanced: false, advanced: true },
   ];
   ```

3. Create desktop view using shadcn/ui Table:
   - Header row with "Features" column and tier names (Primary, Enhanced, Advanced)
   - Data rows with feature name and Check/X icons per tier
   - Apply bg-muted to header, hover:bg-muted/50 to rows

4. Create mobile view (hidden on md+):
   - Three stacked cards, one per tier
   - Each card shows tier name as heading
   - List only the features available for that tier with Check icons

5. Use responsive classes:
   - Desktop table: hidden md:table
   - Mobile cards: md:hidden

6. Export the component as named export
  </action>
  <verify>
File exists at src/components/pricing/feature-comparison-table.tsx
TypeScript compiles without errors: npx tsc --noEmit src/components/pricing/feature-comparison-table.tsx
  </verify>
  <done>
Component renders responsive feature comparison table with Check/X icons for each tier
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate FeatureComparisonTable into pricing page</name>
  <files>src/app/(marketing)/pricing/page.tsx</files>
  <action>
Update the pricing page to include the feature comparison table:

1. Add import at top:
   ```typescript
   import { FeatureComparisonTable } from "@/components/pricing/feature-comparison-table";
   ```

2. Add a new section between pricing cards and FAQs (after the closing div of pricing cards grid, around line 221):
   ```tsx
   {/* Feature Comparison */}
   <div className="mx-auto mt-16 max-w-6xl">
     <h2 className="mb-8 text-center text-2xl font-bold">
       Compare all features
     </h2>
     <FeatureComparisonTable />
   </div>
   ```

3. Ensure the section maintains consistent spacing with mt-16 (matching CTA section)
  </action>
  <verify>
Run dev server: npm run dev
Navigate to /pricing
Verify feature comparison table appears below tier cards
Verify table shows Check icons for available features, X icons for unavailable
Verify mobile view shows stacked cards with features (resize browser or use DevTools)
  </verify>
  <done>
Pricing page displays feature comparison table below tier cards with responsive design
  </done>
</task>

</tasks>

<verification>
1. Visit /pricing page
2. Scroll below tier cards to see "Compare all features" section
3. Desktop: Verify horizontal table with Features column and 3 tier columns
4. Desktop: Verify Check icons for available features, X icons for unavailable
5. Mobile (< 768px): Verify three stacked cards showing tier features
6. npm run lint passes with no new errors
</verification>

<success_criteria>
- FeatureComparisonTable component exists and exports correctly
- Pricing page imports and renders the component
- Table shows all 13 features with correct tier availability
- Responsive design: table on desktop, cards on mobile
- Visual consistency with existing pricing page styling
</success_criteria>

<output>
After completion, create `.planning/phases/27-pricing-portal-ui/27-01-SUMMARY.md`
</output>
