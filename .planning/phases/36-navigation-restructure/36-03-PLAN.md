---
phase: 36-navigation-restructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/payments/dashboard/page.tsx
  - src/app/(dashboard)/payments/analytics/page.tsx
  - src/app/(dashboard)/payments/forecast/page.tsx
  - src/app/(dashboard)/payments/subscriptions/page.tsx
  - src/app/(dashboard)/payments/subscriptions/new/page.tsx
  - src/app/(dashboard)/payments/subscriptions/[id]/page.tsx
  - src/app/(dashboard)/payments/subscriptions/[id]/edit/page.tsx
  - src/app/(dashboard)/payments/transactions/page.tsx
  - src/app/(dashboard)/payments/suggestions/page.tsx
  - src/app/(dashboard)/payments/reminders/page.tsx
  - src/app/(dashboard)/vault/load/page.tsx
autonomous: true
requirements:
  - NAV-02

must_haves:
  truths:
    - "Navigating to /payments/dashboard renders the dashboard page (not a 404)"
    - "Navigating to /payments/subscriptions renders the subscriptions list"
    - "Navigating to /payments/subscriptions/new renders the new subscription form"
    - "Navigating to /payments/subscriptions/[id] renders the subscription detail page"
    - "Navigating to /payments/subscriptions/[id]/edit renders the edit form"
    - "Navigating to /payments/analytics renders the analytics page"
    - "Navigating to /payments/forecast renders the forecasting page"
    - "Navigating to /payments/transactions renders the transactions page"
    - "Navigating to /payments/suggestions renders the suggestions page"
    - "Navigating to /payments/reminders renders the reminders page"
    - "Navigating to /vault/load renders the batch import page"
  artifacts:
    - path: "src/app/(dashboard)/payments/dashboard/page.tsx"
      provides: "Dashboard at new URL"
    - path: "src/app/(dashboard)/payments/subscriptions/page.tsx"
      provides: "Subscriptions list at new URL"
    - path: "src/app/(dashboard)/vault/load/page.tsx"
      provides: "Batch import at new URL"
  key_links:
    - from: "src/app/(dashboard)/payments/subscriptions/page.tsx"
      to: "existing subscriptions page component"
      via: "re-export or content copy"
      pattern: "export default"
    - from: "src/app/(dashboard)/vault/load/page.tsx"
      to: "existing import/batch page component"
      via: "re-export or content copy"
      pattern: "export default"
---

<objective>
Create all new route files under /payments/* and /vault/load/ by moving page content from old locations to new locations. Old route files remain in place (next.config.ts redirects handle external links; old files are dead code after this phase).

Purpose: Makes all new nav paths functional — clicking sidebar items navigates to real pages instead of 404s.
Output: 11 new page.tsx files under the new URL structure.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-navigation-restructure/36-CONTEXT.md
@.planning/phases/36-navigation-restructure/36-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /payments/* route files by copying content from old locations</name>
  <files>
    src/app/(dashboard)/payments/dashboard/page.tsx
    src/app/(dashboard)/payments/analytics/page.tsx
    src/app/(dashboard)/payments/forecast/page.tsx
    src/app/(dashboard)/payments/subscriptions/page.tsx
    src/app/(dashboard)/payments/subscriptions/new/page.tsx
    src/app/(dashboard)/payments/subscriptions/[id]/page.tsx
    src/app/(dashboard)/payments/subscriptions/[id]/edit/page.tsx
    src/app/(dashboard)/payments/transactions/page.tsx
    src/app/(dashboard)/payments/suggestions/page.tsx
    src/app/(dashboard)/payments/reminders/page.tsx
  </files>
  <action>
For each new route file, READ the old source file first, then WRITE the new file with the same content. Use this mapping:

| New path | Source (old path) |
|----------|-------------------|
| `src/app/(dashboard)/payments/dashboard/page.tsx` | `src/app/(dashboard)/dashboard/page.tsx` |
| `src/app/(dashboard)/payments/analytics/page.tsx` | `src/app/(dashboard)/analytics/page.tsx` |
| `src/app/(dashboard)/payments/forecast/page.tsx` | `src/app/(dashboard)/dashboard/forecasting/page.tsx` |
| `src/app/(dashboard)/payments/subscriptions/page.tsx` | `src/app/(dashboard)/subscriptions/page.tsx` |
| `src/app/(dashboard)/payments/subscriptions/new/page.tsx` | `src/app/(dashboard)/subscriptions/new/page.tsx` |
| `src/app/(dashboard)/payments/subscriptions/[id]/page.tsx` | `src/app/(dashboard)/subscriptions/[id]/page.tsx` |
| `src/app/(dashboard)/payments/subscriptions/[id]/edit/page.tsx` | `src/app/(dashboard)/subscriptions/[id]/edit/page.tsx` |
| `src/app/(dashboard)/payments/transactions/page.tsx` | `src/app/(dashboard)/transactions/page.tsx` |
| `src/app/(dashboard)/payments/suggestions/page.tsx` | `src/app/(dashboard)/suggestions/page.tsx` |
| `src/app/(dashboard)/payments/reminders/page.tsx` | `src/app/(dashboard)/reminders/page.tsx` |

Also check if `/dashboard/` has a `loading.tsx` — if so, copy it to `/payments/dashboard/loading.tsx`. Check if `/subscriptions/` has a `loading.tsx` — if so, copy it to `/payments/subscriptions/loading.tsx`.

Strategy: Copy the full file content verbatim. Do NOT refactor, simplify, or change any imports at this stage. Internal link references within these new files (e.g., breadcrumb hrefs pointing to old paths) will be updated in Task 2 of this plan.

Create all required parent directories as needed (e.g., `payments/`, `payments/subscriptions/`, `payments/subscriptions/[id]/`).

Do NOT delete the old route files — they remain as dead code, handled by next.config.ts redirects.
  </action>
  <verify>
All 10+ new page.tsx files exist under `src/app/(dashboard)/payments/`. `npm run build` completes without errors — no missing module errors from the new files.
  </verify>
  <done>
All /payments/* route files exist and mirror old content. npm run build passes. In dev server, navigating to /payments/dashboard, /payments/subscriptions, /payments/analytics renders pages without 404.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /vault/load route and update all internal link references across the codebase</name>
  <files>
    src/app/(dashboard)/vault/load/page.tsx
    src/app/(dashboard)/payments/dashboard/page.tsx
    src/app/(dashboard)/payments/subscriptions/page.tsx
    src/app/(dashboard)/payments/subscriptions/[id]/page.tsx
    src/app/(dashboard)/payments/subscriptions/new/page.tsx
    src/app/(dashboard)/payments/subscriptions/[id]/edit/page.tsx
    src/app/(dashboard)/payments/analytics/page.tsx
    src/app/(dashboard)/payments/reminders/page.tsx
    src/app/(dashboard)/onboarding/page.tsx
    src/app/(dashboard)/settings/layout.tsx
    src/components/suggestions/evidence-list.tsx
    src/components/sources/incomplete-batch-banner.tsx
    src/components/vault/vault-empty-state.tsx
    src/lib/hooks/use-batch-upload.ts
    src/lib/hooks/use-bulk-patterns.ts
    src/app/(auth)/login/login-form.tsx
    src/app/(dashboard)/admin/layout.tsx
    src/app/page.tsx
  </files>
  <action>
**Step 1 — Create /vault/load route**

Read `src/app/(dashboard)/import/batch/page.tsx` and write its content verbatim to `src/app/(dashboard)/vault/load/page.tsx`. Create the `vault/load/` directory. Also check if `/import/batch/` has a `loading.tsx` and copy it if present.

**Step 2 — Update internal links in each file**

For each file listed, read the current content and apply the following specific replacements. These are the ONLY changes to make in each file — do not alter anything else.

**src/app/(dashboard)/payments/dashboard/page.tsx** (copied from old /dashboard/page.tsx):
- `href="/subscriptions"` → `href="/payments/subscriptions"`
- `href="/analytics"` → `href="/payments/analytics"`
- `href="/import"` → `href="/vault/load"`
- Any breadcrumb `href="/dashboard"` → `href="/payments/dashboard"`

**src/app/(dashboard)/payments/subscriptions/page.tsx** (copied from old /subscriptions/page.tsx):
- `href="/subscriptions/new"` → `href="/payments/subscriptions/new"`
- `href="/import"` → `href="/vault/load"`
- Any breadcrumb `href="/dashboard"` → `href="/payments/dashboard"`
- Any breadcrumb label `"Dashboard"` → `"subs Dash"` (only when paired with the updated href)

**src/app/(dashboard)/payments/subscriptions/[id]/page.tsx**:
- `href="/subscriptions"` → `href="/payments/subscriptions"`
- `router.push("/subscriptions")` → `router.push("/payments/subscriptions")`
- Any breadcrumb `href="/dashboard"` → `href="/payments/dashboard"`
- Any breadcrumb `href="/subscriptions"` → `href="/payments/subscriptions"`

**src/app/(dashboard)/payments/subscriptions/new/page.tsx**:
- `router.push("/subscriptions")` → `router.push("/payments/subscriptions")`
- Any breadcrumb `href="/dashboard"` → `href="/payments/dashboard"`
- Any breadcrumb `href="/subscriptions"` → `href="/payments/subscriptions"`

**src/app/(dashboard)/payments/subscriptions/[id]/edit/page.tsx**:
- `router.push("/subscriptions/${id}")` or similar → `router.push("/payments/subscriptions/${id}")`
- `router.push("/subscriptions")` → `router.push("/payments/subscriptions")`
- Any breadcrumb hrefs: `/subscriptions` → `/payments/subscriptions`

**src/app/(dashboard)/payments/analytics/page.tsx**:
- Any breadcrumb `{ label: "Dashboard", href: "/dashboard" }` → `{ label: "subs Dash", href: "/payments/dashboard" }`

**src/app/(dashboard)/payments/reminders/page.tsx**:
- Any breadcrumb `{ label: "Dashboard", href: "/dashboard" }` → `{ label: "subs Dash", href: "/payments/dashboard" }`

**src/app/(dashboard)/onboarding/page.tsx** (OLD file — update in place):
- `router.push("/dashboard")` → `router.push("/payments/dashboard")`
- `router.push("/subscriptions/new")` → `router.push("/payments/subscriptions/new")`
- `router.push("/import")` → `router.push("/vault/load")`

**src/app/(dashboard)/settings/layout.tsx** (OLD file — update in place):
- `href="/reminders"` → `href="/payments/reminders"`
- Any breadcrumb `href="/dashboard"` → `href="/payments/dashboard"`

**src/components/suggestions/evidence-list.tsx** (OLD file — update in place):
- `href="/transactions?search=` → `href="/payments/transactions?search=`
- (or any href containing `/transactions`) → `/payments/transactions`

**src/components/sources/incomplete-batch-banner.tsx** (OLD file — update in place):
- `href="/import/batch"` → `href="/vault/load"`

**src/components/vault/vault-empty-state.tsx** (OLD file — update in place):
- `href="/import/batch"` → `href="/vault/load"`

**src/lib/hooks/use-batch-upload.ts** (OLD file — update in place):
- `window.location.href = "/suggestions"` → `window.location.href = "/payments/suggestions"`

**src/lib/hooks/use-bulk-patterns.ts** (OLD file — update in place):
- `window.location.href = "/subscriptions"` → `window.location.href = "/payments/subscriptions"`

**src/app/(auth)/login/login-form.tsx** (OLD file — update in place):
- `callbackUrl` default value `/dashboard` → `/payments/dashboard`
- Any `href="/dashboard"` → `href="/payments/dashboard"`

**src/app/(dashboard)/admin/layout.tsx** (OLD file — update in place):
- `redirect("/dashboard")` → `redirect("/payments/dashboard")`

**src/app/page.tsx** (OLD file — update in place):
- `redirect("/dashboard")` → `redirect("/payments/dashboard")`

**IMPORTANT:** Before editing each file, READ it first to confirm the exact string being replaced exists. If a file doesn't contain the expected old string, skip that replacement (don't introduce errors). The research was based on grep results from earlier in the session but the exact string context matters.

Also check `src/app/(dashboard)/payments/forecast/page.tsx` (copied from dashboard/forecasting/page.tsx) and update any breadcrumb `href="/dashboard"` → `href="/payments/dashboard"`.

Also update `src/app/(dashboard)/payments/suggestions/page.tsx` if it has internal links to transactions or subscriptions.
  </action>
  <verify>
`npm run build` completes with no TypeScript errors.

Grep check: `grep -r '"/dashboard"' src/app src/components src/lib --include="*.tsx" --include="*.ts"` — should return 0 matches (all old /dashboard internal links replaced). Exception: The old route files under `src/app/(dashboard)/dashboard/` remain as dead code with old references — that is acceptable.

`grep -r '"/subscriptions"' src/app src/components src/lib --include="*.tsx" --include="*.ts"` — should return matches only in OLD route files (not in shared components or hooks).

`ls src/app/(dashboard)/vault/load/page.tsx` confirms the file exists.
  </verify>
  <done>
/vault/load route exists and renders batch import content. All shared components, hooks, and non-old-route pages use new paths. npm run build passes with no errors.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` — no TypeScript errors
2. `ls src/app/(dashboard)/payments/` — shows dashboard, analytics, forecast, subscriptions, transactions, suggestions, reminders directories
3. `ls src/app/(dashboard)/vault/load/page.tsx` — file exists
4. `grep -r 'window.location.href.*"/suggestions"' src/lib` — returns no matches (updated)
5. `grep -r 'window.location.href.*"/subscriptions"' src/lib` — returns no matches (updated)
6. In dev server: navigate to /payments/dashboard, /payments/subscriptions, /vault/load — all render content, no 404s
</verification>

<success_criteria>
- All 11 new route files created with correct page content (NAV-02)
- Internal link references in shared components and hooks updated to new paths
- Internal link references in the new /payments/* page files updated (breadcrumbs, router.push)
- Core app redirects (proxy.ts, page.tsx, admin layout, login form) updated to /payments/dashboard
- npm run build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/36-navigation-restructure/36-03-SUMMARY.md`
</output>
