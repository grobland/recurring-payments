---
phase: 46-performance-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - src/app/(dashboard)/payments/dashboard/page.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/payments/analytics/page.tsx
  - src/app/(dashboard)/analytics/page.tsx
  - src/app/(dashboard)/payments/forecast/page.tsx
  - src/app/(dashboard)/dashboard/forecasting/page.tsx
  - src/components/suggestions/suggestion-card.tsx
  - src/components/accounts/account-spending-tab.tsx
  - .planning/performance/bundle-treemap-after.html
  - .planning/performance/LIGHTHOUSE.md
autonomous: false
requirements:
  - PERF-01
  - PERF-02
  - PERF-04

must_haves:
  truths:
    - "All 9 recharts chart components are loaded via next/dynamic with ssr:false at their call sites"
    - "react-pdf is confirmed not leaking into initial client bundle (already dynamic)"
    - "After-optimization treemap is committed showing reduced initial bundle"
    - "Lighthouse baseline scores are documented for 3 key pages"
  artifacts:
    - path: "src/app/(dashboard)/payments/dashboard/page.tsx"
      provides: "Dynamic imports for CategoryChart, YearOverYearChart, CategoryTrendsChart"
      contains: "next/dynamic"
    - path: "src/app/(dashboard)/payments/analytics/page.tsx"
      provides: "Dynamic imports for CategoryPieChart, SpendingTrendChart"
      contains: "next/dynamic"
    - path: "src/app/(dashboard)/payments/forecast/page.tsx"
      provides: "Dynamic imports for MonthlyForecastChart, AnnualForecastFanChart"
      contains: "next/dynamic"
    - path: "src/components/suggestions/suggestion-card.tsx"
      provides: "Dynamic import for SuggestionTimeline"
      contains: "next/dynamic"
    - path: "src/components/accounts/account-spending-tab.tsx"
      provides: "Dynamic import for AccountSpendingChart"
      contains: "next/dynamic"
    - path: ".planning/performance/bundle-treemap-after.html"
      provides: "Post-optimization bundle treemap"
      min_lines: 10
    - path: ".planning/performance/LIGHTHOUSE.md"
      provides: "Lighthouse baseline scores for 3 pages"
      contains: "Performance"
  key_links:
    - from: "src/app/(dashboard)/payments/dashboard/page.tsx"
      to: "recharts bundle"
      via: "next/dynamic lazy loading"
      pattern: "dynamic.*import.*chart"
    - from: "src/app/(dashboard)/payments/analytics/page.tsx"
      to: "recharts bundle"
      via: "next/dynamic lazy loading"
      pattern: "dynamic.*import.*chart"
---

<objective>
Convert all 9 recharts chart component imports to next/dynamic with ssr:false at their call sites, verify react-pdf is not leaking, generate the after-optimization treemap, and document Lighthouse baseline scores.

Purpose: Reduce initial page load bundle by deferring heavy recharts library to lazy-loaded async chunks, then measure and document the results.
Output: All chart imports dynamified, after-treemap committed, LIGHTHOUSE.md with baseline scores.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-performance-audit/46-CONTEXT.md
@.planning/phases/46-performance-audit/46-RESEARCH.md
@.planning/phases/46-performance-audit/46-01-SUMMARY.md

<interfaces>
<!-- Import sites that need conversion to dynamic imports -->

From src/app/(dashboard)/payments/dashboard/page.tsx:
```typescript
import { CategoryChart } from "@/components/dashboard/category-chart";
import { YearOverYearChart } from "@/components/charts/year-over-year-chart";
import { CategoryTrendsChart } from "@/components/charts/category-trends-chart";
```

From src/app/(dashboard)/dashboard/page.tsx:
```typescript
import { CategoryChart } from "@/components/dashboard/category-chart";
import { YearOverYearChart } from "@/components/charts/year-over-year-chart";
import { CategoryTrendsChart } from "@/components/charts/category-trends-chart";
```

From src/app/(dashboard)/payments/analytics/page.tsx:
```typescript
import { CategoryPieChart, SpendingTrendChart } from "@/components/charts";
```

From src/app/(dashboard)/analytics/page.tsx:
```typescript
import { CategoryPieChart, SpendingTrendChart } from "@/components/charts";
```

From src/app/(dashboard)/payments/forecast/page.tsx:
```typescript
import { MonthlyForecastChart } from "@/components/forecast/monthly-forecast-chart";
import { AnnualForecastFanChart } from "@/components/forecast/annual-forecast-fan-chart";
```

From src/app/(dashboard)/dashboard/forecasting/page.tsx:
```typescript
import { MonthlyForecastChart } from "@/components/forecast/monthly-forecast-chart";
import { AnnualForecastFanChart } from "@/components/forecast/annual-forecast-fan-chart";
```

From src/components/suggestions/suggestion-card.tsx:
```typescript
import { SuggestionTimeline } from "./suggestion-timeline";
```

From src/components/accounts/account-spending-tab.tsx:
```typescript
import { AccountSpendingChart } from "./account-spending-chart";
```

<!-- Dynamic import pattern (from existing pdf-viewer-modal.tsx) -->
```typescript
import dynamic from "next/dynamic";
import { Skeleton } from "@/components/ui/skeleton";

const Component = dynamic(
  () => import("./path").then((m) => ({ default: m.Component })),
  {
    ssr: false,
    loading: () => <Skeleton className="h-[300px] w-full" />,
  }
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert all recharts imports to next/dynamic at call sites</name>
  <files>
    src/app/(dashboard)/payments/dashboard/page.tsx,
    src/app/(dashboard)/dashboard/page.tsx,
    src/app/(dashboard)/payments/analytics/page.tsx,
    src/app/(dashboard)/analytics/page.tsx,
    src/app/(dashboard)/payments/forecast/page.tsx,
    src/app/(dashboard)/dashboard/forecasting/page.tsx,
    src/components/suggestions/suggestion-card.tsx,
    src/components/accounts/account-spending-tab.tsx
  </files>
  <action>
In each file listed below, replace the static chart import with a next/dynamic import. Add `import dynamic from "next/dynamic"` and `import { Skeleton } from "@/components/ui/skeleton"` at the top (if not already present). Use `ssr: false` on all chart dynamic imports since recharts uses browser-only APIs.

**Pattern for each chart — use `.then((m) => ({ default: m.ExportName }))` since all charts use named exports:**

```typescript
const ChartComponent = dynamic(
  () => import("@/components/path/chart-file").then((m) => ({ default: m.ChartComponent })),
  { ssr: false, loading: () => <Skeleton className="h-[300px] w-full" /> }
);
```

**Files and their chart replacements:**

1. **src/app/(dashboard)/payments/dashboard/page.tsx** — Replace 3 static imports:
   - `CategoryChart` from `@/components/dashboard/category-chart`
   - `YearOverYearChart` from `@/components/charts/year-over-year-chart`
   - `CategoryTrendsChart` from `@/components/charts/category-trends-chart`

2. **src/app/(dashboard)/dashboard/page.tsx** — Replace same 3 static imports (same components, different page route):
   - `CategoryChart` from `@/components/dashboard/category-chart`
   - `YearOverYearChart` from `@/components/charts/year-over-year-chart`
   - `CategoryTrendsChart` from `@/components/charts/category-trends-chart`

3. **src/app/(dashboard)/payments/analytics/page.tsx** — Replace barrel import with 2 direct dynamic imports:
   - Remove `import { CategoryPieChart, SpendingTrendChart } from "@/components/charts"`
   - Add dynamic import for `CategoryPieChart` from `@/components/charts/category-pie-chart`
   - Add dynamic import for `SpendingTrendChart` from `@/components/charts/spending-trend-chart`

4. **src/app/(dashboard)/analytics/page.tsx** — Same as #3 (mirror route):
   - Remove barrel import, add 2 direct dynamic imports

5. **src/app/(dashboard)/payments/forecast/page.tsx** — Replace 2 static imports:
   - `MonthlyForecastChart` from `@/components/forecast/monthly-forecast-chart`
   - `AnnualForecastFanChart` from `@/components/forecast/annual-forecast-fan-chart`

6. **src/app/(dashboard)/dashboard/forecasting/page.tsx** — Same as #5 (mirror route):
   - Replace 2 static imports with dynamic

7. **src/components/suggestions/suggestion-card.tsx** — Replace 1 static import:
   - `SuggestionTimeline` from `./suggestion-timeline`
   - Use a smaller skeleton: `<div className="h-20 w-full animate-pulse bg-accent rounded-md" />`

8. **src/components/accounts/account-spending-tab.tsx** — Replace 1 static import:
   - `AccountSpendingChart` from `./account-spending-chart`

**Important:**
- Do NOT modify the chart component files themselves — only change the import sites
- Leave the charts barrel `src/components/charts/index.ts` as-is (analytics pages now import directly, barrel remains for any other consumers)
- Every dynamic import MUST use `ssr: false` (recharts uses ResizeObserver)
- Preserve all other imports and component logic unchanged in each file

**Verify react-pdf:** Check that `pdf-viewer-inner.tsx` is ONLY imported via the dynamic import in `pdf-viewer-modal.tsx`. No action needed if already correct (it should be, per existing implementation). Just confirm.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/OneDrive/19. Vibe codeing/recurring-payments" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>All 9 recharts components are dynamically imported via next/dynamic with ssr:false across 8 call-site files. Build succeeds. react-pdf confirmed not leaking into initial bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Generate after-optimization treemap and create Lighthouse doc</name>
  <files>.planning/performance/bundle-treemap-after.html, .planning/performance/LIGHTHOUSE.md</files>
  <action>
1. Run `ANALYZE=true npm run build` (or `npm run analyze`) to generate the post-optimization treemap.

2. Copy the client treemap: `cp .next/analyze/client.html .planning/performance/bundle-treemap-after.html`

3. Compare before and after treemaps briefly — note whether recharts chunks moved from initial to async/lazy chunks. Document findings.

4. Create `.planning/performance/LIGHTHOUSE.md` with the following template. Since Lighthouse requires a running browser and server, fill in placeholder scores and note that the user should run Lighthouse manually to capture actual scores:

```markdown
# Lighthouse Baseline — Phase 46

**Date:** 2026-03-05
**Targets:** Performance 80+, Accessibility 95+, Best Practices 95+
**Environment:** Production build (npm run build && npm run start) on localhost

## Scores

| Page | Performance | Accessibility | Best Practices | SEO |
|------|-------------|---------------|----------------|-----|
| Landing (/) | _run pending_ | _run pending_ | _run pending_ | _run pending_ |
| Dashboard (/payments/dashboard) | _run pending_ | _run pending_ | _run pending_ | _run pending_ |
| Vault (/vault) | _run pending_ | _run pending_ | _run pending_ | _run pending_ |

## How to Run

1. Build and start: `npm run build && npm run start`
2. In Chrome DevTools > Lighthouse tab, run audit for each page
3. Or use CLI: `npx lighthouse http://localhost:3000 --output json --output html`
4. Update scores above

## Gap Analysis

[Update after scores are captured — note any category below target]

## Bundle Optimization Impact

[Summary of before vs after treemap comparison — recharts chunk movement, total size change]

## Recommendations for Future Phases

[Any additional findings from the treemap audit that are out of scope for Phase 46]
```

5. If possible, attempt to run Lighthouse CLI headlessly: `npx lighthouse http://localhost:3000 --chrome-flags="--headless" --output json --quiet`. If it works, parse the JSON and fill in actual scores. If it fails (common on Windows without proper Chrome path), leave as "run pending" — the checkpoint task will handle it.
  </action>
  <verify>
    <automated>ls -la "C:/Users/andre/OneDrive/19. Vibe codeing/recurring-payments/.planning/performance/bundle-treemap-after.html" "C:/Users/andre/OneDrive/19. Vibe codeing/recurring-payments/.planning/performance/LIGHTHOUSE.md" 2>&1</automated>
  </verify>
  <done>After-optimization treemap committed. LIGHTHOUSE.md created with score template (actual scores filled if CLI worked, otherwise marked for manual capture).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify charts render and capture Lighthouse scores</name>
  <files>.planning/performance/LIGHTHOUSE.md</files>
  <action>Human verifies dynamic-imported charts still render correctly and captures Lighthouse scores for the 3 target pages.</action>
  <verify>
    <automated>cat "C:/Users/andre/OneDrive/19. Vibe codeing/recurring-payments/.planning/performance/LIGHTHOUSE.md" | head -20</automated>
  </verify>
  <done>Charts render correctly on all pages. LIGHTHOUSE.md updated with actual scores.</done>
  <what-built>
All recharts chart components (9 files across 8 call sites) converted to next/dynamic with ssr:false. Bundle analyzer configured with before/after treemaps. optimizePackageImports set for lucide-react. LIGHTHOUSE.md created.
  </what-built>
  <how-to-verify>
1. Run `npm run build && npm run start` to start a production server
2. Open http://localhost:3000/payments/dashboard — verify charts load (may show skeleton briefly then render)
3. Open http://localhost:3000/payments/analytics — verify pie chart and trend chart load
4. Open http://localhost:3000/payments/forecast — verify forecast charts load
5. Open Chrome DevTools > Lighthouse tab on each of these 3 pages + landing page (/), run audit
6. Update `.planning/performance/LIGHTHOUSE.md` with actual scores
7. Confirm the before/after treemaps exist in `.planning/performance/` and the after treemap shows recharts in async chunks (not initial bundle)
  </how-to-verify>
  <resume-signal>Type "approved" after verifying charts render and updating Lighthouse scores, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -r "next/dynamic" src/app/\(dashboard\)/payments/dashboard/page.tsx` shows dynamic imports
- `grep -r "next/dynamic" src/app/\(dashboard\)/payments/analytics/page.tsx` shows dynamic imports
- `grep -r "next/dynamic" src/app/\(dashboard\)/payments/forecast/page.tsx` shows dynamic imports
- `grep -r "next/dynamic" src/components/suggestions/suggestion-card.tsx` shows dynamic import
- `grep -r "next/dynamic" src/components/accounts/account-spending-tab.tsx` shows dynamic import
- `npm run build` succeeds
- `.planning/performance/bundle-treemap-after.html` exists
- `.planning/performance/LIGHTHOUSE.md` exists with score table
</verification>

<success_criteria>
- All 9 recharts-importing components are loaded via next/dynamic with ssr:false at their 8 call sites (some charts used in 2 pages)
- react-pdf confirmed not leaking into initial client bundle
- After-optimization treemap committed showing recharts in async/lazy chunks
- LIGHTHOUSE.md documents baseline scores for Landing, Dashboard, and Vault pages (targets: Performance 80+, Accessibility 95+, Best Practices 95+)
- Build passes with no TypeScript or runtime errors
- Charts still render correctly on all affected pages
</success_criteria>

<output>
After completion, create `.planning/phases/46-performance-audit/46-02-SUMMARY.md`
</output>
