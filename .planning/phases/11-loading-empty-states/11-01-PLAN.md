---
phase: 11-loading-empty-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/hooks/use-delayed-loading.ts
  - src/components/shared/empty-state.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/subscriptions/page.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard shows skeleton cards while analytics data loads (with 200ms delay)"
    - "Subscription list shows skeleton rows while fetching (with varied widths)"
    - "Empty subscription list shows icon + friendly message + dual CTAs"
    - "Skeletons avoid flash on fast loads (skip if < 200ms)"
    - "Content fades in smoothly when replacing skeleton"
  artifacts:
    - path: "src/lib/hooks/use-delayed-loading.ts"
      provides: "Delayed loading state management"
      exports: ["useDelayedLoading"]
    - path: "src/components/shared/empty-state.tsx"
      provides: "Reusable empty state component"
      exports: ["EmptyState"]
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Enhanced dashboard with delayed skeletons"
      contains: "useDelayedLoading"
    - path: "src/app/(dashboard)/subscriptions/page.tsx"
      provides: "Enhanced subscriptions with varied skeletons + improved empty state"
      contains: "EmptyState"
  key_links:
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "src/lib/hooks/use-delayed-loading.ts"
      via: "import useDelayedLoading"
      pattern: "useDelayedLoading.*isLoading"
    - from: "src/app/(dashboard)/subscriptions/page.tsx"
      to: "src/components/shared/empty-state.tsx"
      via: "import EmptyState"
      pattern: "EmptyState.*icon.*CreditCard"
---

<objective>
Create shared loading infrastructure and enhance Dashboard and Subscriptions pages with polished skeleton loaders and empty states.

Purpose: Improve perceived performance and provide clear feedback when data is loading or when lists are empty. Users should never see a flash of skeleton on fast loads, and empty states should guide users toward action.

Output: useDelayedLoading hook, reusable EmptyState component, enhanced Dashboard and Subscriptions pages with proper loading/empty states.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-loading-empty-states/11-CONTEXT.md
@.planning/phases/11-loading-empty-states/11-RESEARCH.md

@src/components/ui/skeleton.tsx
@src/app/(dashboard)/dashboard/page.tsx
@src/app/(dashboard)/subscriptions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared loading infrastructure</name>
  <files>
    src/lib/hooks/use-delayed-loading.ts
    src/components/shared/empty-state.tsx
  </files>
  <action>
Create two reusable components:

**useDelayedLoading hook** (`src/lib/hooks/use-delayed-loading.ts`):
- Accepts `isLoading: boolean` and optional `{ delayMs?: number, minDisplayMs?: number }`
- Default delayMs = 200 (don't show skeleton until 200ms passed)
- Default minDisplayMs = 300 (once shown, display for at least 300ms)
- Returns `showSkeleton: boolean`
- Implementation from RESEARCH.md Pattern 1:
  - Use useRef for loadingStartRef and delayTimeoutRef
  - On isLoading true: start delay timer
  - On isLoading false: clear timer if not shown yet, or wait for minDisplayMs
  - Clean up timers on unmount

**EmptyState component** (`src/components/shared/empty-state.tsx`):
- Props: icon (LucideIcon), title (string), description (string), primaryAction?, secondaryAction?
- primaryAction/secondaryAction: { label: string, href: string, icon?: LucideIcon }
- Layout: centered, icon at top (h-12 w-12 text-muted-foreground), title below, description below that, action buttons at bottom
- Use Next.js Link + Button with asChild pattern
- Example from RESEARCH.md Pattern 2

Export both from the files. Also add `use-delayed-loading` to the hooks barrel export if one exists.
  </action>
  <verify>
Files exist with correct exports:
- `grep -l "useDelayedLoading" src/lib/hooks/use-delayed-loading.ts`
- `grep -l "EmptyState" src/components/shared/empty-state.tsx`
TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
useDelayedLoading hook exports function with correct signature. EmptyState component exports functional component with icon, title, description, and optional action props.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance Dashboard page with delayed loading</name>
  <files>
    src/app/(dashboard)/dashboard/page.tsx
  </files>
  <action>
Integrate useDelayedLoading into the Dashboard page:

1. Import useDelayedLoading from the new hook file

2. Replace direct `isLoading` checks with `showSkeleton`:
   ```typescript
   const showSkeleton = useDelayedLoading(isLoading);
   ```

3. Update all skeleton conditionals to use `showSkeleton` instead of `isLoading`:
   - Stats cards (Monthly Spending, Active Subscriptions, Renewing Soon, Needs Attention)
   - Upcoming Renewals list

4. Add fade-in animation when content appears:
   - Wrap content sections in a div with `className="animate-in fade-in duration-150"`
   - Only apply when !showSkeleton AND !isLoading (data is ready)

5. Ensure skeleton layout matches actual content dimensions exactly (already mostly correct, but verify icon placeholders in card headers)

6. For "Upcoming Renewals" section when empty (upcomingRenewals.length === 0 && !showSkeleton):
   - Keep existing message "No upcoming renewals in the next 7 days" - this is fine as-is, not a true empty state

Do NOT change the ServiceUnavailable error handling - that's already correct from Phase 10.
  </action>
  <verify>
Run dev server and verify:
- On slow network (throttle to Slow 3G in DevTools): skeleton appears after brief delay, displays for minimum time
- On fast network: skeleton may not appear at all (data loads < 200ms)
- Content fades in smoothly

`npm run build` passes without errors.
  </verify>
  <done>
Dashboard uses useDelayedLoading hook. Skeleton display is delayed by 200ms. Content fades in with 150ms animation. No layout shift between skeleton and content.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance Subscriptions page with varied skeletons and improved empty state</name>
  <files>
    src/app/(dashboard)/subscriptions/page.tsx
  </files>
  <action>
Enhance the Subscriptions page loading and empty states:

1. Import useDelayedLoading and EmptyState component

2. Replace isLoading checks with showSkeleton:
   ```typescript
   const showSkeleton = useDelayedLoading(isLoading);
   ```

3. Enhance skeleton rows with varied widths per CONTEXT.md "varied skeleton widths for text elements":
   - Create SKELETON_WIDTHS object at top of file:
     ```typescript
     const SKELETON_WIDTHS = {
       name: ["w-32", "w-40", "w-36", "w-28", "w-44"],
       category: ["w-20", "w-24", "w-16", "w-20", "w-24"],
       amount: ["w-16", "w-14", "w-18", "w-16", "w-14"],
     };
     ```
   - Apply varied widths to each skeleton row (5 rows) using index

4. Update skeleton to use Table structure (matching actual content):
   - Currently shows Card skeletons, but actual content is a Table
   - Replace with TableRow skeletons with all columns:
     - Name (avatar + text)
     - Category (badge shape)
     - Amount
     - Frequency
     - Next Renewal (date + relative text)
     - Status (badge shape)
     - Actions (small button)

5. Replace the existing EmptyState function with the shared EmptyState component:
   - When hasFilters: show "No subscriptions found" with description about adjusting filters (no CTAs)
   - When !hasFilters: Use EmptyState with:
     - icon: CreditCard
     - title: "No subscriptions yet"
     - description: "Add your first subscription to start tracking your recurring payments, or import them from a bank statement."
     - primaryAction: { label: "Add subscription", href: "/subscriptions/new", icon: Plus }
     - secondaryAction: { label: "Import from PDF", href: "/import" }

6. Add fade-in animation to content container.

Remove the local EmptyState function definition - use the shared component instead.
  </action>
  <verify>
Run dev server:
- Skeleton table rows appear with varied widths (visually inspect - rows should look different)
- Empty state shows icon + message + two buttons when no subscriptions
- Empty state with filters shows different message without buttons
- Content fades in smoothly

`npm run build` passes.
  </verify>
  <done>
Subscriptions page uses useDelayedLoading. Skeleton rows have varied widths and match table structure. Empty state uses shared EmptyState component with CreditCard icon and dual CTAs ("Add subscription" + "Import from PDF").
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` - no TypeScript errors
2. `npm run lint` - no linting errors
3. Manual verification in browser:
   - Dashboard: skeleton timing correct, fade-in smooth
   - Subscriptions: varied skeleton widths visible, empty state has icon and two action buttons
   - Fast loads don't flash skeleton
</verification>

<success_criteria>
- useDelayedLoading hook implemented with 200ms delay and 300ms minimum display
- EmptyState component reusable with icon, title, description, actions
- Dashboard skeletons use delayed loading with fade-in
- Subscriptions skeletons have varied widths and table structure
- Empty subscription list shows CreditCard icon + friendly message + "Add subscription" (primary) + "Import from PDF" (secondary)
- No layout shift between skeleton and content states
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-loading-empty-states/11-01-SUMMARY.md`
</output>
