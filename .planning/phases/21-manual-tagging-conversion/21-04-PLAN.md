---
phase: 21-manual-tagging-conversion
plan: 04
type: execute
wave: 3
depends_on: ["21-02", "21-03"]
files_modified:
  - src/components/transactions/transaction-browser.tsx
  - src/components/transactions/transaction-table.tsx
  - src/components/transactions/transaction-row.tsx
  - src/components/transactions/transaction-card.tsx
  - src/components/transactions/transaction-card-list.tsx
  - src/components/transactions/bulk-action-bar.tsx
  - src/app/api/transactions/bulk/route.ts
  - src/lib/hooks/use-bulk-tag-transactions.ts
autonomous: true

must_haves:
  truths:
    - "User can select multiple transactions via checkboxes"
    - "Header checkbox selects/deselects all visible rows"
    - "Floating bar appears at bottom when items are selected"
    - "Bar shows selected count and Tag action button"
    - "Bulk tag applies selected tag to all selected transactions"
    - "Changing filters clears selection state"
  artifacts:
    - path: "src/components/transactions/bulk-action-bar.tsx"
      provides: "Floating bottom bar with bulk actions"
      min_lines: 30
    - path: "src/app/api/transactions/bulk/route.ts"
      provides: "POST endpoint for bulk tagging"
      exports: ["POST"]
    - path: "src/lib/hooks/use-bulk-tag-transactions.ts"
      provides: "Mutation hook for bulk operations"
      exports: ["useBulkTagTransactions"]
  key_links:
    - from: "src/components/transactions/transaction-browser.tsx"
      to: "src/components/transactions/bulk-action-bar.tsx"
      via: "conditional render when selectedIds.size > 0"
      pattern: "selectedIds\\.size.*BulkActionBar"
    - from: "src/lib/hooks/use-bulk-tag-transactions.ts"
      to: "/api/transactions/bulk"
      via: "fetch POST"
      pattern: "fetch.*transactions/bulk"
---

<objective>
Implement bulk selection and tagging for multiple transactions at once.

Purpose: Allow users to efficiently tag many transactions with the same label without clicking through each one individually. Selection uses checkboxes; bulk actions appear in a floating bar.

Output: Checkbox selection in table, floating action bar, bulk tag API, and integration.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-manual-tagging-conversion/21-CONTEXT.md
@.planning/phases/21-manual-tagging-conversion/21-RESEARCH.md
@.planning/phases/21-manual-tagging-conversion/21-01-SUMMARY.md
@.planning/phases/21-manual-tagging-conversion/21-02-SUMMARY.md
@.planning/phases/21-manual-tagging-conversion/21-03-SUMMARY.md

# Existing components
@src/components/transactions/transaction-browser.tsx
@src/components/transactions/transaction-table.tsx
@src/components/transactions/transaction-row.tsx
@src/components/transactions/transaction-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selection state to TransactionBrowser</name>
  <files>
    src/components/transactions/transaction-browser.tsx
    src/components/transactions/transaction-table.tsx
    src/components/transactions/transaction-row.tsx
  </files>
  <action>
1. Update src/components/transactions/transaction-browser.tsx:
   - Add state: const [selectedIds, setSelectedIds] = useState<Set<string>>(new Set())
   - Compute isAllVisibleSelected: check if all allTransactions ids are in selectedIds
   - Compute isSomeSelected: selectedIds.size > 0 && !isAllVisibleSelected
   - Add toggleAll function: if isAllVisibleSelected, remove all visible ids; else add all visible ids
   - Add toggleOne(id: string) function: toggle single id in/out of set
   - Clear selection when filters change: useEffect(() => setSelectedIds(new Set()), [debouncedFilters])
   - Pass selection props to TransactionTable: { selectedIds, onToggleAll: toggleAll, onToggleOne: toggleOne, isAllSelected: isAllVisibleSelected, isSomeSelected }
   - Pass selection props to TransactionCardList similarly

2. Update src/components/transactions/transaction-table.tsx:
   - Add checkbox column header:
     - Use shadcn Checkbox component
     - checked={isAllSelected}
     - indeterminate={isSomeSelected} (use data-state="indeterminate")
     - onCheckedChange={onToggleAll}
   - Pass selectedIds and onToggleOne to TransactionRow

3. Update src/components/transactions/transaction-row.tsx:
   - Add props: isSelected: boolean, onToggle: () => void
   - Add checkbox cell at start of row:
     - Checkbox checked={isSelected}
     - onCheckedChange={() => onToggle()}
   - Style selected rows with subtle background highlight

Ensure virtualization still works with checkbox column added.
  </action>
  <verify>
Dev server running:
1. Navigate to /transactions
2. Checkbox column visible in table
3. Clicking row checkbox toggles selection
4. Header checkbox selects/deselects all visible
5. Selection clears when filter changes
  </verify>
  <done>Selection state works with checkboxes in table</done>
</task>

<task type="auto">
  <name>Task 2: Create bulk tag API and hook</name>
  <files>
    src/app/api/transactions/bulk/route.ts
    src/lib/hooks/use-bulk-tag-transactions.ts
  </files>
  <action>
1. Create src/app/api/transactions/bulk/route.ts:
   - POST handler for bulk tagging:
     - Authenticate user
     - Validate body: { transactionIds: string[], tagId: string, action: "add" | "remove" }
     - Verify all transactions belong to user (security - query with userId check)
     - Verify tag belongs to user
     - If action is "add":
       - Insert into transactionTags for each transactionId (use ON CONFLICT DO NOTHING for upsert)
     - If action is "remove":
       - Delete from transactionTags where transactionId IN ids AND tagId = tagId
     - Return { success: true, updatedCount: number }
   - Handle errors: 401, 400 (validation), 404 (tag not found)

2. Create src/lib/hooks/use-bulk-tag-transactions.ts:
   - useBulkTagTransactions mutation hook:
     - mutationFn: POST to /api/transactions/bulk with { transactionIds, tagId, action }
     - onSuccess:
       - Invalidate transactionKeys.lists()
       - toast.success(`Tagged ${count} transactions`)
     - onError: toast.error with message
   - Export for use in BulkActionBar
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit`</verify>
  <done>Bulk tag API and hook ready for UI integration</done>
</task>

<task type="auto">
  <name>Task 3: Create BulkActionBar and integrate with browser</name>
  <files>
    src/components/transactions/bulk-action-bar.tsx
    src/components/transactions/transaction-browser.tsx
    src/components/transactions/transaction-card.tsx
    src/components/transactions/transaction-card-list.tsx
  </files>
  <action>
1. Create src/components/transactions/bulk-action-bar.tsx:
   - Props: { count: number; onTag: (tagId: string) => void; onClear: () => void }
   - Render fixed-position bar at bottom center of screen:
     - className: "fixed bottom-4 left-1/2 -translate-x-1/2 z-50"
     - Inner div: flex, items-center, gap-4, bg-background, border, rounded-lg, shadow-lg, px-4, py-3
   - Show: "{count} item(s) selected"
   - Divider: vertical line (h-4 w-px bg-border)
   - Tag button: opens Popover with TagCombobox-like list to select tag to apply
     - When tag selected, call onTag(tagId)
   - Clear button: X icon, ghost variant, calls onClear
   - Animate entrance with CSS transition (optional)

2. Update src/components/transactions/transaction-browser.tsx:
   - Import BulkActionBar, useBulkTagTransactions, useTags
   - Render BulkActionBar conditionally: {selectedIds.size > 0 && <BulkActionBar ... />}
   - onTag handler:
     - Call bulkTagMutation with transactionIds from selectedIds, action "add"
     - Clear selection after success
   - onClear: setSelectedIds(new Set())

3. Update mobile card list:
   - Update src/components/transactions/transaction-card.tsx:
     - Add props: isSelected, onToggle (same as row)
     - Add checkbox in card (top-left corner or card header)
   - Update src/components/transactions/transaction-card-list.tsx:
     - Pass selectedIds and toggle handlers to cards
   - BulkActionBar should appear on mobile too (same fixed positioning)
  </action>
  <verify>
Dev server running:
1. Select multiple transactions
2. Floating bar appears showing count
3. Click Tag button, select a tag
4. All selected transactions receive the tag
5. Selection clears after tagging
6. Works on mobile card view too
  </verify>
  <done>Bulk selection and tagging fully functional</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. Checkbox column works for selection
3. Header checkbox selects/deselects all visible
4. Floating bar appears with selected count
5. Bulk tag applies to all selected transactions
6. Selection clears on filter change
7. Mobile card view has checkboxes and action bar
</verification>

<success_criteria>
- Checkbox column enables multi-select
- Header checkbox controls visible row selection (not all matching filter)
- Floating action bar shows count and Tag button
- Bulk tag applies selected tag to all selected transactions
- Filter change clears selection
- Works on both desktop table and mobile card views
</success_criteria>

<output>
After completion, create `.planning/phases/21-manual-tagging-conversion/21-04-SUMMARY.md`
</output>
