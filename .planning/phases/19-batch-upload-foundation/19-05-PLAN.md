---
phase: 19-batch-upload-foundation
plan: 05
type: execute
wave: 4
depends_on: ["19-03", "19-04"]
files_modified:
  - src/app/(dashboard)/import/batch/page.tsx
  - src/components/layout/sidebar.tsx
autonomous: false

must_haves:
  truths:
    - "User can navigate to batch import from the sidebar menu"
    - "User sees files processed one-by-one with progress indicators"
    - "User sees completion summary showing files processed and transactions extracted"
  artifacts:
    - path: "src/app/(dashboard)/import/batch/page.tsx"
      provides: "Batch import page"
      min_lines: 50
    - path: "src/components/layout/sidebar.tsx"
      provides: "Navigation link to batch import"
      contains: "/import/batch"
  key_links:
    - from: "src/app/(dashboard)/import/batch/page.tsx"
      to: "BatchUploader component"
      via: "import and render"
      pattern: "BatchUploader"
---

<objective>
Create batch import page and add navigation. Verify end-to-end batch upload flow.

Purpose: Integration and verification - connect all batch upload components, add to navigation, verify the complete flow works from drag-and-drop to transaction storage.

Output:
- Batch import page at /import/batch
- Sidebar link to batch import
- Visual verification of end-to-end flow
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-batch-upload-foundation/19-CONTEXT.md
@.planning/phases/19-batch-upload-foundation/19-01-SUMMARY.md
@.planning/phases/19-batch-upload-foundation/19-02-SUMMARY.md
@.planning/phases/19-batch-upload-foundation/19-03-SUMMARY.md
@.planning/phases/19-batch-upload-foundation/19-04-SUMMARY.md
@src/components/layout/sidebar.tsx
@src/app/(dashboard)/import/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create batch import page</name>
  <files>src/app/(dashboard)/import/batch/page.tsx</files>
  <action>
Create a new page for batch import that uses the BatchUploader component.

```typescript
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { Check, ArrowRight } from "lucide-react";
import { DashboardHeader } from "@/components/layout";
import { BatchUploader } from "@/components/batch/batch-uploader";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import type { BatchUploadResult } from "@/lib/hooks/use-batch-upload";

export default function BatchImportPage() {
  const router = useRouter();
  const [result, setResult] = useState<BatchUploadResult | null>(null);

  const handleComplete = (uploadResult: BatchUploadResult) => {
    setResult(uploadResult);
  };

  const handleStartNew = () => {
    setResult(null);
  };

  return (
    <>
      <DashboardHeader
        title="Batch Import"
        breadcrumbs={[
          { label: "Dashboard", href: "/dashboard" },
          { label: "Import", href: "/import" },
          { label: "Batch Import" },
        ]}
      />
      <main className="flex-1 p-4 md:p-6">
        <div className="mx-auto max-w-3xl space-y-6">
          {/* Header */}
          <div>
            <h2 className="text-2xl font-bold tracking-tight">
              Batch Import Statements
            </h2>
            <p className="text-muted-foreground">
              Upload multiple bank statements at once. We&apos;ll store all transactions
              and automatically detect potential subscriptions.
            </p>
          </div>

          {/* Result or Uploader */}
          {result ? (
            <Card>
              <CardContent className="py-12 text-center">
                <div className="mx-auto flex h-12 w-12 items-center justify-center rounded-full bg-green-100 dark:bg-green-900">
                  <Check className="h-6 w-6 text-green-600 dark:text-green-400" />
                </div>
                <h3 className="mt-4 text-lg font-semibold">Batch Import Complete!</h3>
                <div className="mt-2 space-y-1 text-muted-foreground">
                  <p>
                    {result.successful} of {result.totalFiles} files processed successfully
                  </p>
                  <p className="text-lg font-medium text-foreground">
                    {result.totalTransactions} transactions found
                  </p>
                  {result.failed > 0 && (
                    <p className="text-destructive">
                      {result.failed} file{result.failed !== 1 ? "s" : ""} failed
                    </p>
                  )}
                  {result.skipped > 0 && (
                    <p className="text-yellow-600 dark:text-yellow-400">
                      {result.skipped} duplicate{result.skipped !== 1 ? "s" : ""} skipped
                    </p>
                  )}
                </div>
                <div className="mt-6 flex flex-col-reverse gap-3 sm:flex-row sm:justify-center sm:gap-2">
                  <Button
                    variant="outline"
                    onClick={handleStartNew}
                    className="w-full sm:w-auto h-11"
                  >
                    Import More
                  </Button>
                  <Button
                    onClick={() => router.push("/subscriptions")}
                    className="w-full sm:w-auto h-11"
                  >
                    View Subscriptions
                    <ArrowRight className="ml-2 h-4 w-4" />
                  </Button>
                </div>
              </CardContent>
            </Card>
          ) : (
            <BatchUploader onComplete={handleComplete} />
          )}

          {/* Help text */}
          <div className="rounded-lg bg-muted/50 p-4 text-sm text-muted-foreground">
            <p className="font-medium text-foreground mb-2">How batch import works:</p>
            <ol className="list-decimal list-inside space-y-1 ml-2">
              <li>Select the bank/credit card account for all statements</li>
              <li>Drag and drop your PDF statements (up to 12+ files)</li>
              <li>Files are processed one at a time to avoid memory issues</li>
              <li>All transactions are stored for future browsing</li>
              <li>Recurring charges are automatically flagged as potential subscriptions</li>
            </ol>
          </div>
        </div>
      </main>
    </>
  );
}
```
  </action>
  <verify>Run `npx tsc --noEmit` to verify types. Check page compiles without errors.</verify>
  <done>Batch import page renders and uses BatchUploader component.</done>
</task>

<task type="auto">
  <name>Task 2: Add batch import to sidebar navigation</name>
  <files>src/components/layout/sidebar.tsx</files>
  <action>
Add a navigation item for batch import in the sidebar. Find the existing Import link and add a child item or a separate link.

Look for the import-related navigation items and add:

```typescript
{
  title: "Batch Import",
  href: "/import/batch",
  icon: FolderUp, // or similar icon
}
```

If the sidebar uses a nested structure, add it as a child of the Import section. If it's flat, add it as a separate item near the existing Import link.

Make sure to import the appropriate icon (FolderUp or Upload from lucide-react).

The exact placement depends on the existing sidebar structure - maintain consistency with the current pattern.
  </action>
  <verify>Run `npx tsc --noEmit` to verify types. Check that navigation link appears correctly.</verify>
  <done>Batch import appears in sidebar navigation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete batch upload flow:
1. Database schema (statements, transactions tables)
2. API endpoints (check-hash, upload, process)
3. useBatchUpload hook for queue management
4. UI components (BatchUploader, FileQueue, FileItem)
5. Batch import page with navigation
  </what-built>
  <how-to-verify>
1. Run `npm run dev` to start the development server
2. Navigate to /import/batch (or click the Batch Import link in sidebar)
3. Enter an account name (e.g., "Chase Visa")
4. Drag-and-drop 2-3 PDF bank statements onto the dropzone
5. Click "Process Files" button
6. Verify:
   - Files appear in the queue with "Waiting" status
   - Each file shows progress (Hashing -> Checking -> Uploading -> Processing)
   - Files complete one at a time (sequential processing)
   - Completion summary shows file count and transaction count
   - If you upload the same file again, it should show as "Duplicate"

7. Check database:
   - Run `npm run db:studio` to open Drizzle Studio
   - Verify `statements` table has records
   - Verify `transactions` table has records linked to statements

8. Expected behavior for 2 PDF files:
   - Both files queue up
   - First file processes completely before second starts
   - Progress bars show incremental progress
   - Final summary shows "X files processed, Y transactions found"
  </how-to-verify>
  <resume-signal>Type "verified" if batch upload works, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
1. Page compiles: `npx tsc --noEmit`
2. Navigation link works
3. BatchUploader renders on page
4. End-to-end flow: drag files -> process -> completion
5. Database records created in statements and transactions tables
</verification>

<success_criteria>
- User can access /import/batch from navigation
- User can drag-and-drop multiple PDF files
- Files process sequentially with per-file progress
- Duplicate files are detected and can be skipped
- Completion summary shows total files and transactions
- Database contains statement and transaction records
</success_criteria>

<output>
After completion, create `.planning/phases/19-batch-upload-foundation/19-05-SUMMARY.md`
</output>
