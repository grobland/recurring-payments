---
phase: 12-mobile-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/subscriptions/page.tsx
  - src/app/(dashboard)/subscriptions/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard page has reduced spacing on mobile (p-4) that increases on desktop (md:p-6)"
    - "All interactive elements have minimum 44px touch target height"
    - "Subscription list items have adequate touch-friendly padding on mobile"
    - "Subscription detail page cards have responsive padding"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Touch target utility class"
      contains: "touch-target"
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Mobile-optimized dashboard"
      contains: "p-4 md:p-6"
    - path: "src/app/(dashboard)/subscriptions/page.tsx"
      provides: "Mobile-optimized subscriptions list"
      contains: "p-4 md:p-6"
  key_links:
    - from: "globals.css"
      to: "all pages"
      via: "Tailwind CSS import"
      pattern: "touch-target"
---

<objective>
Add mobile-optimized spacing patterns and touch target utilities to dashboard pages.

Purpose: Users on mobile devices need comfortable touch targets (44px minimum) and appropriate spacing that doesn't feel cramped but also doesn't waste precious screen real estate.

Output: Dashboard, subscriptions list, and subscription detail pages with responsive spacing (`p-4 md:p-6` pattern) and touch-target utility available globally.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-mobile-visual-polish/12-RESEARCH.md

@src/app/globals.css
@src/app/(dashboard)/dashboard/page.tsx
@src/app/(dashboard)/subscriptions/page.tsx
@src/app/(dashboard)/subscriptions/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add touch-target utility to globals.css</name>
  <files>src/app/globals.css</files>
  <action>
Add a custom utility class for touch targets at the end of globals.css:

```css
@layer utilities {
  .touch-target {
    min-height: 44px;
    min-width: 44px;
  }
}
```

This provides a reusable utility for ensuring interactive elements meet the 44px minimum touch target size recommended by Apple HIG.

Do NOT modify the existing @theme or :root sections - only add the new @layer utilities block at the end.
  </action>
  <verify>The globals.css file contains the new `.touch-target` utility class within a `@layer utilities` block.</verify>
  <done>Touch-target utility class is available for use across all components.</done>
</task>

<task type="auto">
  <name>Task 2: Update Dashboard page with mobile-optimized spacing</name>
  <files>src/app/(dashboard)/dashboard/page.tsx</files>
  <action>
Update the Dashboard page to use mobile-first responsive spacing:

1. Change main container from `space-y-6 p-6` to `space-y-4 p-4 md:space-y-6 md:p-6`

2. In the service unavailable fallback, also update to `p-4 md:p-6`

3. For the trial banner, update padding from `p-4` to `p-3 md:p-4` for tighter mobile spacing

4. For the "Upcoming Renewals" list items (Link components inside the card), ensure they have adequate touch targets by adding `min-h-[44px]` to the existing `p-2` (making touch area larger)

5. For the "Quick Actions" buttons, they're already w-full which is good. The shadcn Button default height is h-10 (40px), which is close. Add `h-11` to make them 44px for better touch targets.

Key changes:
- Line ~55 (ServiceUnavailable fallback): `<main className="flex-1 p-4 md:p-6">`
- Line ~69: `<main className="flex-1 space-y-4 p-4 md:space-y-6 md:p-6">`
- Line ~72: Trial banner `p-3 md:p-4`
- Line ~243: Upcoming renewal Link items add `min-h-[44px]`
- Lines ~291-303: Quick action buttons add `h-11` class
  </action>
  <verify>
1. Run `npm run build` to ensure no TypeScript/build errors
2. Verify responsive spacing pattern exists: `grep -q "p-4 md:p-6" src/app/\(dashboard\)/dashboard/page.tsx && echo "PASS" || echo "FAIL"`
3. Verify touch target height exists: `grep -q "h-11" src/app/\(dashboard\)/dashboard/page.tsx && echo "PASS" || echo "FAIL"`
  </verify>
  <done>Dashboard page uses responsive spacing with tighter mobile values and touch-friendly interactive elements.</done>
</task>

<task type="auto">
  <name>Task 3: Update Subscriptions list and detail pages with mobile spacing</name>
  <files>
src/app/(dashboard)/subscriptions/page.tsx
src/app/(dashboard)/subscriptions/[id]/page.tsx
  </files>
  <action>
**Subscriptions List Page (subscriptions/page.tsx):**

1. Line ~139: Change main container from `space-y-6 p-6` to `space-y-4 p-4 md:space-y-6 md:p-6`

2. The filter select triggers have fixed widths like `w-[130px]` and `w-[150px]`. On mobile these can cause horizontal overflow. Wrap the filter row better:
   - Line ~191: The outer flex already has `flex-col gap-4 lg:flex-row` which is good
   - Line ~201: The inner flex-wrap is good. Make select triggers use `w-full sm:w-[130px]` pattern for the status/frequency/sort-by selects, and `w-full sm:w-[150px]` for category select

3. For the table on mobile, tables are inherently problematic. Add horizontal scroll wrapper:
   - Wrap the Table in a `<div className="overflow-x-auto">` for the border div that contains it

4. The Add Subscription button in header - ensure it has touch target sizing by adding `h-11`

**Subscription Detail Page (subscriptions/[id]/page.tsx):**

1. Line ~73, ~94, ~124: Change main container padding from `p-6` to `p-4 md:p-6`

2. Line ~127: The header flex with icon and title - on mobile the flex items can get cramped. Add `flex-col sm:flex-row gap-4` pattern for the outer container and buttons

3. Line ~150: The action buttons (Edit/Delete) - stack vertically on mobile:
   Change `<div className="flex gap-2">` to `<div className="flex flex-col-reverse sm:flex-row gap-2">`

4. Ensure Edit and Delete buttons have `h-11` for touch targets

5. The grid layouts in cards (Cost, Renewal) already use `sm:grid-cols-3` and `sm:grid-cols-2` which is mobile-first - good, no changes needed there
  </action>
  <verify>
1. Run `npm run build` to ensure no TypeScript/build errors
2. Verify subscriptions list has pattern: `grep -q "p-4 md:p-6" src/app/\(dashboard\)/subscriptions/page.tsx && echo "PASS" || echo "FAIL"`
3. Verify detail page has pattern: `grep -q "p-4 md:p-6" src/app/\(dashboard\)/subscriptions/\[id\]/page.tsx && echo "PASS" || echo "FAIL"`
  </verify>
  <done>Subscriptions list and detail pages have mobile-optimized spacing and touch-friendly layouts.</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. `npm run build` completes without errors
2. Dashboard page has `space-y-4 p-4 md:space-y-6 md:p-6` pattern
3. Subscriptions pages have mobile-responsive spacing
4. globals.css contains touch-target utility
5. Interactive elements use h-11 (44px) for touch targets
</verification>

<success_criteria>
- All three pages use responsive spacing pattern (tighter on mobile, roomier on desktop)
- Touch-target utility is available globally
- No build errors
- Quick action buttons, Add Subscription button, and Edit/Delete buttons have 44px height
- Detail page buttons stack vertically on mobile, horizontally on desktop
</success_criteria>

<output>
After completion, create `.planning/phases/12-mobile-visual-polish/12-01-SUMMARY.md`
</output>
