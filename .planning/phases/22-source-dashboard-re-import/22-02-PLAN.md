---
phase: 22-source-dashboard-re-import
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/components/sources/source-dashboard.tsx
  - src/components/sources/source-list.tsx
  - src/components/sources/source-row.tsx
  - src/components/sources/coverage-gap-warning.tsx
  - src/app/(dashboard)/sources/page.tsx
autonomous: true

must_haves:
  truths:
    - "User sees expandable list of sources on /sources page"
    - "Each source row shows date range and transaction counts"
    - "Gaps are highlighted with warning badges"
    - "Expanding a source shows its statements"
  artifacts:
    - path: "src/app/(dashboard)/sources/page.tsx"
      provides: "Sources dashboard page"
      min_lines: 20
    - path: "src/components/sources/source-list.tsx"
      provides: "Accordion-based source list"
      contains: "AccordionItem"
    - path: "src/components/sources/source-row.tsx"
      provides: "Source row with coverage summary"
      contains: "SourceCoverage"
  key_links:
    - from: "src/components/sources/source-dashboard.tsx"
      to: "useSources"
      via: "hook import"
      pattern: "useSources"
    - from: "src/components/sources/source-list.tsx"
      to: "Accordion"
      via: "component import"
      pattern: "from.*accordion"
---

<objective>
Create the source dashboard UI with expandable accordion rows showing statement coverage by source.

Purpose: Allow users to see all their imported statement sources with coverage stats, gaps, and ability to drill into specific statements.
Output: Source dashboard page at /sources with accordion list, source rows, and gap warnings.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-source-dashboard-re-import/22-CONTEXT.md
@.planning/phases/22-source-dashboard-re-import/22-RESEARCH.md
@src/components/ui/accordion.tsx
@src/lib/hooks/use-sources.ts
@src/types/source.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create source row and gap warning components</name>
  <files>
    src/components/sources/source-row.tsx
    src/components/sources/coverage-gap-warning.tsx
  </files>
  <action>
Create CoverageGapWarning component (src/components/sources/coverage-gap-warning.tsx):
- Accept gaps: string[] prop
- If no gaps, render null
- Display warning badge showing gap count
- On hover or expand, show missing months (max 3 with +N more)
- Use amber/warning colors per existing Badge patterns

Create SourceRow component (src/components/sources/source-row.tsx):
- Accept source: SourceCoverage prop
- Display source name (sourceType) as primary text
- Show date range: "Jan 2024 - Feb 2026" format using date-fns
- Show secondary info: "{N} statements / {M} transactions"
- Show stats: "{X} converted / {Y} pending / {Z} skipped"
- Include CoverageGapWarning if gaps exist
- Include "Last import: {date}" in muted text

Layout should be compact but readable:
```
[Source Name]                    [Gap badge if any]
Jan 2024 - Feb 2026
12 statements / 156 transactions
8 converted • 4 pending • 2 skipped
Last import: Feb 8, 2026
```
  </action>
  <verify>
Components render correctly with mock data in Storybook or test page
  </verify>
  <done>
SourceRow displays all coverage info, CoverageGapWarning shows gaps with proper styling
  </done>
</task>

<task type="auto">
  <name>Task 2: Create source list with accordion and statement list</name>
  <files>
    src/components/sources/source-list.tsx
    src/components/sources/statement-list.tsx
  </files>
  <action>
Create SourceList component (src/components/sources/source-list.tsx):
- Use shadcn/ui Accordion with type="single" collapsible
- Map over sources to create AccordionItems
- AccordionTrigger contains SourceRow (without underline on hover)
- AccordionContent contains StatementList for the expanded source
- Handle empty state: "No statement sources found. Import statements to get started."

Create StatementList component (src/components/sources/statement-list.tsx):
- Accept sourceType prop
- Use useSourceStatements hook to fetch statements
- Display loading skeleton while fetching
- Render list of statement rows with:
  - Original filename
  - Statement date
  - Transaction count with status breakdown
  - "View details" link that navigates to statement detail
- Sort by statement date desc (newest first)

Statement row format:
```
[filename.pdf]                   [View details ->]
Feb 2026 • 47 transactions
12 converted • 8 skipped • 27 pending
```
  </action>
  <verify>
Accordion expands/collapses, statements load when source is expanded
  </verify>
  <done>
SourceList renders accordion with expandable sources, StatementList fetches and displays statements
  </done>
</task>

<task type="auto">
  <name>Task 3: Create source dashboard page</name>
  <files>
    src/components/sources/source-dashboard.tsx
    src/app/(dashboard)/sources/page.tsx
  </files>
  <action>
Create SourceDashboard component (src/components/sources/source-dashboard.tsx):
- Use useSources hook to fetch source coverage data
- Show loading skeleton while fetching (use existing skeleton patterns)
- Render SourceList with sources data
- Handle error state with toast or inline message
- Add page header: "Statement Sources"
- Add subtitle: "View your imported statements organized by source"

Create /sources page (src/app/(dashboard)/sources/page.tsx):
- Page metadata: title="Statement Sources"
- Render SourceDashboard component
- Follow existing dashboard page patterns (container, padding, max-width)

Add navigation link to sources page in the sidebar/nav:
- Find existing sidebar component
- Add "Statement Sources" link with appropriate icon (FileStack or similar from lucide)
- Position appropriately in nav hierarchy (after Transactions)
  </action>
  <verify>
Navigate to /sources, see list of sources with coverage stats, expand to see statements
  </verify>
  <done>
/sources page renders dashboard with all sources, expandable to show statements
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Navigate to /sources shows source dashboard
3. Sources display with coverage dates, counts, and gaps
4. Expanding source shows list of statements with stats
5. Empty state displays when no sources exist
</verification>

<success_criteria>
- /sources page accessible from dashboard navigation
- Source list uses accordion for expand/collapse
- Source rows show date range, counts, and gap warnings
- Statements load lazily when source is expanded
- UI matches CONTEXT.md decisions (list layout, not card grid)
</success_criteria>

<output>
After completion, create `.planning/phases/22-source-dashboard-re-import/22-02-SUMMARY.md`
</output>
