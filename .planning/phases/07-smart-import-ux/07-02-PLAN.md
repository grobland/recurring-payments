---
phase: 07-smart-import-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/badge.tsx
autonomous: true

must_haves:
  truths:
    - "Badge component accepts 'success' variant"
    - "Badge component accepts 'warning' variant"
    - "Success variant renders green background"
    - "Warning variant renders yellow background"
  artifacts:
    - path: "src/components/ui/badge.tsx"
      provides: "Extended Badge variants"
      contains: "success"
  key_links:
    - from: "src/components/ui/badge.tsx"
      to: "badgeVariants"
      via: "CVA variants config"
      pattern: "success.*bg-green"
---

<objective>
Extend Badge component with success and warning variants for confidence score visualization.

Purpose: Enable semantic color-coded badges for high (green), medium (yellow), and low (red/destructive) confidence scores.
Output: Badge component with success and warning variants matching design system patterns.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-smart-import-ux/07-RESEARCH.md

@src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add success and warning variants to Badge component</name>
  <files>src/components/ui/badge.tsx</files>
  <action>
Update the `badgeVariants` CVA config to add two new variants after the existing `outline` variant:

```typescript
const badgeVariants = cva(
  "inline-flex items-center justify-center rounded-full border px-2 py-0.5 text-xs font-medium w-fit whitespace-nowrap shrink-0 [&>svg]:size-3 gap-1 [&>svg]:pointer-events-none focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive transition-[color,box-shadow] overflow-hidden",
  {
    variants: {
      variant: {
        default:
          "border-transparent bg-primary text-primary-foreground [a&]:hover:bg-primary/90",
        secondary:
          "border-transparent bg-secondary text-secondary-foreground [a&]:hover:bg-secondary/90",
        destructive:
          "border-transparent bg-destructive text-white [a&]:hover:bg-destructive/90 focus-visible:ring-destructive/20 dark:focus-visible:ring-destructive/40 dark:bg-destructive/60",
        outline:
          "text-foreground [a&]:hover:bg-accent [a&]:hover:text-accent-foreground",
        // NEW - Confidence score variants
        success:
          "border-transparent bg-green-500 text-white [a&]:hover:bg-green-600 dark:bg-green-600 dark:[a&]:hover:bg-green-700",
        warning:
          "border-transparent bg-yellow-500 text-black [a&]:hover:bg-yellow-600 dark:bg-yellow-600 dark:text-white dark:[a&]:hover:bg-yellow-700",
      },
    },
    defaultVariants: {
      variant: "default",
    },
  }
)
```

Key styling notes:
- `success`: Green background (#22c55e / green-500), white text, darker on hover
- `warning`: Yellow background (#eab308 / yellow-500), black text (for contrast), switches to white text in dark mode
- Both variants follow the pattern of existing variants with hover states and dark mode support
  </action>
  <verify>
- TypeScript compilation passes: `npx tsc --noEmit`
- Badge component can be imported and used with new variants:
  ```tsx
  <Badge variant="success">85%</Badge>
  <Badge variant="warning">55%</Badge>
  <Badge variant="destructive">25%</Badge>
  ```
  </verify>
  <done>
- Badge component has success variant (green background)
- Badge component has warning variant (yellow background)
- TypeScript recognizes both variants as valid
- Styling matches design system (dark mode support, hover states)
  </done>
</task>

</tasks>

<verification>
1. TypeScript: `npx tsc --noEmit` passes
2. Visual: Start dev server, create test usage of Badge with success/warning/destructive variants
3. Dark mode: Verify variants look correct in both light and dark modes
</verification>

<success_criteria>
- Badge component exports success and warning variants
- TypeScript autocomplete suggests all variants correctly
- Colors match design: green for success, yellow for warning, red for destructive
</success_criteria>

<output>
After completion, create `.planning/phases/07-smart-import-ux/07-02-SUMMARY.md`
</output>
