---
phase: 28-voucher-system
plan: 03
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - docs/admin/voucher-workflow.md
autonomous: false

must_haves:
  truths:
    - "Checkout page shows promo code input field (VCHR-02)"
    - "Admin can create voucher codes via Stripe Dashboard workflow (VCHR-01)"
    - "Promo codes apply discount correctly during checkout"
    - "Voucher workflow is documented for future admin reference"
  artifacts:
    - path: "docs/admin/voucher-workflow.md"
      provides: "Admin documentation for creating voucher codes in Stripe"
      min_lines: 30
  key_links:
    - from: "src/app/api/billing/create-checkout/route.ts"
      to: "Stripe Checkout Session"
      via: "allow_promotion_codes: true"
      pattern: "allow_promotion_codes.*true"
---

<objective>
Verify Stripe promo code checkout integration and document the voucher creation workflow for admins.

Purpose: Confirm that checkout promo codes work end-to-end and create persistent documentation so admins know how to create vouchers in Stripe Dashboard without custom UI.

Output: Verified checkout promo code support, documented admin workflow in docs/admin/voucher-workflow.md.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-voucher-system/28-CONTEXT.md
@.planning/phases/28-voucher-system/28-RESEARCH.md
@.planning/phases/28-voucher-system/28-01-SUMMARY.md
@.planning/phases/28-voucher-system/28-02-SUMMARY.md

Key files:
@src/app/api/billing/create-checkout/route.ts (checkout session creation)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Stripe checkout promo code field is functional (VCHR-02)</name>
  <what-built>
    Per CONTEXT.md, checkout promo codes use Stripe's native `allow_promotion_codes` field. This should already be enabled in the create-checkout route. This task verifies it works end-to-end.
  </what-built>
  <how-to-verify>
    1. Go to Stripe Dashboard -> Products -> Coupons
    2. Create a test coupon (e.g., 10% off, code: TESTPROMO)
    3. Under the coupon, create a Promotion Code with that code
    4. Start checkout flow in the app (click upgrade/subscribe)
    5. On Stripe checkout page, look for "Add promotion code" link
    6. Enter TESTPROMO and verify discount is applied
    7. Cancel checkout (do not complete purchase)
    8. Delete the test coupon in Stripe Dashboard

    If step 5 fails (no promo code field visible):
    - Check src/app/api/billing/create-checkout/route.ts
    - Ensure `allow_promotion_codes: true` is set in checkout session
    - If missing, add it and re-test
  </how-to-verify>
  <resume-signal>Type "promo code verified" if field appears and works, or describe what's missing</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Document Stripe Dashboard voucher creation workflow (VCHR-01)</name>
  <what-built>
    Per CONTEXT.md decisions, vouchers are created in Stripe Dashboard only (no custom admin UI). This task documents the workflow for admins and verifies the process works.
  </what-built>
  <how-to-verify>
    Verify admin can create vouchers in Stripe Dashboard:

    **For percentage-off discounts:**
    1. Go to Stripe Dashboard -> Products -> Coupons
    2. Click "Create coupon"
    3. Select "Percentage off" and enter amount (e.g., 25%)
    4. Set duration (once, repeating, forever)
    5. Set redemption limits and expiration date
    6. Save coupon
    7. Click into coupon -> Promotion codes -> Create code
    8. Enter memorable code (e.g., SUMMER25)
    9. Set any per-code limits
    10. Save - code is now ready to share

    **For free months (100% off for X months):**
    1. Create coupon with 100% off
    2. Set duration to "repeating" with N months
    3. Create promotion code as above

    **To share:**
    - Copy the promotion code from Stripe Dashboard
    - Share with user manually (email, support chat, etc.)
    - User enters code during checkout

    Verify by creating a test coupon and promotion code, then delete it.
  </how-to-verify>
  <resume-signal>Type "stripe voucher workflow verified" when you've confirmed the workflow works, or describe any issues</resume-signal>
  <done>
    After verification, create docs/admin/voucher-workflow.md with the following content:

    ```markdown
    # Voucher Creation Workflow

    This document describes how to create promotional voucher codes for the subscription manager.

    ## Overview

    Vouchers are managed entirely in the Stripe Dashboard. There is no custom admin UI for voucher creation - this keeps the system simple and leverages Stripe's built-in coupon/promotion code management.

    ## Creating a Discount Voucher

    ### Step 1: Create a Coupon

    1. Go to [Stripe Dashboard](https://dashboard.stripe.com) -> Products -> Coupons
    2. Click **Create coupon**
    3. Configure the discount:
       - **Percentage off**: e.g., 25% off
       - **Amount off**: e.g., $5 off (for fixed discounts)
    4. Set **Duration**:
       - **Once**: Applies to first payment only
       - **Repeating**: Applies for X months
       - **Forever**: Applies to all future payments
    5. (Optional) Set redemption limits and expiration date
    6. Click **Create coupon**

    ### Step 2: Create a Promotion Code

    1. Click into the coupon you just created
    2. Go to **Promotion codes** tab
    3. Click **Create code**
    4. Enter a memorable code (e.g., `SUMMER25`, `WELCOME10`)
    5. (Optional) Set per-code limits:
       - Max redemptions
       - First-time customers only
       - Minimum order amount
    6. Click **Create**

    ## Common Voucher Types

    ### Percentage Discount (e.g., 25% off first 3 months)

    - Coupon: 25% off, duration "repeating" for 3 months
    - Promotion code: `SAVE25`

    ### Free Trial Extension (e.g., 1 month free)

    - Coupon: 100% off, duration "repeating" for 1 month
    - Promotion code: `FREEMONTH`

    ### Forever Discount (e.g., 10% off all payments)

    - Coupon: 10% off, duration "forever"
    - Promotion code: `LOYAL10`

    ## Sharing Vouchers

    Once created, share the promotion code with users via:
    - Email
    - Support chat
    - Social media campaigns
    - Partner referrals

    Users enter the code during checkout in the "Add promotion code" field.

    ## Tracking Usage

    View redemption stats in Stripe Dashboard:
    - Coupons page shows total redemptions per coupon
    - Click into a coupon to see individual promotion codes and their usage
    - View in Payments section to see which payments used a coupon

    ## Notes

    - Promotion codes are case-insensitive
    - Each coupon can have multiple promotion codes with different restrictions
    - Deleted coupons cannot be recovered - use expiration dates instead
    - Test in Stripe Test Mode before creating production coupons
    ```
  </done>
</task>

</tasks>

<verification>
After all tasks:

1. Checkout promo verification:
   - Checkout page shows "Add promotion code" link
   - Test promo code applies discount correctly
   - VCHR-02 requirement satisfied

2. Stripe voucher workflow:
   - Admin can create coupons and promotion codes in Stripe Dashboard
   - Codes work during checkout
   - docs/admin/voucher-workflow.md exists with workflow documentation
   - VCHR-01 requirement satisfied

3. Documentation artifact:
   - docs/admin/voucher-workflow.md exists
   - Contains step-by-step instructions for coupon/promotion code creation
   - Contains common voucher type examples
</verification>

<success_criteria>
- Checkout page shows promo code input field (VCHR-02)
- Promo codes apply discount correctly during checkout
- Admin can create vouchers in Stripe Dashboard (VCHR-01)
- docs/admin/voucher-workflow.md exists with complete workflow documentation
- Documentation covers coupon creation, promotion codes, and common voucher types
</success_criteria>

<output>
After completion, create `.planning/phases/28-voucher-system/28-03-SUMMARY.md`
</output>
