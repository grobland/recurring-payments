---
phase: 25-multi-tier-product-setup
plan: 05
type: execute
wave: 2
depends_on: ["25-04"]
files_modified:
  - src/app/(marketing)/pricing/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Pricing page displays 3 tiers (Primary/Enhanced/Advanced)"
    - "Each tier shows its features from TIER_CONFIG"
    - "Prices are fetched from database, not hardcoded"
    - "Interval toggle switches between monthly and annual pricing"
  artifacts:
    - path: "src/app/(marketing)/pricing/page.tsx"
      provides: "Multi-tier public pricing page"
      min_lines: 150
  key_links:
    - from: "src/app/(marketing)/pricing/page.tsx"
      to: "/api/billing/prices"
      via: "fetch GET or server component data fetch"
      pattern: "fetch.*api/billing/prices|prices.*tiers"
    - from: "src/app/(marketing)/pricing/page.tsx"
      to: "TIER_CONFIG"
      via: "import for features/descriptions"
      pattern: "import.*TIER_CONFIG"
---

<objective>
Update marketing pricing page to display 3 subscription tiers with database-driven pricing

Purpose: Close verification gap - pricing page currently imports non-existent PRICING export and shows only 2 plans instead of 3 tiers

Output: Public pricing page showing Primary/Enhanced/Advanced tiers with correct pricing from database and interval toggle
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-multi-tier-product-setup/25-04-PLAN.md
@src/lib/stripe/products.ts
@src/app/api/billing/prices/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite pricing page for multi-tier display</name>
  <files>src/app/(marketing)/pricing/page.tsx</files>
  <action>
Rewrite the marketing pricing page to display 3 tiers with database-driven pricing.

This is a server component, so it can fetch prices directly.

Remove:
- Import of PRICING from products.ts (does not exist)
- Hardcoded pricing values
- Two-card layout (monthly/annual)

Add:
- Import TIER_CONFIG, formatPrice from products.ts
- Fetch prices from /api/billing/prices (internal fetch in server component) or query database directly
- Three-tier card layout with tier comparison
- Client component for interval toggle (needs useState)

Structure:
1. Header section (existing - keep title and description)
2. Interval toggle (client component) - Monthly | Annual with savings badge
3. Three tier cards in grid:
   - Primary: Essential subscription tracking - $4/mo or $40/yr
   - Enhanced: General banking insights - $7/mo or $70/yr
   - Advanced: Full financial picture - $11/mo or $110/yr
4. Each card shows:
   - Tier name and tagline
   - Price (dynamic based on selected interval)
   - Features list from TIER_CONFIG
   - CTA button "Start free trial" linking to /register
5. Feature comparison table (optional, nice-to-have)
6. FAQ section (keep existing)
7. Final CTA section (keep existing)

Implementation approach:
- Create a client component PricingTiers that handles interval state
- Server component fetches prices and passes to PricingTiers
- Or: Make entire page a client component with useEffect fetch

Price display:
- Show selected currency price (default USD)
- For annual: show monthly equivalent "That's $X/month"
- Show savings percentage on annual plans

Keep existing:
- Metadata export
- FAQ section
- "Ready to take control?" CTA section
- Overall page styling approach
  </action>
  <verify>
1. npm run build compiles without errors
2. Visit /pricing - see 3 tier cards with correct prices
3. Toggle interval - prices update between monthly and annual
4. Click "Start free trial" - navigates to /register
  </verify>
  <done>
Pricing page displays Primary/Enhanced/Advanced tiers with database-driven pricing and working interval toggle
  </done>
</task>

<task type="auto">
  <name>Task 2: Add feature comparison highlight</name>
  <files>src/app/(marketing)/pricing/page.tsx</files>
  <action>
Enhance the pricing page with visual tier differentiation:

1. Add "Most Popular" badge to Enhanced tier (middle option)
2. Highlight Advanced tier with premium styling (subtle gradient or border)
3. Ensure clear visual hierarchy: Primary (basic) < Enhanced (highlighted) < Advanced (premium)

Visual treatment:
- Primary: Standard border, outline button
- Enhanced: Primary color border, "Most Popular" badge, filled button
- Advanced: Gradient background or gold accent, filled button

No new files - enhance existing PricingTiers component styling.
  </action>
  <verify>Visit /pricing - Enhanced tier has "Most Popular" badge and highlighted styling</verify>
  <done>Tier cards have visual hierarchy with Enhanced highlighted as recommended option</done>
</task>

</tasks>

<verification>
1. **Build passes:** `npm run build` completes without TypeScript errors
2. **Three tiers displayed:** Pricing page shows Primary, Enhanced, Advanced
3. **Prices from database:** Displayed amounts match stripe_prices table
4. **Interval toggle works:** Switching updates all prices
5. **Visual hierarchy:** Enhanced tier highlighted, clear tier progression
6. **Navigation works:** CTA buttons link to /register
</verification>

<success_criteria>
- No TypeScript errors (PRICING import removed)
- Three tiers displayed with correct names and features
- Prices fetched from database, not hardcoded
- Interval toggle functional
- Enhanced tier highlighted as recommended
- All CTAs link to /register for free trial
</success_criteria>

<output>
After completion, create `.planning/phases/25-multi-tier-product-setup/25-05-SUMMARY.md`
</output>
