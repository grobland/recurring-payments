import { RegisterForm } from "./register-form";

export const metadata = {
  title: "Sign Up - Subscription Manager",
  description: "Create your account",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
